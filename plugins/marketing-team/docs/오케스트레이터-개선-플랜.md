# 마케팅팀 오케스트레이터 개선 플랜

> 작성일: 2026-08-30
> 마지막 정리: 2026-08-31
> 대상: `marketing-team` v0.50.0
> 관련 문서: [런타임 안정성 수정 내역](./런타임-안정성-수정내역-2026-08-30.md)
> 목표: AI 마케터의 판단력은 유지하면서 라우팅·승인·체인·검토·완료를 재현 가능한 실행 시스템으로 만든다.

## 1. 한 줄 결론

현재 구조는 **상태와 계약으로 실행을 통제하는 오케스트레이터**까지 올라왔다.
P0인 계획 봉인·체인 그래프·단계 재개는 구현됐고, 설치본 새 대화 실기 3종(초급/중급/고급)도
2026-08-30 에 완주했다 (`실제확인-기록/9093422.md`). 남은 핵심은 실제 사용자 표본과
실측 기반 하드 예산·반복 오류 회로 차단이다.

```text
구현 상태  P0 완료 · P1 부분 완료(반복 오류 회로 차단 잔여) · 설치본 실기 3종 완료 (2026-08-30)
기계 검증  verify 오류 0 · plugin validate 통과 · CI=1 RELEASE=1 게이트 통과 (v0.50.0)
남은 관문  실제 사용자 표본 (초급 5명 기준 첫 업무 시작 성공률 90% · §13 P3)
```

| 작업 트랙 | 현재 상태 | 다음 행동 |
|---|---|---|
| 실행 안정성 | P0 완료 · 설치본 실기 3종 완료 · P1 부분 완료 | 반복 오류 회로 차단 · 도구 호출 자동 계측 |
| 첫 사용 경험 | P0·P1 구현·실측 완료 (v0.49.0 · 2026-08-31) · P2 부분 | 대표 요청 10개 상시 회귀화 · P3 사용자 검증 |
| 일일 자가검증 | M0·M1 완료 (실행기·회귀·정책) · 예약 운영 전 | M2 격리 worktree · M3 Codex 예약 · M4 수정 루프 |
| 현장 실효성 | 설계 점수 83/100 · 사용자 표본 없음 | 실제 마케터 비교 테스트로 시간·완주율·사용성 측정 |

새 실행 담당이나 가상의 조직은 추가하지 않는다.

```text
사용자
  ↓
AI 마케터                    판단·제작·보고
  ↓
오케스트레이션 제어층         라우팅·계획 봉인·상태 전이·예산·복구
  ├─ AI 사업검토자            사업 판단
  └─ AI 규제검토자            규제·개인정보 판단
```

스킬 100번 `나만의 마케팅 에이전트 구축`은 사용자의 업무 배선 설계다.
이 문서가 개선하는 것은 그와 다른 **플러그인 내부 런타임 제어층**이다.

## 2. 현재 구조 진단

### 잘되어 있는 것

- `ROUTING.md` 한 장만 상시 읽고 선택된 스킬만 여는 Progressive Disclosure 구조
- G1~G5로 사용자 결재 지점을 단순화한 대화 흐름
- `review`, `gate`, `pii`, `writes_to`가 스킬 frontmatter에 선언됨
- AI 마케터와 독립 검토 담당의 제작·판정 권한 분리
- `run.json`과 SHA-256으로 입력·산출물·검토 보고서의 동일성 확인
- PreToolUse 훅으로 G2 승인 전 쓰기·실행 차단
- 샘플·추정·자료 충돌·기간 불일치에 대한 안전 규칙

### 남은 구조적 한계

| 영역 | 현재 | 위험 |
|---|---|---|
| 라우팅 | AI 의미 판단 + 로컬 후보 라우터 + 596건 회귀 | 새 복합 표현의 분해·동률 질문은 설치본 실기가 더 필요 |
| 계획 | `plan/v1` 해시와 승인 봉인 | G2를 구조화 계획에서 렌더링하는 실제 대화 준수 여부 |
| 체인 | 정본 15개와 입력 의존 DAG 기계 검사 | 이름 없는 즉석 체인의 업무적 타당성은 여전히 AI 판단 |
| 실행 상태 | 단계별 상태·입력 지문·선택적 재개 | 화면 예상 남은 시간은 없음 |
| 실행 예산 | 시간·검토 자동 계측, 도구 호출 수동 계측·소프트 경고 | 자동 호출 계측과 실측 기반 하드 상한 없음 |
| 검토 | frontmatter에서 산출물별 요구 자동 생성 | 외부 검토 담당의 실제 호출은 설치 환경에 좌우됨 |
| 복구 | 변경 영향을 받은 단계만 무효화하고 재개 | 프로세스 강제 종료 직전 이벤트 유실 가능 |
| 관측 | 원장 + `orchestrator-events.jsonl` + 요약 명령 | 장기 집계·대시보드 없음 |
| 테스트 | 라우팅·계획·체인·단계·검토·PII·이벤트 회귀 · 설치본 실기 3종 완료 | 실제 사용자 표본 미완 |

### 일반 마케터 기준 최우선 개선 5가지

일반 사용자는 스킬 번호·frontmatter·영수증·훅을 몰라도 된다. 아래 다섯 가지가 화면 밖에서
정상 작동해야 “하고 싶은 일을 말하면 결과가 나오는 플러그인”이 된다.

| 우선순위 | 개선 과제 | 일반 마케터에게 중요한 이유 | 완료 기준 | 연결 Phase |
|---|---|---|---|---|
| **1** | **자연어 라우팅 정확도** | 사용자는 006·046 같은 번호를 모른다. 평소 말로 요청해도 정확한 업무가 선택돼야 한다. | 주요 요청 Top-1 95% 이상 · Top-2 99% 이상 · 애매할 때 질문 1회 | Phase 0·2 |
| **2** | **계획·승인·실행 일치** | 승인한 계획과 실제 스킬·순서·산출물이 다르면 결과를 신뢰할 수 없다. | 계획 해시 봉인 · 변경 시 재승인 · 승인 전 실행 0건 | Phase 1 |
| **3** | **결과물 완주와 중단 복구** | 좋은 대화보다 파일이 실제로 남고 다음 날 다시 열리는 것이 중요하다. | 파일·검토·원장·영수증 생성 · 중단 지점부터 재개 · 완료 오표기 0건 | Phase 3 |
| **4** | **데이터 입력 경험과 안전성** | CSV 위치·기간·열 이름을 모르면 첫 사용에서 막힌다. 샘플 오인과 개인정보 유출도 위험하다. | 채널별 가져오기 안내 · 입력 자동 탐색 · 샘플 표시 · 기간 충돌·PII 자동 차단 | Phase 1·4 |
| **5** | **실행 속도와 설치본 일관성** | 오래 멈추거나 개발본·캐시본이 다르면 사용자는 플러그인이 고장 났다고 판단한다. | 진행 상태·실행 예산·안전 중단 · GitHub 설치본 회귀 통과 | Phase 0·4·5·6 |

사용자 가치 기준 실행 순서:

```text
자연어 라우팅 기준선
 → 계획 스키마·승인 해시·상태 머신
 → 단계별 완주·중단 복구
 → 데이터 가져오기·개인정보 안전
 → 실행 예산·설치 및 업데이트 검증
```

스킬을 더 추가하는 것보다 이 다섯 가지를 먼저 개선한다. 현재 플러그인의 가장 큰 위험은
기능 부족이 아니라 **요청부터 완료까지의 연결이 모델 판단에 많이 의존한다는 것**이다.

## 3. 목표 원칙

1. **판단은 AI, 전이는 기계** — 무엇을 할지는 AI가 제안하지만 다음 상태로 갈 수 있는지는 코드가 결정한다.
2. **계획과 실행의 동일성** — 승인받은 스킬·순서·입력·산출물·검토 정책만 실행한다.
3. **작은 명령과 점진적 공개** — 거대한 프롬프트 한 번 대신 라우팅·계획·실행·검증을 작은 단계로 나눈다.
4. **중간 산출물도 계약** — 체인의 앞 단계 출력이 다음 단계 입력으로 실제 연결됐는지 기록한다.
5. **실패를 정상 상태로 취급** — 막힘·중단·예산 초과를 숨기지 않고 재개 가능한 상태로 남긴다.
6. **설치본 한 곳** — 실행 중 패키지 루트는 `${CLAUDE_PLUGIN_ROOT}` 하나다.
7. **기본은 보수적** — 승인·검토·PII가 불확실하면 통과가 아니라 정지다.
8. **기존 사용자 경험 유지** — 사용자는 계속 AI 마케터 한 명과 대화한다.

## 4. 목표 상태 머신

```text
INTAKE
  ↓
ROUTED
  ↓
PLAN_READY
  ↓
AWAITING_APPROVAL ── 사용자 수정 ──→ PLAN_READY
  ↓ 진행 승인
APPROVED
  ↓
RUNNING(step 1..n)
  ├─ 예산 초과/자료 부족 ──→ BLOCKED
  ├─ 도구 오류 ───────────→ INTERRUPTED
  └─ 단계 완료 ───────────→ REVIEWING 또는 다음 step
                               ↓
                            FINALIZING
                               ↓
                            COMPLETED
```

허용 전이는 코드로 고정한다.

| 현재 상태 | 허용되는 다음 상태 | 금지 예시 |
|---|---|---|
| `draft` | `routed` | 산출물 작성 |
| `routed` | `plan-ready` | 검토자 호출 |
| `plan-ready` | `awaiting-approval` | 분석 실행 |
| `awaiting-approval` | `approved`, `plan-ready` | 사용자 승인 없는 `running` |
| `approved` | `running` | 계획에 없는 스킬 추가 |
| `running` | `reviewing`, `blocked`, `interrupted` | 산출물 없이 `completed` |
| `reviewing` | `running`, `finalizing`, `blocked` | 조건부 판정으로 완료 |
| `finalizing` | `completed`, `verification-failed` | 원장·해시 누락 통과 |

## 5. 목표 구성 요소

### A. 라우터 · `scripts/router.mjs`

역할은 **후보를 좁히는 것까지**다. 산출물을 만들지 않는다.

입력:

- 사용자 요청
- 작업 폴더 신호 (`brand/`, `inputs/`, 최근 원장)
- `ROUTING.md` 또는 생성된 라우팅 색인

출력 예시:

```json
{
  "request_class": "analysis-and-decision",
  "candidates": [
    { "id": "046", "score": 0.91, "reason": "ROAS 판정과 예산 재배분" },
    { "id": "045", "score": 0.42, "reason": "주간 성과 취합 표현" }
  ],
  "decision": "046",
  "confidence": "high",
  "needs_user_choice": false
}
```

규칙:

- 점수가 낮거나 상위 두 후보 차이가 작으면 한 번 묻는다.
- 복합 요청은 먼저 하위 업무로 분해한 뒤 최대 4개만 제안한다.
- `chains_to`는 후보 근거이지 자동 실행 명령이 아니다.
- 라우터 평가셋은 각 스킬의 `routing-eval.jsonl`을 통합해 만든다.

### B. 계획 컴파일러 · `scripts/plan-compiler.mjs`

AI가 쓴 G2 계획을 기계가 검사할 수 있는 구조로 변환한다.

```json
{
  "schema": "marketing-team.plan/v1",
  "plan_id": "...",
  "request": "...",
  "skills": ["046"],
  "steps": [
    {
      "step": 1,
      "skill": "046",
      "inputs": ["plugin:sample-data/A브랜드-채널성과-90일.csv"],
      "outputs": ["workspace:outputs/.../046-roas-budget-rebalance.md"],
      "reviews": [{ "kind": "business", "perspective": "재무" }]
    }
  ],
  "budget": { "tool_calls": 0, "wall_minutes": 0, "review_rounds": 3 },
  "plan_sha256": "..."
}
```

검사 항목:

- 스킬이 실제 존재하는가
- 순서가 `requires`, `inputs`, `chains_to`와 충돌하지 않는가
- 모든 `writes_to`가 예정 산출물에 있는가
- `review`, `gate`, `pii` 정책이 빠지지 않았는가
- 조합의 앞 출력이 뒤 입력으로 연결되는가
- 승인 화면의 경로와 구조화 계획의 경로가 같은가

### C. 승인 봉인 · 계획 해시

현재의 `진행 승인` 문장은 유지하되 승인 대상을 명확히 한다.

1. 계획 컴파일러가 `plan_sha256`을 만든다.
2. G2 화면은 해당 계획에서 렌더링한다.
3. 사용자가 `진행 승인`이라고 답하면 현재 `plan_sha256`을 승인 상태로 봉인한다.
4. 이후 스킬·순서·출력·검토가 달라지면 새 계획과 새 승인이 필요하다.

훅은 단순히 최근 문장을 찾는 수준에서 다음 단계로 발전한다.

- 현재 계획 ID 존재
- 승인된 계획 해시와 현재 계획 해시 일치
- 호출하려는 도구가 해당 단계에서 허용됨
- 쓰려는 파일이 현재 단계의 outputs 또는 허용된 보고서 경로임

### D. 체인 컴파일러 · `scripts/chain-compiler.mjs`

특정 시나리오의 번호를 코드에 하드코딩하지 않는다.

스킬 frontmatter에서 실행 그래프를 만든다.

```text
requires + inputs + chains_to + writes_to + review + gate + pii
                              ↓
                        실행 DAG
```

검사:

- 순환 체인
- 앞 단계 없이 실행할 수 없는 입력
- 서로 다른 폴더·형식으로 끊긴 전달 경로
- 정본 체인과 사용자 지정 순서의 충돌
- 같은 파일을 두 단계가 덮어쓰는 충돌

사용자가 역방향 순서를 명시하면 자동 교정하지 않는다.
G2에서 충돌과 결과 제한을 설명하고, 승인받은 순서를 그대로 기록한다.

### E. 단계별 실행 영수증 · `run/v2`

기존 `run/v1`을 즉시 폐기하지 않고 `run/v2`를 추가한다.

```json
{
  "schema": "marketing-team.run/v2",
  "plan_id": "...",
  "plan_sha256": "...",
  "status": "running",
  "current_step": 2,
  "steps": [
    { "skill": "061", "status": "completed", "outputs": [], "verified_at": "..." },
    { "skill": "073", "status": "running", "inputs_from": [1] },
    { "skill": "065", "status": "pending" },
    { "skill": "066", "status": "pending" }
  ]
}
```

효과:

- 중단 지점을 정확히 알 수 있다.
- 완료된 단계를 다시 만들지 않고 다음 단계부터 재개한다.
- 앞 단계 산출물이 바뀌면 영향을 받는 뒤 단계만 무효화한다.
- 검토가 필요한 파일과 현재 해시를 단계별로 연결한다.

### F. 실행 예산과 회로 차단기

모든 실행에 예산을 둔다.

| 예산 | 초기 정책 | 초과 시 |
|---|---|---|
| 도구 호출 | 실측 P95를 수집한 뒤 스킬·체인별 상한 확정 | 새 도구 호출 차단 · 현재까지 결과 저장 |
| 벽시계 시간 | 단일/체인별 실측 P95 + 안전 여유 | `blocked: budget-exceeded` |
| 검토 라운드 | 최대 3회 | 사용자 판단 요청 |
| 동일 오류 | 2회 | 다른 방법 1회 또는 중단 |
| 계획 밖 스킬 | 0회 | 즉시 차단 · 새 계획 요구 |

처음부터 임의의 숫자로 전체를 막지 않는다.

1. 006·046·고급 조합의 정상 완주 도구 호출·시간을 기록한다.
2. 스킬 난이도별 P50/P95를 구한다.
3. 소프트 경고를 먼저 적용한다.
4. 두 번의 릴리스 실측 뒤 하드 상한을 켠다.

### G. 검토 정책 컴파일러 · `scripts/review-policy.mjs`

스킬의 선언을 실행 전 필수 검토표로 변환한다.

| 선언 | 자동 정책 |
|---|---|
| `review: 재무` | 모든 의사결정 산출물에 재무 검토 1건 |
| `gate: true` | 모든 대외 발행 산출물에 규제 검토 1건 |
| `pii: true` | 기계 PII 검사 + 규제검토자 개인정보 관점 |
| 여러 산출물 | 산출물별 검토 대상과 해시 명시 |

검토 결과가 `conditional`, `needs-fix`면 자동 완료하지 않는다.

```text
검토 → 수정 → 같은 현재 파일 재검토 → 최종 승인 봉인
```

### H. 이벤트 로그와 운영 지표

원장은 사람이 읽는 결과 기록으로 유지한다. 별도로 기계 이벤트를 남긴다.

```text
logs/orchestrator-events.jsonl
```

최소 이벤트:

- `route.selected`, `route.ambiguous`
- `plan.created`, `plan.approved`, `plan.changed`
- `step.started`, `step.completed`, `step.failed`
- `review.requested`, `review.conditional`, `review.approved`
- `guard.denied`, `budget.warning`, `budget.exceeded`
- `run.completed`, `run.blocked`, `run.resumed`

운영 지표:

- 자연어 라우팅 정확도
- 계획 승인 전 차단 건수
- 실행당 도구 호출·시간
- 검토 평균 라운드
- 완료율·중단 후 재개율
- `writes_to`·원장·PII 실패율

## 6. 구현 로드맵

### Phase 0 · 기준선 고정 — P0

목표: 개선 전 성능을 측정해 이후 변화가 좋아졌는지 비교한다.

- [x] 수정본으로 006·046·고급 조합 완주 — **설치본 새 대화 3종** (2026-08-30 · `실제확인-기록/9093422.md`)
- [x] 실행별 시간·검토 라운드 기록 — 기준선(도구 호출은 수동 계측이라 미기록):

  | 시나리오 | run_id | 벽시계(분) | 검토 라운드 |
  |---|---|---:|---:|
  | 초급 006 | `…-006-fef911` | 11.15 ⚠️(권한벽·도구수리 왕복 포함) | 2 |
  | 중급 046 | `…-046-c5dee4` | 2.16 | 1 |
  | 고급 4스킬 | `…-061-3791e9` | 17.57 | 3 |

- [x] 자연어 라우팅 평가셋 통합 · `eval-routing.mjs`가 100개 파일 596건을 전수 검사
- [x] 현재 run/v1 샘플 3종을 회귀 픽스처로 보존 · `scripts/_픽스처/run-v1/` + `test-first-run-ux.mjs` 파스 검사

완료 조건:

- 세 시나리오의 정상 기준값과 실패 재현값이 문서에 존재한다.

### Phase 1 · 계획과 상태의 기계화 — P0

- [x] `marketing-team.plan/v1` 스키마 정의
- [x] `plan-compiler.mjs` 구현
- [x] 계획 해시와 승인 봉인 구현
- [x] 상태 전이 표를 `run-receipt.mjs`에 적용
- [x] 훅을 승인 문장 검사에서 계획 해시 검사로 전환

완료 조건:

- 승인받은 계획과 다른 스킬·순서·산출물은 실행 전에 차단된다.
- 새 계획이 생기면 이전 승인을 재사용할 수 없다.

### Phase 2 · 라우팅과 체인 컴파일 — P0

### 라우팅 실측 (2026-08-30) · **Phase 2 우선순위 하향 근거**

`eval-routing.mjs --live-cc` 로 596건 전수를 실제 모델에 물었다.

| 층 | 값 |
|---|---|
| A층 어휘 프록시 | top1 316/596 (53.0%) — 도구 스스로 「절대 점수는 의미가 없다」고 적어 둔 값 |
| **B층 실제 모델 라우팅 · 596건 전수** | **정확 580/596 (97.3%)** |
| 헷갈릴 만한 것 | 16건 (2.7%) · 평가셋이 `ambiguous_with` 에 스스로 적어 둔 쌍 |
| **틀림** | **0건** |

**목표 95% 를 이미 넘었다.** 53% 라는 숫자가 위기처럼 보였지만 사용자가 겪는 값이 아니다.

부수 확인 · `docs/헷갈리는-쌍.md` 는 애매 쌍 183종 중 **13% 만 덮는데도** 정확도가 97.3% 다.
그 문서를 늘리는 것은 병목이 아니다. **안 해도 되는 일**로 분류한다.

⚠️ **다만 이 평가가 재지 않는 것이 있다.** 프롬프트가 「정확히 하나 고른다」를 강제해
**복합 요청 분해·체인 순서·애매할 때 묻기**를 재지 못한다.
실측 실패(순서 변경·계획 밖 스킬 추가·승인 전 파일 생성)는 전부 그 바깥에서 났다.
→ Phase 2 의 무게를 라우터에서 **체인·계획 쪽으로** 옮긴다.

- [x] `router.mjs` 구현 · 실행 없이 상위 후보·신뢰도·질문 필요 여부 반환
- [x] `routing-eval.jsonl` 통합 테스트 러너 구현 · `eval-routing.mjs`
- [x] `chain-compiler.mjs` 구현 · 카테고리 10 + 교차 5 정본과 선택 분기 컴파일
- [x] 하드코딩된 고급 4스킬 순서 검사를 일반 그래프 검사로 교체
- [x] 역방향 사용자 체인의 위험 고지·승인 흐름 구현 · 자동 교정 없이 `plan.risks`에 봉인

완료 조건:

- 006·046 자연어 라우팅 정확도 100%
- 전체 평가셋 Top-1 목표 95% 이상, Top-2 99% 이상
- 중간 단계 누락·순환·입력 단절 테스트 전부 차단

### Phase 3 · 단계별 실행과 복구 — P0

- [x] 단계 구조 추가 — `run/v1` 에 `steps` 배열을 실었다. **스키마를 가르지 않았다** ·
      `plan.json` 이 없으면 `steps` 도 없고 예전 경로 그대로다 (파괴적 전환 회피)
- [x] 단계별 `pending / running / completed` 기록 · `step-start` · `step-done`
- [x] 앞 출력 → 뒤 입력 해시 연결 · 단계마다 `consumed` 에 **그 단계가 실제로 먹은** 앞 산출물 지문
- [x] 중단 후 재개 명령 추가 · `resume`
- [x] 영향받은 뒤 단계만 무효화하는 재실행 구현

> `blocked` · `reviewing` 단계 상태와 `run/v2` 스키마 분리는 넣지 않았다.
> 지금 필요한 것은 **어디서 멈췄고 무엇을 다시 만들어야 하는가** 하나였고,
> 그것은 `pending/running/completed` 로 닫힌다. 이름만 늘리면 상태가 늘어난 척만 한다.

완료 조건: **둘 다 실측으로 확인** (2026-08-30)

```
① 1·2단계 완료 뒤 중단 → resume
   ▶ 재개 지점 · 단계 3 (스킬 065) · 이미 끝난 단계 1 · 2 는 다시 만들지 않습니다
   🔴 finalize --status completed → 아직 안 끝난 단계가 있습니다: 3(pending) · 4(pending)

② 4단계까지 완료 뒤 2단계 산출물(073)만 수정 → resume
   🟡 앞 산출물이 바뀌어 되돌린 단계: 4
   단계 1 completed · 2 completed · 3 completed · 4 pending
   → 073 을 먹지 않는 3단계는 살아남았다. 뭉뚱그려 뒤를 다 지우지 않는다.
```

회귀 6건을 `test-run-receipt.mjs` 에 넣었다 (앞 단계 건너뛰기 차단 · 미완 완료 차단 ·
재개 지점 · 전부 완료 · 선택적 무효화 · 무관 단계 보존).

### Phase 4 · 검토·예산·회로 차단 — P1

- [x] `review-policy.mjs` 구현
- [x] 산출물별 검토 요구 자동 생성 · 수동 입력은 병합하고 중복 제거
- [ ] 도구 호출·시간·검토 라운드 계측 · **부분 구현:** 시간·검토 자동, 도구 호출은 `metric` 수동 입력
- [x] 소프트 예산 경고 적용 · 시간·검토 자동, 도구 호출은 `metric`으로 계측할 때 경고
- [ ] 반복 오류·계획 밖 도구 회로 차단 · 계획 밖 스킬·쓰기 경로는 훅이 차단, 반복 오류 횟수 차단은 남음

완료 조건:

- 조건부 검토 상태에서 완료 불가
- 단일 산출물만 검토한 다중 실행 완료 불가
- 예산 초과 시 현재 파일·상태·재개 방법이 남는다.

### Phase 5 · 관측과 릴리스 관문 — P1

- [x] `orchestrator-events.jsonl` 추가 · 실행·단계·검토·재개·완료 이벤트
- [x] 실행 요약 명령 구현 · `orchestrator-events.mjs summary`
- [x] 실패 유형별 회귀 테스트 추가 — 훅 우회(결합 옵션·따옴표)·계획 벽돌·finalize 오승격 등
      실기가 잡은 결함 전부를 회귀로 박제 (가드 45검사 · 2026-08-31 · `실제확인-기록/9093422.md` 참조)
- [ ] `claude --plugin-dir` 개발 실기 자동 보조 — 수동 절차는 확립(헤드리스 멀티턴 · 2026-08-30~31 실기)
- [x] GitHub 태그 설치본 실기 체크리스트 연결 — `scripts/실제확인.md` → `실제확인-기록/{커밋}.md` 사이클 가동

완료 조건:

- 한 실행의 라우팅→승인→단계→검토→완료 이력을 한 명령으로 확인한다.
- 초급·중급·고급 시나리오가 서로 다른 새 대화에서 모두 통과한다.

### Phase 6 · 운영 최적화 — P2

- [ ] 실측 P95 기반 하드 실행 예산 적용
- [ ] 자주 쓰는 체인의 컴파일 결과 캐시
- [ ] 반복 라우팅 실패를 `my-playbook` 개선 후보로 집계
- [ ] 스킬 100번 산출물과 실제 런타임 정책의 불일치 검사

완료 조건:

- 품질 저하 없이 중급·고급 실행 시간과 도구 호출을 기준선 대비 30% 이상 줄인다.

## 7. 파일별 예상 변경

| 파일 | 변경 |
|---|---|
| `scripts/router.mjs` | 신규 · 자연어 후보와 신뢰도 반환 |
| `scripts/plan-compiler.mjs` | 신규 · G2 계획 구조화·검증·해시 |
| `scripts/chain-compiler.mjs` | 신규 · frontmatter 기반 실행 DAG |
| `scripts/review-policy.mjs` | 신규 · 검토 요구 자동 생성 |
| `scripts/run-receipt.mjs` | run/v1 하위 호환 단계 상태·영수증·재개·사용량·이벤트 |
| `scripts/runtime-guard.mjs` | 승인 계획 해시·현재 단계·도구·쓰기 경로 검사 |
| `scripts/verify.mjs` | 새 컴파일러·상태·복구·이벤트 회귀 검사 |
| `skills/AI-마케터/SKILL.md` | 기계 명령 호출 순서만 남기고 장황한 중복 규칙 축소 |
| `100-skills/ROUTING.md` | 사람이 읽는 명부 유지 · 기계 색인은 생성물로 분리 |
| `100-skills/CHAINS.md` | 체인 설명 정본 유지 · 실행 그래프 생성의 입력 |
| `docs/실행-영수증.md` | plan/v1·run/v2·상태 전이·재개 규약 |
| `docs/클로드코드-실기검증.md` | 상태 전이·예산 초과·재개 시나리오 추가 |

## 8. 테스트 전략

### 단위 테스트

- 라우팅 후보·동률·미등록 요청
- 계획 해시 변경과 승인 무효화
- 체인 순환·역방향·입력 단절
- 허용/금지 상태 전이
- 계획 밖 파일·도구·검토자
- PII·검토·원장 누락
- 예산 경고·초과·재개

### 계약 테스트

- 100개 스킬의 `writes_to`, `review`, `gate`, `pii`, `chains_to` 컴파일
- 스킬 변경 시 라우팅 색인과 체인 그래프 재생성 여부
- 기존 run/v1 검증 호환

### 블랙박스 실기

| 시나리오 | 확인 |
|---|---|
| 초급 006 | 자연어 라우팅·샘플·TOP 5·PII·완료 |
| 중급 046 | 승인 정지·재무 검토·3안·계획 밖 HTML 0 |
| 고급 4스킬 | 순서·중간 산출물·경영 검토·PII·재개 |
| 승인 변경 | 계획 수정 뒤 옛 승인 재사용 차단 |
| 중단 복구 | 2단계 중단 뒤 3단계부터 재개 |
| 예산 초과 | 완료 오표기 없이 blocked·재개 안내 |
| 설치본 | GitHub 태그 캐시만 사용 |

## 9. 하지 않을 것

- 실행 담당을 100개 에이전트로 분리하지 않는다.
- 모든 스킬을 세션 시작 시 전부 로드하지 않는다.
- 추론 수준을 전 실행에서 일괄 상향하지 않는다.
- 첫 버전부터 자동 발행·자동 예산 변경을 연결하지 않는다.
- 실측 없이 임의의 시간·도구 상한을 하드코딩하지 않는다.
- 기존 run/v1 영수증을 읽지 못하게 만드는 파괴적 전환을 하지 않는다.
- `ROUTING.md`와 `CHAINS.md`를 사람이 수정하는 코드 파일로 바꾸지 않는다.

## 10. 우선순위와 예상 순서

```text
P0  기준선 실기
 → 계획 스키마·승인 해시
 → 상태 머신
 → 라우터·체인 컴파일러
 → run/v2 단계 실행·재개

P1  검토 정책
 → 실행 예산·회로 차단
 → 이벤트 로그·릴리스 관문

P2  실측 기반 최적화·캐시·운영 학습
```

가장 먼저 구현할 것은 라우터가 아니다. **계획 스키마와 상태 전이**다.
라우팅을 잘해도 승인받지 않은 계획이 실행되거나 중간 상태를 잃으면 안정성이 생기지 않는다.

## 11. 1차 구현 착수 범위

첫 구현은 아래 네 항목으로 제한한다.

1. `marketing-team.plan/v1` 스키마
2. `plan-compiler.mjs`
3. 계획 SHA-256 승인 봉인
4. `run-receipt.mjs` 상태 전이 검사

1차 인수 기준:

- [x] 같은 계획은 같은 정규화 해시를 만든다. · `planHash` · 입력 순서처럼 의미가 아닌 것은 정렬해 흡수한다
- [x] 스킬·순서·입력·산출물·검토 중 하나가 바뀌면 해시가 달라진다. · 회귀 4종
- [x] 사용자가 승인한 해시와 다르면 Bash·Write·Agent·Skill이 차단된다.
      `run-receipt start` 와 `runtime-guard` 양쪽에서 막는다. 훅은 문장 검사를 **대체하지 않고
      덧붙였다** — `plan.json` 이 있을 때만 해시를 보고, 없으면 예전 경로 그대로다
- [x] `awaiting-approval`에서 `running`으로 직접 갈 수 없다. · `validateApprovedPlan`
- [x] 기존 `completed/current` run/v1 검증은 계속 통과한다. · `plan.json` 이 없으면 예전 경로
- [x] 전체 `verify.mjs` 오류 0을 유지한다.

### 1차 구현 실측 (2026-08-30)

| 항목 | 결과 |
|---|---|
| 신규 | `scripts/plan-compiler.mjs` · `scripts/test-plan-compiler.mjs` |
| 명령 | `compile` (계약 검사 + 해시 + 승인 대기) · `approve` (봉인) · `check` (유효성) |
| 상태 | `plan-ready → awaiting-approval → approved` |
| 계약 검사 | 없는 스킬 · `skills`↔`steps` 순서 불일치 · `writes_to` 누락 · **계약 밖 산출물** · `review`/`gate` 누락 · 앞 단계가 안 만든 작업 폴더 입력 · 같은 파일 이중 쓰기 |
| `run-receipt start` | 같은 폴더에 `plan.json` 이 있으면 **승인 해시·스킬 순서·산출물·검토를 대조**하고 다르면 실행을 열지 않는다 |
| 영수증 | `run.plan.plan_sha256` 으로 어느 계획을 승인받고 돌았는지 남는다 |
| 회귀 | 해시 재현·민감도 7 · 계약 검사 4 · 지정 순서 2 · 승인 상태 5 · start 차단·통과 2 · 훅 계획 해시 5 |
| 훅 | `runtime-guard` 가 `plan.json` 을 함께 본다 · 승인 대기 ⛔ · 봉인 ✅ · 승인 뒤 변경 ⛔ · 재승인 ✅ · 계획 없음 ✅(하위 호환) |
| 지정 순서 | `requested_order` 를 계획 스키마와 해시에 넣었다. 사용자가 말한 순서를 모델이 바꾸면 컴파일이 막는다 (하드코딩 4스킬 검사의 일반화) |

**함께 앞당겨 끝난 것 (Phase 2~5)**

`review:`·`gate:` 선언은 `start`가 산출물별 `required_reviews`로 자동 생성한다.
그 전에는 검토 의무 60개 중 `pii` 8개만 강제되고 **52개가 「AI 마케터가 기억하기」**에 달려 있었다.
`scripts/output-checks.mjs` (`pii`·`csv-format`·`house-style`) 와
`scripts/check-flag-counts.mjs` 도 `verify.mjs` 에 물렸다. 추가로 자연어 후보 라우터,
15개 정본 체인 컴파일러, 단계 재개, 소프트 예산 사용량, JSONL 이벤트 요약을 회귀 검사에 연결했다.

## 12. 실제 마케터 실효성 평가

### 평가 결론

현재 플러그인의 **설계 실효성은 높고, 현장 실효성은 아직 검증 중**이다.

```text
절차·업무 범위·안전장치        강함
중급 마케터의 반복 업무 지원    실효성 높음
초보자의 설치 즉시 사용         보완 필요
복합 체인의 안정적 완주          보완 필요
실제 매출·ROAS 개선 효과         아직 증거 없음
```

현재 소스와 제한된 Claude Code 실기를 기준으로 한 **잠정 제품 실효성 점수는 83/100**이다.
이 숫자는 사용자 만족도나 매출 효과가 아니다. 일반 마케터가 업무를 시작해 재사용 가능한 결과까지
도달할 가능성을 평가한 설계 점수다.

### 평가 기준과 점수

| 평가 영역 | 배점 | 현재 | 근거 |
|---|---:|---:|---|
| 업무 범위 | 20 | **18** | 10개 카테고리 · 100개 스킬 · 15개 체인 · 474개 부르는 말 |
| 자연어 진입과 초보자 사용성 | 20 | **16** | 실제 모델 596건 97.3% · 로컬 후보 라우터·복합 분해 추가 · 설치·작업 폴더 개념은 여전히 필요 |
| 산출물의 실행 가능성 | 20 | **17** | 100개 전부 `writes_to` 보유 · 실제 파일 계약 · 원장·검토·자동 내용 검사 연결 |
| 실행 안정성과 복구 | 15 | **14** | 승인 해시·일반 체인 그래프·단계별 지문·선택적 재개·소프트 예산·이벤트 기록 |
| 데이터 연결과 현업 적용 | 15 | **8** | 52개 샘플 폴백·플랫폼별 내보내기 안내 · 실제 계정 연결과 자동 수집은 없음 |
| 안전·검토 체계 | 10 | **10** | 대외 발행 게이트 32개 · 사업 검토 27개 · PII 8개 · 산출물별 검토 자동 생성·내용 검사 |
| **합계** | **100** | **83** | 개발 소스와 제한된 실기 기준 · 현장 효과 점수는 아님 |

### 실효성이 높은 사용 영역

| 사용자·업무 | 실효성 | 이유 |
|---|---|---|
| **중급 1인 마케터** | **높음** | 분석·기획·검토 절차는 알지만 매번 처음부터 만들기 번거로운 사용자에게 적합 |
| **로컬 CSV 기반 분석** | **높음** | 광고·매출·리뷰 파일을 `inputs/`에 넣으면 정해진 산출물과 판정 기준으로 반복 가능 |
| **내부 보고·기획 문서** | **높음** | 파일 착지·근거·다음 행동·검토 기록이 함께 남음 |
| **콘텐츠·광고 초안 제작** | **중상** | 브랜드 프로필·톤·규제 게이트가 있어 일반 Claude보다 일관성이 높음 |
| **샘플 학습·시연** | **중상** | 52개 스킬은 실제 데이터가 없어도 완주 가능 |

### 실효성이 아직 낮은 사용 영역

| 사용자·업무 | 현재 한계 |
|---|---|
| **완전 초보 마케터** | 작업 폴더·CSV·프로필·`진행 승인` 등 제품 고유 개념을 익혀야 함 |
| **고급 복합 분석** | 체인·재개는 기계 검증되지만 실제 새 대화 완주율과 소요 시간 표본이 아직 부족함 |
| **실시간 광고 운영** | 광고 계정과 직접 연결되지 않으며 예산 변경은 제안까지만 가능 |
| **CRM·발송 자동화** | 외부 도구 연결·예약·발송·스케줄링은 패키지 기본 범위가 아님 |
| **팀 단위 협업** | 담당자 배정·동시 실행·결재 알림·권한 관리가 없음 |
| **장기 성과 최적화** | 플러그인 사용 전후 매출·ROAS·시간 절감 비교 데이터가 없음 |

### 일반 마케터가 체감할 강점

1. **무엇을 해야 하는지 모를 때 출발점을 준다.** 스킬 번호 대신 자연어와 동사형 선택지를 쓴다.
2. **결과가 파일로 남는다.** 대화가 끝나도 다음 주에 다시 열고 이어갈 수 있다.
3. **숫자에 판정을 붙인다.** ROAS·마진·기간·표본을 근거 없이 “좋다”라고 말하지 않는다.
4. **회사 데이터가 없어도 볼 수 있다.** 샘플로 먼저 결과를 보여 주고 실제 데이터 전환 경로를 안내한다.
5. **발행·예산·개인정보를 조심한다.** 일반 생성형 AI보다 업무 사고를 막는 구조가 강하다.

### 일반 마케터가 체감할 마찰

1. **첫 설정이 아직 폴더 중심이다.** `brand/`, `inputs/`, `outputs/`, `logs/` 개념을 알아야 한다.
2. **데이터를 직접 내려받아야 한다.** 데이터 사용 스킬은 대부분 플랫폼 내보내기에 의존한다.
3. **승인 단계가 반복처럼 느껴질 수 있다.** G1·G2를 왜 나누는지 화면 문장이 충분히 짧아야 한다.
4. **복합 실행은 오래 걸릴 수 있다.** 단계·재개 지점은 남지만 화면의 예상 남은 시간은 아직 없다.
5. **문서 숫자가 표류할 수 있다.** `데이터-가져오기.md`의 샘플 폴백 수는 48로,
   현재 생성 통계 52와 다르다. 사용자에게 보이는 숫자도 자동 검사 대상으로 넓혀야 한다.

### 실제 효과를 입증하는 사용자 검증

실제 마케터에게 유용하다고 확정하려면 기능 테스트가 아니라 **비교 사용성 테스트**가 필요하다.

권장 표본:

- 초급 5명
- 중급 5명
- 고급 5명
- 총 15명 · 각 5개 업무

비교 방식:

```text
같은 마케팅 요청
  A  일반 Claude
  B  marketing-team 플러그인

비교: 정확도 · 완료 시간 · 수정 횟수 · 파일 재사용성 · 의사결정 도움 · 신뢰도
```

핵심 측정 지표:

| 지표 | 출시 목표 |
|---|---:|
| 첫 업무 시작 성공률 | 90% 이상 |
| 자연어 라우팅 정확도 | 95% 이상 |
| 단일 스킬 완주율 | 90% 이상 |
| 3~4개 체인 완주율 | 80% 이상 |
| 첫 결과까지 걸린 시간 | 단일 5분 이내 · 체인 12분 이내 |
| 사용자 추가 설명 | 중앙값 1회 이하 |
| 결과물 “바로 쓸 수 있음” | 5점 중 4점 이상 |
| 다음 날 파일 재사용 성공 | 90% 이상 |
| 중대한 개인정보·발행 사고 | 0건 |
| 일반 Claude 대비 업무 시간 | 30% 이상 단축 |

### 최종 판단

이 플러그인의 가장 큰 가치는 **마케팅 지식을 많이 말하는 것**이 아니다.
일반 Claude가 자주 빠뜨리는 데이터 확인·판정 기준·파일 저장·검토·다음 행동을 한 흐름으로 묶는 데 있다.

따라서 현재도 중급 마케터에게는 실용적이다. 하지만 대중적인 플러그인으로 판단하려면 다음 셋이 먼저다.

1. 자연어 라우팅 95% 이상
2. 중단 복구를 포함한 단일 실행 완주율 90% 이상
3. 실제 마케터 15명 비교 테스트에서 시간 단축과 결과물 사용성 입증

이 셋과 설치본 새 대화 3종을 통과하면 잠정 83점에서 **현장 검증 85점 이상**으로 릴리스 판정을 바꿀 수 있다.

## 13. 사용성 개선 · 첫 문장 즉시 시작

### 13.1 목표와 적용 범위

`headcount`에서 가져올 것은 부서 구조가 아니라 **「설치 뒤 바로 평소 말로 시작한다」는 경험**이다.
사용자는 `brand/`·`inputs/`·스킬 번호·샘플 모드를 먼저 배우지 않는다. 첫 화면에서는 하고 싶은
마케팅 업무만 말하고, AI 마케터가 내부에서 라우팅·자료 확인·계획 컴파일을 처리한다.

```text
지금: 요청 → 제품 구조 설명 → 자료·폴더 안내 → 계획 → 승인 → 실행
목표: 요청 → 필요한 것과 계획을 한 화면에 표시 → 승인 → 실행
```

안전 규칙은 줄이지 않는다. 계획 해시·사용자 승인·규제검토·사업검토·PII·파일 지문·실행 영수증은
그대로 유지하고, **사용자에게 보이는 조작 단계만 줄인다.**

적용 범위는 저위험 단일 업무부터 시작한다. 예산 확정, 외부 게시·발송, 개인정보 사용,
계약·법률 표현은 기존 분리 게이트를 유지한다.

### 13.2 변경 전후 요약

| 구분 | 현재 | 개선 후 | 기대 효과 |
|---|---|---|---|
| 첫 요청 | 구조와 준비 절차부터 안내 | 요청한 업무와 필요한 것부터 안내 | 초급 사용자도 바로 시작 |
| 업무 선택 | 스킬 ID가 노출될 수 있음 | 자연어 요청을 내부에서 자동 판정 | 스킬 탐색 부담 제거 |
| 자료 요청 | 내부 폴더와 파일 계약 안내 | 첨부할 자료 또는 샘플 선택 안내 | 기술 용어 감소 |
| 자료가 없을 때 | 샘플 여부를 별도 질문 | 실제 자료·샘플 경로를 한 화면에 제시 | 대화 왕복 감소 |
| 실행 계획 | 자료 확인 뒤 별도 제시 | 필요한 것·계획·승인을 한 화면에 제시 | 실행 흐름 예측 가능 |
| 저위험 업무 | 승인 전 사용자 응답 3회 이상 가능 | 승인 포함 사용자 응답 1회 목표 | 첫 결과 도달 시간 단축 |
| 고위험 업무 | 개인정보·외부 실행 게이트 적용 | 게이트는 유지하고 안내만 쉬운 말로 변경 | 안전성과 사용성 동시 유지 |
| 오류 안내 | 내부 원인과 경로 중심 | 완료 지점·필요 자료·대안 중심 | 사용자가 스스로 복구 |
| 완료 안내 | 산출물과 경로 중심 | 결과·판단·다음 행동·파일 순서 | 결과 활용성 향상 |

### 13.3 사용자 경험 원칙

1. **업무부터 받는다.** 설치·회사 정보 등록을 선행 조건으로 만들지 않는다.
2. **내부 구조를 먼저 설명하지 않는다.** 스킬 ID와 폴더 이름은 오류 복구나 사용자가 물을 때만 보인다.
3. **질문은 한 번에 하나만 한다.** 결과를 바꾸는 최소 재료만 묻고, 있으면 되묻지 않는다.
4. **자료가 없어도 결과를 보여 준다.** 실제 자료를 받는 방법과 샘플 진행을 동시에 제안한다.
5. **승인은 한 번만 받는다.** 저위험 단일 업무는 G1 되짚기와 G2 계획을 첫 응답 한 화면에 합친다.
6. **완료 뒤에 구조를 보여 준다.** 결과물 이름·무엇을 알게 됐는지·다음 행동을 먼저 말하고 경로는 뒤에 둔다.

### 13.4 목표 첫 화면

사용자:

```text
인스타 광고 문구 만들어줘
```

AI 마케터:

```text
인스타 광고 문구 3안을 만들겠습니다.
제품과 핵심 혜택을 한 줄로 알려주세요. 없으면 연습용 브랜드로 먼저 만들 수 있습니다.

[실행 계획]
광고 문구 3안 → 표현 안전 점검 → 결과 저장

[승인 요청]
⏸ 이대로 갈까요?
그대로: 「진행 승인」 — 연습용 브랜드로 돕니다 (결과 첫 줄에 [샘플])
우리 것으로: 「진행 승인. 제품은 저자극 선크림이고 핵심 혜택은 자외선 차단」
```

첫 화면에서 숨기는 것:

- `043` 같은 스킬 ID
- `brand/`, `inputs/`, `outputs/`, `logs/`
- `plan_sha256`, `run_id`, 상태 머신 이름
- 100개 스킬·담당자 구조 설명
- 사용자가 선택하지 않은 설치·구축 안내

### 13.5 진입 경로와 안전 분기

| 상태 | 첫 응답 | 사용자에게 필요한 답 |
|---|---|---|
| 요청과 재료가 충분함 | 되짚기 + 계획 + 승인 요청 | `진행 승인` 1회 |
| 핵심 재료 1개가 없음 | 한 줄 재료 요청 + 샘플 선택 + 계획 | 재료 또는 샘플 + 승인 1회 |
| 업무가 둘 이상으로 갈림 | 결과가 다른 선택지 최대 2개 | 선택 1회 후 계획 승인 |

고위험·고비용·되돌릴 수 없는 업무는 합치지 않는다. 예산 확정, 외부 게시·발송, 개인정보 사용,
계약·법률 표현은 기존 G1→G2 정지를 유지한다. 단순 카피·요약·분석 초안에만 빠른 진입을 적용한다.

시나리오별 목표:

| 사용자 요청 | 현재 예상 응답 | 개선 목표 | 유지할 안전장치 |
|---|---:|---:|---|
| `인스타 광고 문구 만들어줘` | 3회 이상 | 1회 | 계획 승인·표현 안전 점검·저장 기록 |
| `고객 리뷰 분석해줘` | 3회 이상 | 1회 | 샘플 표시·개인정보 마스킹·원문 식별자 차단 |
| `고객 명단으로 할인 문자 보내줘` | 2회 이상 | 2회 이상 유지 | 수신 동의·제외 대상·별도 승인·사람의 최종 발송 |

### 13.6 단계별 구현

#### P0 · 첫 화면 계약과 기준선 — 완료 (대표 요청 10개 상시 회귀화만 잔여)

- [ ] 초급·중급·고급 대표 요청 10개를 선정한다.
- [x] 첫 응답 실측 (2026-08-31 · 「인스타 광고 문구 만들어줘」 · v0.49.0): 화면 1개 · 질문 1개(최소 재료) ·
      승인 전 턴 0 · 내부 용어 노출 0 (착지 경로 1줄은 계획 정본으로 허용)
- [x] `quick_start` 판정 조건 문서화 — `AI-마케터/SKILL.md` §⚡ (넷 중 하나면 불가)
- [x] 고위험 제외 확정 — mutating(발송·게시) · pii · 예산 확정 · 계약·법률 (gate 는 제외 아님 —
      검사는 계획 안에 남는다)

완료 기준:

- 같은 요청이 실행마다 빠른 진입과 일반 진입 사이에서 흔들리지 않는다.
- 안전 업무를 빠르게 여는 조건과 위험 업무를 멈추는 조건이 테스트로 재현된다.

#### P1 · 단일 업무 빠른 진입 — 완료 · v0.49.0 (`AI-마케터/SKILL.md` §⚡ · 훅 접두 승인)

- `AI-마케터/SKILL.md`에 G1+G2 한 화면 규칙을 추가한다.
- 요청에 충분한 재료가 있으면 추가 질문 없이 계획을 낸다.
- 재료가 없으면 `inputs:` 전체를 나열하지 않고 결과를 바꾸는 최소 재료 한 개만 묻는다.
- 샘플 진행 문장과 실제 자료 진행 문장을 같은 화면에 둔다.
- 사용자가 `진행 승인`으로 시작해 재료를 같은 답에 적어도 현재 계획의 승인으로 인정한다.

예:

```text
진행 승인. 우리 제품은 저자극 선크림이고 민감성 피부가 대상이야
```

완료 기준:

- 재료가 충분한 저위험 단일 요청은 사용자 응답 1회 뒤 실행된다.
- 회사 정보가 없어도 구축 안내에 막히지 않고 `[샘플]` 결과까지 완주한다.
- 기존 승인 해시와 런타임 가드 검사는 그대로 통과한다.

**실측 (2026-08-31 · 설치본 새 대화):** 「인스타 광고 문구 만들어줘」 → 첫 화면 1개(되짚기+계획+승인 요청)
→ 「진행 승인. 샘플로」 → 완주. 사용자 입력 2줄 · 내부 용어 0 · 게이트/영수증/원장 전부 남음.
빠른 진입 판정 회귀는 `test-first-run-ux.mjs`.

#### P2 · 단계적 공개와 쉬운 완료 화면 — 부분 구현 (12줄 상한·첨부 우선 문구는 미검증)

- 파일 업로드가 필요하면 `inputs/에 넣으세요`보다 `여기에 파일을 첨부하세요`를 먼저 쓴다.
- 플랫폼 내보내기 안내는 파일이 실제로 필요할 때만 연다.
- 결과 화면은 `결과 → 핵심 판단 → 부족한 것 → 다음 행동 → 파일 위치` 순서로 고정한다.
- 기술 경로는 클릭 가능한 결과 링크 한 줄로 줄이고 내부 해시·ID는 요청받을 때만 보여 준다.
- 실패 시 내부 오류 대신 `어디까지 됐나 / 무엇이 필요한가 / 지금 대신 할 수 있는 것`을 낸다.

완료 기준:

- 첫 응답에 내부 폴더명·스킬 ID가 0개다.
- 정상 완료 화면은 사용자가 읽어야 할 본문이 12줄 이내다.
- 저장·권한 실패에서도 대화창 결과를 잃지 않고 같은 내용을 다시 저장할 수 있다.

#### P3 · 사용자 검증과 점진 적용 — 구현 전

- 초급 5명·중급 5명에게 기존 흐름과 빠른 진입을 교차 사용시킨다.
- 먼저 저위험 단일 스킬 10개에 적용하고, 지표 통과 뒤 24개 생성 스킬로 넓힌다.
- 복합 체인과 고위험 업무는 단일 흐름의 성공이 확인되기 전까지 기존 절차를 유지한다.

### 13.7 구현 대상

| 파일 | 변경 목적 |
|---|---|
| `skills/AI-마케터/SKILL.md` | 빠른 진입 판정·첫 화면·한 번 질문 규칙 |
| `docs/공통규약.md` | 단계적 공개·샘플 진행·완료 화면 공통 계약 |
| `docs/쉬운말.md` | 사용자 화면 금지 내부 용어와 대체 표현 |
| `scripts/runtime-guard.mjs` | ✅ 완료 (v0.48.4 A안) — 접두 승인+재료 인정 · 보류/취소 거부 · 회귀 포함 |
| `scripts/test-first-run-ux.mjs` | ✅ 신설 (2026-08-31) — 계약 앵커 10 · 승인 접두 재료 2 · 픽스처 3 |
| `scripts/verify.mjs` | 빠른 진입 계약과 테스트 배선 확인 |

### 13.8 회귀 시나리오

| 요청 | 기대 결과 |
|---|---|
| `인스타 광고 문구 만들어줘` | 최소 재료 한 줄 또는 샘플 선택 + 계획을 첫 화면에 함께 표시 |
| `이 리뷰 200개 분석해줘` + 파일 | 추가 질문 없이 006 계획·승인 요청 |
| `광고 예산 다시 짜줘` | 기간·지출 자료 확인, 없으면 내보내기와 샘플 경로 동시 제시 |
| `상세페이지 발행해줘` | 규제·외부 실행 위험 때문에 빠른 진입 제외 |
| `고객 명단으로 문자 보내줘` | PII·외부 발송 때문에 기존 분리 게이트 유지 |
| 이미 프로필·입력 존재 | 같은 정보를 다시 묻지 않음 |
| 모호한 요청 `홍보 좀 해줘` | 결과가 다른 선택지 2개 이하만 질문 |

### 13.9 측정 지표와 출시 기준

| 지표 | 현재 기준선 | 목표 |
|---|---:|---:|
| 첫 응답 내부 용어 노출 | 새로 측정 | **0개** |
| 실행 전 사용자 응답 수·저위험 단일 | 새로 측정 | **1회 이하** |
| 첫 업무 시작 성공률 | 실사용 표본 부족 | **90% 이상** |
| 첫 결과까지 시간 | 실사용 표본 부족 | **5분 이내 중앙값** |
| 추가 설명 요청 | 실사용 표본 부족 | **중앙값 1회 이하** |
| 승인 전 실행·쓰기 사고 | 현재 0건 | **0건 유지** |
| 결과물 바로 사용 가능 | 새로 측정 | **5점 중 4점 이상** |

### 13.10 하지 않을 것

- 100개 업무 스킬을 모두 직접 등록해 자동 발화 충돌을 늘리지 않는다.
- 안전 게이트를 없애거나 `진행 승인` 없이 실행하지 않는다.
- 모든 요청을 샘플로 조용히 처리하지 않는다. 실제 자료인지 샘플인지 화면에 명확히 표시한다.
- 빠른 진입을 이유로 규제검토·사업검토·PII·영수증을 생략하지 않는다.
- 첫 화면을 선택지·설명·조직도로 채우지 않는다.

### 13.11 실행 순서와 최종 인수 조건

```text
P0 기준선·안전 분기 고정
 → P1 저위험 10개 빠른 진입
 → P2 완료·실패 화면 개선
 → 자동 회귀 통과
 → P3 초급·중급 사용자 검증
 → 지표 통과 시 생성 스킬 24개로 확대
```

아래 조건을 모두 충족하기 전에는 전체 스킬에 빠른 진입을 적용하지 않는다.

- [ ] 저위험 단일 요청 10개가 첫 화면 한 번과 승인 한 번으로 실행된다.
- [ ] 고위험 요청 5개는 기존 분리 게이트에서 멈춘다.
- [x] 초급 사용자가 폴더와 스킬 번호를 몰라도 첫 결과를 얻는다. — 2026-08-31 실측 · 초급 리뷰 분석(006) 완주 · 내부 용어 0
- [x] 기존 자동 테스트와 릴리스 검증이 전부 통과한다. — v0.50.0 · `CI=1 RELEASE=1 verify` 오류 0
- [ ] 설치본 새 대화에서 초급 5명 기준 첫 업무 시작 성공률 90% 이상이다.
- [x] 승인 전 쓰기·계획 밖 실행·검토 누락은 0건이다. — 실측 실행 전수에서 0건 · 훅+회귀 45검사로 상시 강제

## 14. 운영 개선 · 일일 자가검증과 안전한 문제 해결

### 14.1 목표와 원칙

매일 플러그인의 구조·회귀·배포 상태를 자동으로 검사하고, 재현 가능한 저위험 결함은 격리된
작업 공간에서 수정안까지 만든다. **자동 병합·자동 배포가 목표는 아니다.** 최종 반영 권한은
사용자에게 둔다.

이 자동화는 사용자에게 보이는 새 담당이 아니다. 기존 `AI 마케터`·`AI 사업검토자`·
`AI 규제검토자`·`AI 규제세팅` 구조를 바꾸지 않고, 플러그인 런타임 밖에서 도는 유지보수 절차다.

핵심 원칙:

1. **검사 대상과 검사자를 분리한다.** 플러그인 실행 규칙이 자기 기준을 임의로 바꾸지 못하게 한다.
2. **수정은 격리된 Git worktree에서만 한다.** 사용자가 작업 중인 브랜치와 파일을 건드리지 않는다.
3. **실패를 재현한 뒤 고친다.** 재현 테스트가 없으면 원인 분석과 보고까지만 한다.
4. **테스트를 약하게 만들어 통과시키지 않는다.** 기준선·게이트·PII 정책의 자동 완화를 금지한다.
5. **수정 뒤 전체 검사를 다시 돈다.** 관련 테스트만 통과한 상태로 완료하지 않는다.
6. **병합·푸시·버전 상승·배포는 사람이 승인한다.** 예약 작업에는 릴리스 권한을 주지 않는다.

### 14.2 목표 운영 흐름

```text
매일 예약 실행
  → 현재 커밋·설치본·Git 상태 기록
  → 격리 worktree 생성
  → 전체 verify + 핵심 회귀 실행
  → 실패 원인 분류
      ├─ 기계적으로 확정되는 저위험 결함 → 최소 수정
      ├─ 코드 결함 → 재현 테스트 + 최소 수정안
      └─ 안전·정책·실데이터 결함 → 수정 금지 · 사용자 판단 요청
  → 관련 테스트 + 전체 verify 재실행
  → 원인·변경·검증·남은 위험 보고
  → 사용자 승인 뒤에만 병합·버전 상승·배포
```

정상일 때는 검사 수와 `🔴 0 · 🟡 0`만 짧게 보고한다. 실패했을 때는 원인·재현 명령·영향 범위·
수정 여부·남은 판단을 구분해 보고한다.

### 14.3 검사 주기

| 주기 | 검사 범위 | 목적 |
|---|---|---|
| 매일 | `verify.mjs` 전체 · Git 추적·버전·링크·생성물·회귀 | 구조와 배포 드리프트 조기 발견 |
| 매일 순환 | 초급·중급·고급 중 한 시나리오 | 한 번에 드는 시간과 모델 변동을 제한 |
| 주 1회 | 초급·중급·고급 새 대화 3종 완주 | 실제 라우팅·승인·검토·파일 착지 확인 |
| 릴리스 전 | Claude Code·Codex 설치본 교차 실기 | 호스트별 훅·도구·하위 담당 차이 확인 |
| 월 1회 | 실제 사용자 실패 표현·오탐·미탐 검토 | 평가셋과 사용자 경험 개선 |

매일 실모델 596건 전체를 호출하지 않는다. 결정론적 회귀는 매일, 비용과 변동성이 큰 실모델 평가는
주간·릴리스 관문으로 분리한다.

### 14.4 자동 수정 정책

| 등급 | 예시 | 자동 행동 |
|---|---|---|
| **A · 기계 수정** | 생성 카탈로그·통계 불일치, 내부 링크, 줄바꿈, 추적 누락 탐지 | 정해진 생성 명령으로 수정안 생성 가능 |
| **B · 재현 가능한 코드 결함** | 경로 파싱, 호스트 도구명 호환, 고정 입력 회귀 | 재현 테스트 추가 후 격리 수정안 생성 가능 |
| **C · 판단이 필요한 동작 변경** | 라우팅 정답, 질문 방식, 산출물 계약, 담당 호출 조건 | 수정하지 않고 대안과 영향 보고 |
| **D · 안전·배포 변경** | `gate`·`pii`·`review`, 승인 조건, 기준선, 버전, 병합·푸시·릴리스 | 자동 수정 금지 · 사용자 승인 필수 |

금지 규칙:

- 실패를 없애기 위해 테스트를 삭제·완화하지 않는다.
- `routing-baseline.json`과 실기 기준값을 자동 갱신하지 않는다.
- 개인정보·발행·예산 안전 게이트를 자동으로 줄이지 않는다.
- 사용자의 dirty worktree를 청소하거나 덮어쓰지 않는다.
- 검증 실패 상태에서 버전을 올리거나 배포하지 않는다.

### 14.5 구현 단계

#### M0 · 기존 검사 기반 고정 — 완료

- [x] 패키지 전체 진입점 `scripts/verify.mjs`
- [x] 라우팅·계획·체인·영수증·PII·검토·이벤트 회귀
- [x] 첫 사용 UX와 실기동 결함 회귀
- [x] 버전·Git 추적·설치본·배포 파일 검사

#### M1 · 일일 실행기 — 완료 (2026-08-31)

- [x] `scripts/daily-health-check.mjs`에서 검사 목록·시간·종료 코드·요약 JSON을 생성한다.
- [x] 개별 실패를 구조·회귀·안전 영역으로 분류한다 (실기는 §14.3 대로 주간·릴리스 관문 —
      일일 실행기는 결정론 검사만 돈다).
- [x] 한 검사가 멈춰도 나머지 읽기 검사를 계속하고 마지막에 통합 실패한다 · 검사별 시간 제한 포함.
- [x] 정상 실행과 의도적 실패를 `scripts/test-daily-health-check.mjs`로 회귀시킨다
      (실패 계속 · 시간 초과 · 연속 실패 회로 · 정책 무결 · 14검사) — `verify.mjs`에 배선.

M1 에서 확정한 것 (구현 반영):

- `verify` 의 🟡 은 실패가 아니라 **「작업 중 드리프트」 정보**로 분류한다 — 개발 중 워킹트리 오탐 방지.
- `maintenance/` 는 **저장소 루트 · 플러그인 패키지 비포함**으로 확정 (배포·버전 드리프트 검사 밖).
  `maintenance/reports/` 는 운영 로그라 git 추적도 하지 않는다.
- 실행기 자신과 정책 파일을 **정책의 자동 수정 금지 목록에 포함**하고, 빠지면 「안전」 검사가 실패한다.
- 같은 검사의 연속 실패는 보고서에 `circuit_break` 로 표시한다 (M4 의 2회 차단이 이 값을 쓴다).

**실측 (2026-08-31 · 커밋 6d0d352):** 검사 13(구조 1 · 회귀 11 · 안전 1) · 실패 0 ·
드리프트 🟡1 정보 분류 · 보고서 `maintenance/reports/` 착지 · 전체 6초.

#### M2 · 격리와 수정 정책 — 구현 전

- [ ] 예약 작업은 항상 전용 worktree와 날짜별 유지보수 브랜치에서 시작한다.
- [ ] A·B 등급만 수정 가능하도록 허용 목록을 둔다.
- [ ] 테스트·기준선·안전 정책·버전 파일은 자동 수정 금지 목록에 둔다.
- [ ] 수정 전후 diff와 실행 명령을 결과 보고서에 남긴다.

#### M3 · Codex 예약 실행 — 구현 전

- [ ] ChatGPT 데스크톱 앱의 Scheduled에서 매일 한 번 실행한다.
- [ ] 프로젝트 worktree 모드와 `workspace-write` 수준의 최소 권한을 사용한다.
- [ ] 컴퓨터·앱이 꺼져 실행하지 못한 날은 실패가 아니라 `미실행`으로 구분한다.
- [ ] 첫 7회는 자동 수정 없이 관찰 모드로 돌려 오탐과 소요 시간을 측정한다.

#### M4 · 안전한 수정 루프 — 구현 전

- [ ] 실패 재현 → 최소 수정 → 관련 테스트 → 전체 `verify` 순서를 강제한다.
- [ ] 수정 성공 시에도 자동 병합하지 않고 검토 가능한 diff만 제공한다.
- [ ] 같은 실패가 2회 반복되면 추가 자동 수정 없이 회로 차단한다.
- [ ] C·D 등급은 원인·선택지·권고안만 보고하고 사용자 결정을 기다린다.

### 14.6 구현 대상

| 대상 | 역할 |
|---|---|
| `scripts/daily-health-check.mjs` | 전체 검사를 호출하고 결과를 구조화하는 단일 진입점 |
| `scripts/test-daily-health-check.mjs` | 정상·실패·중단·오탐 회귀 |
| `maintenance/self-check-policy.json` | 자동 수정 허용·금지 파일과 반복 실패 상한 |
| `maintenance/reports/` | 날짜·커밋별 원인·수정·검증 결과 |
| `scripts/verify.mjs` | 일일 실행기와 정책 파일 존재·회귀 배선 검사 |
| Codex Scheduled 작업 | 매일 실행·worktree 격리·결과 알림 |

`maintenance/`는 배포 플러그인의 사용자 기능이 아니라 저장소 유지보수 자산이다.
**M1 확정 (2026-08-31): 저장소 루트에 두고 플러그인 패키지에 넣지 않는다.** 실행기와 회귀는
`scripts/` 에 있어 배포에 실리지만 런타임이 호출하지 않는 불활성 자산이고, 훅 허용 목록에도 없다.

### 14.7 예약 작업 계약

예약 작업은 다음을 반드시 지킨다.

- 작업 중인 기본 checkout을 수정하지 않는다.
- 먼저 `node plugins/marketing-team/scripts/verify.mjs`를 실행한다.
- 실패를 관련 회귀 테스트로 재현한 뒤에만 소스를 수정한다.
- A·B 등급만 최소 수정하고 C·D 등급은 보고만 한다.
- 수정 후 관련 테스트와 전체 `verify`를 다시 실행한다.
- 커밋·병합·푸시·버전 상승·배포를 수행하지 않는다.
- 문제가 없으면 짧게, 문제가 있으면 재현과 영향을 포함해 보고한다.

### 14.8 측정 지표와 인수 조건

| 지표 | 목표 |
|---|---:|
| 예약 실행 성공률 | 최근 30일 **95% 이상** |
| 결정론적 회귀 통과율 | **100%** |
| 실패 탐지부터 보고까지 | **30분 이내** |
| 자동 수정 오탐 | **0건** |
| 사용자 작업 폴더 오염 | **0건** |
| 테스트·기준선 자동 완화 | **0건** |
| 승인 없는 병합·배포 | **0건** |

최종 인수 조건:

- [ ] 관찰 모드 7회가 사용자 작업 폴더를 바꾸지 않고 완료된다.
- [ ] 의도적으로 넣은 A·B 결함을 탐지하고 격리된 수정안으로 복구한다.
- [ ] C·D 결함에서는 파일을 바꾸지 않고 사용자 판단을 요청한다.
- [ ] 같은 실패 2회에서 회로 차단이 동작한다.
- [ ] 수정 뒤 관련 테스트와 전체 `verify`가 모두 통과한다.
- [ ] Claude Code·Codex 양쪽 설치본에서 기존 승인·검토·PII 규칙이 유지된다.
