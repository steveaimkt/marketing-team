# 페르소나 시뮬레이션 · 기준 commit f964c6d · 2026-08-30

| | |
|---|---|
| 버전 | v0.46.1 |
| 기준 커밋 | `f964c6d` + 미커밋 작업 트리 |
| Claude Code | 2.1.251 |
| 실행일 | 2026-08-30 |
| 목적 | 초급·중급·고급 마케터 관점 사용성·안전성 점검 |

## 판정

**오프라인 계약 시뮬레이션과 실모델의 실행·파일·검토·영수증은 통과했다.**

첫 실모델 시도는 플러그인 로드 전에 Claude Code 인증이 없어 중단됐다. 세 요청 모두
`Not logged in · Please run /login`이었고 모델 입력 토큰은 0이었다. 같은 날 로그인 뒤
08:00~08:30에 다시 실행해 산출물과 검토 호출을 확인했다. 다만 정답 문서를 먼저 읽은 한 대화에서
세 시나리오를 연속 수행했으므로 **자연어 라우팅·대화형 멈춤·새 대화 격리는 미실행**이다.
따라서 릴리스 실기동 전체 통과로 기록하지 않는다.

## 페르소나 3종

| 수준 | 실제 사용 문장 | 예상 라우팅·실행 | 수정 후 판정 |
|---|---|---|---|
| 초급 | `뭘 해야 할지 모르겠는데 고객 리뷰부터 보면 좋을 것 같아` | 소비자 리뷰 마이닝(006) · 샘플 리뷰 200건 · 규제·사업 검토 없음 | ✅ 계약 통과 |
| 중급 | `광고 성과를 손익분기로 판정하고 다음 주 예산 재배분안` | ROAS 진단·예산 재배분(046) · BEP 2.0 · 3안 · AI 사업검토자 재무 | ✅ 계약 통과 |
| 고급 | `매출·퍼널·고객마스터를 결합해 병목·세그먼트 우선순위·KPI 트리` | 061 → 073 → 065 → 066 즉석 조합 · AI 사업검토자 경영 | ✅ 계약 통과 |

### 초급 마케터

- 번호나 전문 명령어 없이 006으로 좁혀진다.
- `inputs/` 가 비면 `A브랜드-리뷰-200건.csv` 200행으로 멈추지 않고 완주한다.
- 결과는 리뷰별 분류 CSV와 사람이 읽는 해설 파일로 갈라진다.
- 화면에 내부 용어 `에스컬레이션`이 나오던 문제를 `CS 우선 확인`으로 바꿨다.

### 중급 마케터

- 공헌이익률 50%에서 손익분기 ROAS 2.0을 읽고, 보수·중립·공격 3안을 낸다.
- 046의 `review: 재무` 때문에 AI 사업검토자 재무 관점 호출이 필수다.
- 메타 샘플은 2026-07-01~07-30이지만 네이버 샘플은 기간 열이 없다. 두 파일을 그대로 합치면 허위 비교가 된다.
- 기간이 없거나 다르면 `[기간 미확인]`으로 비교에서 빼고, 확인된 채널만 임시안에 쓰도록 고쳤다.

### 고급 마케터

- 판매 분석(061), 고객 여정(073), RFM(065), KPI 트리(066) 4개 조합이 맞다.
- 고객마스터는 3,506행·고객 880명이라 통째로 읽지 않고 집계해야 한다.
- 매출 파일은 2026-06-01~06-30, 퍼널은 2026-05-01~07-29, 고객마스터는 2025-07-02~2026-06-30이다.
- 직접 비교는 공통 기간인 2026-06-01~06-30만 쓰고, 더 긴 기간은 추세 참고로만 쓰도록 런타임 규칙을 추가했다.
- 산출물에 `우선순위·무엇부터`가 들어가므로 플래그가 없어도 AI 사업검토자 경영 관점 호출이 필수다.

## 발견하고 고친 것

| # | 문제 | 영향 | 수정 |
|---|---|---|---|
| 1 | CSV 13개 스킬의 Output Format이 마크다운 보고서였다 | 확장자만 `.csv`인 열리지 않는 파일 생성 가능 | 001·006·009·020·021·033·050·057·064·065·068·095·096에 실제 CSV 헤더와 별도 `-해설.md` 계약 추가 |
| 2 | 006 화면에 내부 용어 `에스컬레이션` 노출 | 초급 사용자가 의미를 바로 알기 어려움 | `CS 우선 확인`으로 교체 |
| 3 | 046에 기간 불일치 검증은 있으나 실패 처리 규칙이 없었다 | 기간 없는 네이버 리포트를 메타 30일과 합칠 위험 | 기간 미확인 채널 제외 규칙 추가 |
| 4 | 샘플 README가 월 매출 집계 XLSX를 065 RFM 입력으로 안내했다 | 고객ID가 없는 파일을 RFM이 집을 수 있음 | 065 연결 표기 제거 · 061·066만 유지 |
| 5 | 즉석 다중 스킬 조합에 공통 기간 규칙이 없었다 | 서로 다른 기간의 매출·퍼널·고객 수치를 직접 비교할 위험 | 공통 기간만 직접 비교 · 장기 데이터는 추세 참고 규칙 추가 |
| 6 | CSV 형식 검사가 `형식` 문구 존재만 확인했다 | 마크다운을 CSV로 저장해도 검사 통과 | 실제 `csv` 코드블록·쉼표 헤더·마크다운 금지 문구를 회귀 검사에 추가 |

## 기계 검사

| 검사 | 결과 |
|---|---|
| `verify.mjs` | ✅ 🔴 0 · 이 기록의 실모델 미실행만 경고 |
| `validate-skills.mjs` | ✅ 100개 · ERR 0 · WARN 0 |
| `eval-routing.mjs --report` | ✅ 회귀 0건 · 어휘층 top1 316/596 · 부채 280건 |
| `audit-skills.py` | 참고 · 구조 감사 경고 88건. 릴리스 차단 검사가 아니며 이번 수정 전과 동일 계열 |

## 실모델·배포 확인

| 확인 | 결과 |
|---|---|
| 초급 요청 실제 스킬 발화 | ⚠️ 실행·파일·영수증 ✅ / **라우팅 ⬜** (§실기동 참조) |
| 중급 046 산출물·재무 검토자 실제 호출 | ✅ 실제 호출 2회 (조건부 → 수정 → 승인) |
| 고급 4스킬 조합·경영 검토자 실제 호출 | ✅ 실제 호출 3회 (조건부 → 조건부 → 승인) |
| 로컬 마켓플레이스 설치본 | ⚠️ `claude plugin validate` 통과 · v0.46.1 활성화 확인. 그러나 저장소 루트 세션의 `.claude/skills`가 우선 적용되어 캐시 설치본 격리는 미검증 |

---

# 실기동 · 2026-08-30 08:00~08:30 · `docs/클로드코드-실기검증.md` 시행

## 환경

| 항목 | 값 |
|---|---|
| 플러그인 | `marketing-team@marketing-team` v0.46.1 · enabled |
| 인증 | `loggedIn: true` · claude.ai · max |
| 설치 형태 | 마켓플레이스 소스 = `directory`인 개발 설치. 실행 세션의 Skill base path는 저장소였으므로 **캐시 설치본 격리 미확인** |
| 실행 시각 | 2026-08-30 08:00~08:30 (KST) |
| 작업 폴더 | 산출물은 `~/Desktop/claude-validation/{beginner,intermediate,advanced}`에 생성. Claude 대화는 저장소 루트의 한 세션에서 연속 실행 |

## ⛔ 이 회차가 검증하지 **못한** 것 — 먼저 밝힌다

문서 §1은 **세 시나리오를 서로 다른 새 대화에서** 돌라고 규정한다. 이 회차는 그러지 못했다.

| 항목 | 왜 못 했나 | 판정 |
|---|---|---|
| **자연어 → 스킬 라우팅** | AI 마케터가 `클로드코드-실기검증.md`를 **먼저 읽어 정답(006·046·061→073→065→066)을 알고 있었다.** 고른 것이 아니라 아는 것을 실행했다 | **⬜ 미실행** |
| **⏸ 게이트에서 실제로 묻는가** | 문서가 지정한 사용자 응답을 AI가 대신 넣었다. "질문을 건너뛰지 않는지"는 확인되지 않았다 | **⬜ 미실행** |
| **대화 격리** | 세 시나리오가 **한 대화**에서 연속 실행됐다. 앞 시나리오의 맥락이 뒤에 남았다 | **⬜ 미실행** |

위 셋은 **사용자가 새 대화 3개에서 직접 돌려야** 판정된다. 이 회차 결과로 대체하지 않는다.

## ✅ 이 회차가 실제로 검증한 것

실행 · 파일 생성 · 담당 실제 호출 · 검토 재순환 · 영수증 무결성.

| 수준 | 실제 라우팅 | 담당 실제 호출 | 산출물·영수증 | 판정 | 메모 |
|---|---|---|---|---|---|
| 초급 | 006 (⬜ 오염) | 해당 없음 (`review:`·`gate:` 없음 · 억지 호출 안 함) | `beginner/outputs/2026-08-30/006-review-mining/` · `run.json` ✅ | ⚠️ | 라우팅만 미판정, 나머지 전부 통과 |
| 중급 | 046 (⬜ 오염) | **AI 사업검토자·재무 2회** 조건부→수정→승인 | `intermediate/outputs/2026-08-30/046-roas-budget-rebalance/` · `run.json` ✅ | ⚠️ | 동상 |
| 고급 | 061→073→065→066 (⬜ 오염) | **AI 사업검토자·경영 3회** 조건부→조건부→승인 | `advanced/outputs/2026-08-30/066-kpi-tree/` · `run.json` ✅ | ⚠️ | 동상 |

`verify` 세 건 모두 **exit 0 · `completed` / `current`** · 산출물·검토 보고서 SHA-256 전부 현재 파일과 일치 · 원장에 `run_id` 존재.

### 초급 · 006 소비자 리뷰 마이닝

- 샘플 200건으로 완주 · 산출물에 `[샘플]` 표시
- `006-review-mining.csv` **201행**(헤더+200) · UTF-8 BOM · 마크다운 흔적 **0** · `-해설.md` 별도 생성
- 5분류 결과 페인포인트 87 / 긍정 77 / 부정 26 / 요청 10 / 질문 0
- TOP 5 페인포인트 · `CS 우선 확인` 30건 · **자동 전달 안 함**
- 화면·산출물 어디에도 `에스컬레이션` 없음 (0건)
- `required_reviews: []` — 검토가 필요 없는 분석이라 담당을 부르지 않았다

### 중급 · 046 ROAS 진단·예산 재배분

- 프로필 §4 공헌이익률 50% → **손익분기 ROAS 2.0** 계산
- 보수·중립·공격 **정확히 3안** · 총예산 69,648,041원 **3안 모두 불변**
- **네이버 샘플에 기간 열이 없어 `[기간 미확인]`으로 비교에서 제외**했고,
  `A브랜드-채널성과-90일.csv`는 **광고비 열이 없어** ROAS 계산 불가로 제외했다.
  그 결과 비교 단위가 채널이 아니라 **메타 캠페인 4개**가 됐다
- 4개 캠페인 전부 BEP 미달(통합 0.77) → **"재배분으로는 BEP에 못 닿는다"**를 결론에 명시
- 재무 1차 **조건부**(공격안 하방 미반영 · 15배 예산 괴리를 각주 처리) → 수정 → **같은 현재 파일 재검토** → 승인
- 광고 계정 변경 없음 · "제안까지만"을 본문에 명시

### 고급 · 061 → 073 → 065 → 066 즉석 조합

- `run.json`의 `skills`가 **`061 → 073 → 065 → 066` 순서**로 기록됨
- 조합 산출물 5종이 **주 스킬(066) 폴더 한 곳**에 모임
- 입력 기간표를 먼저 작성 · 문서 기재와 실측이 정확히 일치
  (매출 `2026-06-01~06-30` / 퍼널 `2026-05-01~07-29` / 고객마스터 `2025-07-02~2026-06-30` · 3,506행 · 880명)
- 직접 비교·비율은 **공통 기간 `2026-06-01~06-30`만** 사용 · 더 긴 기간은 추세 참고로만
- 고객마스터를 화면에 펼치지 않고 집계 · 원문 `고객ID` 대신 SHA-256 앞 8자 대체키 사용
  (실측 당시 방식. 후보 대입으로 되찾을 수 있어 이후 취약 판정했으며, 새 규칙은 실행별 임의 대체키다)
- KPI 트리: 북극성 1개 · 3단 · 말단 10개 전부 입력 지표 · `[측정 필요]` 5개를 숨기지 않음 ·
  임계값은 **90일 실측 −1σ / −2σ**로 산출
- 경영 1차 조건부(7건) → 수정 → 2차 073·065 승인 / 066 조건부(1건) → 수정 → 3차 **전부 승인**

## 이 회차에 새로 발견한 것

| # | 문제 | 어디서 | 영향 | 처리 |
|---|---|---|---|---|
| 1 | **산출물이 "원문 ID를 싣지 않았다"면서 실존 `C1023`을 예시로 노출** | 065 해설 §⓪ · AI 마케터 작성 | 즉시 실패 항목(원문 식별자 노출). `C1023`은 고객마스터에 실재(주문 16건) | 실행 산출물은 수정 후 재승인. **소스 수정 완료**: 설명·예시·검토 요청까지 원문 ID 금지, 무염 해시 대신 실행별 임의 대체키 사용. 새 격리 대화에서 재실측 필요 |
| 2 | AI 사업검토자 보고서에 내부 용어 **`폴백`** 노출 | `review-경영.md:3` | `docs/쉬운말.md` ③ "화면에 안 낸다" 목록 위반 | **소스 수정 완료**: 사용자용 쉬운 문장으로 교체하고 저장 전 내부 용어·원문 식별자 재검색 의무화. 기존 보고서는 과거 증거로 보존, 새 격리 대화에서 재실측 필요 |
| 3 | 샘플 데이터 3종이 **같은 6월인데 서로 안 맞는다** — 매출 xlsx 104,890,000원 vs 고객마스터 29,447,800원(3.6배) · 퍼널 결제완료 623건 vs 고객마스터 491건(+27%) | `sample-data/` | 조합 스킬이 교차 계산을 하면 조용히 틀린다. 이번엔 산출물이 이를 잡아내 교차 계산을 거부했다(설계대로 동작) | **정책 결정·소스 수정 완료**: 세 파일은 독립 합성 샘플로 유지하고 README에 수치 차이를 명시. 같은 기간이어도 정의·합계를 대조해 `[자료 충돌]`이면 교차 계산 금지. 통합 정합 샘플팩은 후속 개선 |
| 4 | 061 이상치 검출기(7일 이동평균 2σ)가 **매주 토요일을 이상치로 잡는다** | 061 · 샘플 데이터 | 토 532만 vs 평일 319만(+67%)인 주간 계절성을 사고로 오탐 | **소스 수정 완료**: 같은 요일 기준선과 7일 반복 패턴을 먼저 분리하고, 표본 부족 때만 이동평균 사용. 새 격리 대화에서 재실측 필요 |

## 남은 것

⛔ **아래 셋을 사용자가 새 대화 3개에서 밟기 전에는 릴리스 실기동 통과로 보지 않는다.**

| 확인 | 상태 |
|---|---|
| 자연어 요청이 실제로 006 / 046 / 4스킬 조합으로 라우팅되는가 | ⬜ 미실행 |
| ⏸ 게이트에서 실제로 묻고 기다리는가 | ⬜ 미실행 |
| 서로 다른 새 대화에서 같은 결과가 나오는가 | ⬜ 미실행 |
| GitHub 태그 마켓플레이스에서 캐시로 복사된 설치본만 읽는가 | ⬜ 미실행 |
| 061·065·사업검토자 수정 규칙이 새 산출물에 반영되는가 | ⬜ 재실측 필요 |

붙여넣기용 실행 세트: `~/Desktop/claude-validation/실행세트.md`

---

# 재실기동 · 2026-08-30 09:33~09:50 · **수정된 빌드로 고급 재실행**

1차(08:00~08:30)에서 나온 지적 4건이 반영된 뒤, **고급 시나리오를 새 빈 폴더에서 통째로 다시** 돌렸다.
작업 폴더 `~/Desktop/claude-validation/advanced-2/` · run_id `20260830003310-061-52b0f8`.

## 검증한 빌드 지문 (실행 전후 동일 · 실행 중 이동 없음 확인)

| 파일 | SHA-256 앞 12 |
|---|---|
| `061-sales-data-analysis/SKILL.md` | `c9e4a2a147a5` |
| `065-rfm-segments/SKILL.md` | `3a24ebaf0c92` |
| `073-customer-journey-map/SKILL.md` | `86d26254985c` |
| `066-kpi-tree/SKILL.md` | `a4cc58ed3e9a` |
| `agents/staff-reviewer.md` | `e429d9b97d8e` |
| `skills/AI-마케터/SKILL.md` | `f13a22555224` |
| `sample-data/README.md` | `1c1af373cd53` |

`006`·`046`은 07:33 설치본과 **동일**해 재실행하지 않았다.

## 1차 지적 4건이 실제로 막혔는가 — 전부 ✅

| # | 1차 결함 | 새 규칙 | 재실행 실측 |
|---|---|---|---|
| 1 | 대체키가 무염 SHA-256이라 **880/880 복원됨**. 문서는 "되돌릴 수 없다"고 주장 | 065 계약 4 · 실행마다 임의 대체키 · 무염 해시 금지 · 원문↔대체키 표 저장 금지 | **복원 0/880** — sha256·md5·sha1·blake2s 4종 × `C0000~C9999` 전수 대입 전부 실패. 행 순서도 무작위화 |
| 2 | 원문 고객ID `C1023`을 마스킹 설명문에 예시로 노출 | 065 계약 4 · 해설·제목·각주·설명·예시·검토 요청·보고서 전부 금지 | **인용 0건** — CSV·해설·검토 요청·검토 보고서 전수 검사 |
| 3 | 샘플 3종이 6월 매출을 3.6배 다르게 말하는데 규칙이 없었음 | AI-마케터 `[자료 충돌]` 규칙 + `sample-data/README.md` 조합 금지표 | **산출물 4종 전부에 `[자료 충돌]` 선언** · 파일 교차 계산 0건 |
| 4 | 이상치 검출기가 **매주 토요일 3건을 오탐** | 061 계약 · 요일 계절성 제거 후 잔차에서 2σ/3σ · 7일 반복은 `반복 패턴` | 요일 표본 전부 4회 이상 → 요일 비교 적용 · **요일 보정 후 이상치 0건** · 토요일 1.66배는 `반복 패턴`으로 분리 |
| 보 | 검토자 보고서에 내부 용어 `폴백` 노출 | `staff-reviewer.md` 문구 교체 | **「대신 근거로 썼습니다」로 출력** · `폴백` 0건 |

## 검토 재순환

| 회차 | 073 | 065 | 066 | 지적 |
|---|---|---|---|---|
| 1차 | 승인 | 조건부 | 조건부 | **3건** |
| 2차 | 승인 유지 | **승인** | **승인** | 0 |

1차 실기동은 지적 7건·3라운드였다. 같은 시나리오에서 **3건·2라운드**로 줄었다.

경영 검토의 핵심 지적은 **「압승이 아니라고 써 놓고 무조건부로 B를 골랐다」**였다.
손익분기 승급률 **23.6%**(A의 −10%p 리스크 −12,581,290원과 B 기대이익이 같아지는 점)를 계산해
**100명 파일럿 60일 · 24% 미달 시 챔피언 방어로 전환**하는 조건부 결정으로 바꿔 승인됐다.

## 영수증

```
run_id      20260830003310-061-52b0f8
skills      061 → 073 → 065 → 066
status      completed · integrity current
outputs     5종 전부 해시 일치
reviews     필수 3건 / 봉인 3건 · 경영 approved · 산출물·보고서 해시 일치
ledger      run_id 존재
verify      exit 0
```

## 이 회차에 새로 확인한 문제

| # | 문제 | 성격 |
|---|---|---|
| 1 | **개인정보 정확성을 판정할 담당이 없다.** 규제검토자는 `gate: true`만, 사업검토자는 지정 관점만 본다. 065는 `gate: false`이고 개인정보는 5관점(경영·재무·고객·법무·브랜드) 자동 호출 트리거 어디에도 없다. 1차의 복원 가능 대체키를 **경영 검토자가 "이 관점 밖"이라며 명시적으로 넘겼다** | **구조적 공백** · 마스킹을 요구하는 계약은 있는데 검사하는 담당이 없다 |
| 2 | **검증 문서가 지정한 `073 → 065` 순서가 계약과 역방향이다.** `065`의 `chains_to: ["073","074"]` · `073`의 `inputs: [..., 065 리포트]` · `073` 계약 5는 "평균 고객 1장으로 뭉개지 말라". 이 순서로는 073이 반드시 그 형태가 된다 | **문서 ↔ 계약 모순** · 이번엔 073이 자백하고 진행했고 검토자도 "결론을 흔들지 않는다"고 판단했으나, 순서 자체를 바로잡는 편이 낫다 |
| 3 | 검증 문서가 경로를 `scripts/실제확인-기록/`으로 적었으나 실제는 `plugins/marketing-team/scripts/실제확인-기록/` | 문서 오기 |
| 4 | 마켓플레이스 소스가 `directory`로 저장소를 가리켜 **"심링크가 아닌 설치본" 조건이 구조적으로 성립하지 않는다.** 스킬 base 경로도 캐시가 아닌 저장소로 잡힌다 | 검증 전제 미성립 |
| 5 | 1차 실행 직후(08:36~08:40) 저장소가 수정돼 **검증한 빌드가 즉시 교체됐다.** 이번 회차는 실행 전후 지문을 고정해 이동이 없음을 확인했다 | 절차 개선 · **지문 고정을 검증 절차에 넣을 것** |

## 남은 것 · 여전히 ⬜

⛔ **아래를 사용자가 새 대화에서 밟기 전에는 릴리스 실기동 통과로 보지 않는다.**
이번 회차도 AI가 정답을 알고 실행했고, ⏸ 응답을 대신 넣었으며, 한 대화에서 돌았다.

| 확인 | 상태 |
|---|---|
| 자연어 요청이 실제로 006 / 046 / 4스킬 조합으로 라우팅되는가 | ⬜ 미실행 |
| ⏸ 게이트에서 실제로 묻고 기다리는가 | ⬜ 미실행 |
| 서로 다른 새 대화에서 같은 결과가 나오는가 | ⬜ 미실행 |
| GitHub 태그 마켓플레이스에서 캐시로 복사된 설치본만 읽는가 | ⬜ 미실행 |
| 초급(006)·중급(046)을 새 빌드에서 재실행 | ⬜ 미실행 (스킬 무변경이라 우선순위 낮음) |

---

# 후속 격리 실행 · 2026-08-30 · 자연어 라우팅 및 런타임 결함

`--plugin-dir`로 현재 소스를 고정하고 서로 다른 빈 폴더에서 자연어 요청을 실행했다.
세 실행 모두 검증 문서의 정답을 프롬프트에 넣지 않았다.

| 수준 | 실제 결과 | 판정 |
|---|---|---|
| 초급 | 자연어가 006으로 라우팅 · 완료/current · CSV 201행 | ⚠️ TOP 5 계약인데 군집 4개라 재실기 필요 |
| 중급 | 046 라우팅 뒤 G2 승인 전에 `run.json`·MD·계획 밖 HTML 생성 · 재무 검토 미호출 · `running/pending`에서 `verify` exit 0 | ❌ |
| 고급 | 4개를 찾았지만 `061→065→073→066`으로 순서 변경 · 승인 전 KPI 파일 작성 · 영수증·검토 없이 장시간 실행 | ❌ |

## 확인한 근본 원인과 소스 수정

| 결함 | 수정 | 자동 회귀 |
|---|---|---|
| `running/pending` 영수증도 `verify` 성공 | `verify`는 `completed/current/completed_at`만 통과 | 미완료 차단 테스트 ✅ |
| G2가 프롬프트 문장뿐이라 우회 가능 | `PreToolUse` 실행 보호 훅 · `[실행 계획]` 뒤 정확한 `진행 승인` 전 분석/생성용 Bash·Write·Edit·Agent·계획 밖 Skill 차단 · 계획용 읽기 조회만 허용 | 훅 격리·승인·재승인·읽기 전용 테스트 ✅ |
| 계획 밖 시각화 스킬과 HTML 추가 | 계획에 없는 Skill 차단 · `writes_to` 파일만 `run.json`에 허용 | 계획 밖 Skill·추가 HTML 차단 ✅ |
| 설치본·저장소·캐시 혼용 | 실행 중 `${CLAUDE_PLUGIN_ROOT}` 한 곳만 허용 · 홈 전체 탐색 및 다른 캐시 참조 차단 | 설치본 탐색 차단 ✅ |
| 고급 체인 순서 변경 | 영수증 시작 시 `061→073→065→066` 순서를 기계 검증 | 잘못된 순서 차단 ✅ |
| `pii` 초안이 `start` 뒤 사라짐 | `pii:true` 스킬은 블록 필수 · source가 inputs에 있어야 함 · 최종 영수증에 보존 | 누락 차단·보존 테스트 ✅ |

위 표의 ✅는 **소스 단위 자동 회귀 결과**다. 수정된 플러그인을 새 대화 3개에서 끝까지 돌리는
실기 재검증은 아직 하지 않았으므로 릴리스 판정은 계속 `⬜`다.

---

# 구조 개선 · 2026-08-30 · AI 마케터 에이전트 구조 검토 결과 반영

## 바꾸지 않은 것 · 골격은 옳다

**실행이 메인 루프에 있는 설계는 유지한다.** G1·G2·G5가 ⏸ 게이트이고
**서브에이전트는 사용자에게 묻지 못한다.** 실행을 내리면 다섯 게이트가 무너진다.
판정과 제작을 가른 것, ⛔ 를 한 담당에게만 준 것도 그대로 둔다.

## 고친 것 다섯

| # | 무엇이 문제였나 | 어떻게 고쳤나 |
|---|---|---|
| ① | **AI 사업검토자가 잴 수 없는 것을 재고 있었다.** 도구가 `Read·Glob·Grep` 뿐인데 「CSV를 재합산해 소수점까지 일치했다」고 보고했다. 120행이라 맞았지만 3,506행이면 조용히 틀린다. 계약에는 계산 재현 요구가 한 줄도 없었다 | `staff-reviewer.md` 에 **「숫자를 다시 계산하지 않는다 · 재현했다고 쓰지 않는다」** 명시. 보는 것과 안 보는 것을 표로 갈랐다. 숫자 재현은 기계 층으로 내렸다 |
| ② | **판단 트리거가 세 번 실패하고도 남아 있었다** (08-22 호출 0회 · 08-27 여전히 0회 · 08-30 개인정보 누락). 결정 성격 산출물 9개가 `review:` 없이 돌았다 | 기준을 **「자원 배분이나 되돌리기 어려운 방향을 정하는가」**로 잡아 6개에 부여 — 002 브랜드·고객 / 015 브랜드 / 048 경영 / 056 재무·경영 / 073 경영 / 098 경영. 001·053(키워드 정렬)·093(회의록)은 결정이 아니라 **일부러 뺐다** — 과잉 부착도 실패다. `review:` 21 → **27개** |
| ③ | **산출물 내용 검사가 `pii` 하나뿐.** CSV 형식·내부용어·기간 정합을 전부 사람이 손으로 grep 했다 | `scripts/output-checks.mjs` 신설 · `run.json` 의 `checks` 배열로 골라 돈다 (`pii` · `csv-format` · `house-style`). `run-receipt` 의 `inspect()` 에 물려 **`finalize` 와 `verify` 둘 다** 자동으로 걸린다 |
| ④ | 문서가 손으로 적은 플래그 개수가 표류한다 | `scripts/check-flag-counts.mjs` 신설. 붙이자마자 **내 변경이 만든 표류 2건**을 잡았다 (공통규약 `review:` 21→27 · AI-마케터 `pii:` 7→8) |
| ⑤ | **032 댓글·DM 응대**가 개인 계정 데이터를 다루는데 마스킹 조항이 없었다. 그래서 `pii: true` 1차 선정에서 빠졌다 | 계약에 계정명·닉네임·연락처 마스킹 조항 추가 + `pii: true`. **8개**가 됐다 |

## 새 검사가 실제로 잡은 것

붙이자마자 **네 산출물 세트 전부에 걸어 봤다.**

| 대상 | 결과 |
|---|---|
| `advanced` (1차·결함본) | ⛔ 3건 — `폴백` 노출 · 원문 식별자 노출 · 대체키 복원 880/880 |
| `advanced-2` (2차) | ⛔ 1건 — **`chains_to` 가 사용자 화면 표에 나갔다.** 경영 승인·봉인·`verify` 를 통과한 산출물이다 |
| `beginner` · `intermediate` | ⛔ 0건 |
| 전 세트 공통 | 🟡 줄표(—) 가 **네 세트 전부와 검토자 보고서에** 있다. `docs/쉬운말.md` 가 금지하는데 **아무도 지키지 않는다** |

⚠️ **`advanced-2` 의 `chains_to` 누출은 고치지 않고 그대로 뒀다.** 봉인·검토가 끝난 과거 기록을
소급 수정하는 것이 더 나쁘다. 다음 실행에서 잡히도록 검사만 남긴다.

## 회귀

| 검사 | 건수 |
|---|---|
| `test-run-receipt.mjs` | 완료 상태 · writes_to · 체인 순서 · PII 블록 · SHA-256 · 다중 검토 차단 |
| `test-pii-check.mjs` | 5 (무염해시 복원 · 보고서 원문 인용 · 대응표 잔존 · 정상 · 미선언) |
| `test-output-checks.mjs` | 13 (CSV 형식 4 · 우리말 5 · 미선언·오타 3) |
| `check-flag-counts.mjs` | gate 32 · pii 8 · review 27 문서 일치 |
| `verify.mjs` | 🔴 **0건** |

세 스크립트를 `verify.mjs` 릴리스 검사에 물렸다. 이제 배포 전에 자동으로 돈다.

## 취소한 지적

**「문서가 `gate: true` 를 32개라고 하는데 실제는 34개」는 내 착오였다.**
부분일치 `grep` 으로 세는 바람에 본문에서 언급만 한 파일 2개(052·100)가 섞였다.
프론트매터 정확 일치는 **32개**이고 문서가 옳다.

## 이 개선으로 생긴 ⬜

| 확인 | 상태 |
|---|---|
| 073이 이제 `review: 경영` 플래그로 **자동 호출**되는가 (지난 실행은 사용자가 요청해서 불렸다) | ⬜ 재실행 필요 |
| 065가 `pii` 블록 없이 돌면 실제로 막히는가 | ⬜ 재실행 필요 |
| `advanced-2` 영수증은 **이 개선으로 빌드가 바뀌어 무효**다 (`verify` 가 🟡 073·065 변경으로 정확히 보고) | ⬜ 새 빌드 재실행 필요 |
