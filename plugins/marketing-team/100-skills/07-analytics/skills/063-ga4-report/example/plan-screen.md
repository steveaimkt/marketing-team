# 실습 계획 화면 · 063 GA4 유입 분석 (스펙 기반 재구성)

> ⚠️ **실제 대화 로그 캡처가 아니다.** 이 스킬은 원고소스에 실측 기록이 없어, SKILL.md 계약과
> ai-마케터 SKILL.md의 G1/G2 규칙에서 그대로 구성했다. 실제로 불러본 결과가 이것과 다르면
> 실제 결과가 맞는지, 이 재구성이 계약을 잘못 읽은 것인지 확인하고 필요하면 SKILL.md를 고친다.

> ⚡ **이 스킬은 「빠른 진입」 대상이다 — 계획 화면(`[실행 계획]`/`[승인 요청]`)이 뜨지 않는다.**
> 063은 `gate: false`·`mutating: false`이고 `pii` 필드가 없으며, GA4를 변경하지 않는 읽기 전용
> 분석이라 예산 확정·계약/법률 표현도 없다. ⚡ 네 조건 중 하나도 해당하지 않아 요청 한 줄 뒤
> 곧바로 G4 보고로 간다. 아래는 그 보고 화면이다.

## 나

```
GA4 리포트 뽑아줘
```

## AI 마케터

````text
GA4 연결이 없어 연습용 샘플(가상의 A브랜드)로 돌렸습니다 (결과에 [샘플]). 핵심 전환 이벤트는
기본값 `purchase`로 잡았습니다 — 다른 이벤트면 "전환 이벤트는 {이름}"이라고 다시 요청하시면 됩니다.

✅ GA4 유입 분석 완료

무엇이 나왔나
  📁 outputs/2026-09-15/063-ga4-report/063-ga4-report.html

무엇을 알게 됐나
 세팅 신뢰도부터 확인했습니다 — 핵심 이벤트 수집·중복 계측 징후·unassigned 비율 3종 검사 결과를 첫머리에 표기했습니다
 유입 소스 → 랜딩 TOP 10 → 전환 퍼널의 3단 구조로 정리했고, 표마다 해석 1줄씩 붙였습니다
 최대 이탈 구간 1곳을 "개입 지점"으로 지목하고, 개선 액션 6건을 우선순위(High/Mid/Low)와 담당 스킬로 정리했습니다

무엇이 부족한가
 연습용 데이터라 실제 세팅 오류 여부·이탈 구간과 다를 수 있습니다
 읽기 전용이라 GA4 자체는 바꾸지 않았습니다 — 세팅을 고치려면 GA4 관리자에서 직접 수정해야 합니다

다음 액션: → 048 랜딩페이지 진단 (랜딩 개입) / 050 유입 경로 추적 설계 (unassigned 비율 정리)
````

## 이 재구성이 근거로 삼은 것

- **⚡ 빠른 진입 판정** — `plugins/marketing-team/skills/ai-마케터/SKILL.md` 325~341행. mutating·pii·예산·법률 네 조건 모두 해당 없어 계획 화면을 생략했다. "결과 첫 줄에 무엇으로 도는지 밝힌다" 규칙에 따라 [샘플] 여부와 전환 이벤트 기본값을 첫 줄에 적었다.
- **G4 보고 형식** — 같은 문서 921~965행의 `✅ 완료 → 무엇이 나왔나(📁 경로) → 무엇을 알게 됐나(표+결론 3줄) → 무엇이 부족한가` 틀.
- **063 SKILL.md Contract·게이트 표** — 세팅 신뢰도 검사가 해석보다 먼저라는 규칙(36행), 유입→랜딩→전환 3단 구조(35행), 개선 액션은 담당 스킬로 연결(38행), 핵심 전환 이벤트를 한 번만 묻고 답 없으면 `purchase` 기본값을 쓴다는 게이트 표(52행)를 그대로 인용했다.
- **저장 경로·형식** — `writes_to`: `outputs/{날짜}/063-ga4-report/063-ga4-report.html` (오늘 날짜 2026-09-15).
- **다음 액션** — `chains_to: ["048", "050"]`.
