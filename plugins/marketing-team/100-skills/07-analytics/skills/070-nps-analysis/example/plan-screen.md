# 실습 계획 화면 · 070 설문 응답 분석 (스펙 기반 재구성)

> ⚠️ **실제 대화 로그 캡처가 아니다.** 이 스킬은 원고소스에 실측 기록이 없어, SKILL.md 계약과
> ai-마케터 SKILL.md의 G1/G2 규칙에서 그대로 구성했다. 실제로 불러본 결과가 이것과 다르면
> 실제 결과가 맞는지, 이 재구성이 계약을 잘못 읽은 것인지 확인하고 필요하면 SKILL.md를 고친다.

> ⛔ **이 스킬은 ⚡ 빠른 진입 대상이 아니다.** `pii: true` — 응답자 이름·연락처가 섞일 수 있는
> 주관식 설문 응답을 다루므로, ai-마케터 SKILL.md 325~341행의 ⚡ 네 조건 중 「개인정보」에
> 걸려 계획 화면을 생략할 수 없다.

## 나

```
NPS 응답 분석해줘
```

## AI 마케터

````text
[실행 계획]
**계획, 설문 응답 분석**

| 순서 | 스킬 | 무엇이 나오나 |
|---|---|---|
| 1 | 스킬 070 · 설문 응답 분석 | NPS 산출·그룹 분포, 주관식 테마 분류표(대표 인용 포함), 그룹별 사유 분석, 불만 고객 후속 플로우 제안 |

**분석할 자료** — `inputs/` 폴더가 비어 있어 연습용 샘플(가상의 A브랜드)로 진행합니다. 결과에는 `[샘플]`이 붙습니다.
- 응답 데이터: `sample-data/A브랜드-리뷰-200건.csv` (점수 열은 별점 1~5라 0~10 NPS 척도가 아닙니다 — 환산식 `(별점−1)÷4×10`을 적용하고 결과에 `[추정]`을 함께 표기합니다)

실데이터로 다시 하시려면 점수 열 + 주관식 열이 있는 설문 응답을 `inputs/`에 넣고 다시 요청하시면 됩니다. 어느 열이 점수/주관식인지 알 수 없으면 먼저 여쭤봅니다(지어내지 않습니다).

**근거로 쓰는 것** — `brand/profile.md` §2 고객이 사는/떠나는 이유 (주관식 테마 명명을 기존 언어와 정렬)
**나오는 곳** — `outputs/2026-09-15/070-nps-analysis/070-nps-analysis.md`
**나오는 형식** — 문서 `.md` (이 스킬의 고정 형식)
**걸리는 시간** — 3~4분
**멈추는 곳** — 이름·연락처를 전 구간 마스킹해서 다루는 개인정보 분석이라, 완성한 뒤 AI 규제검토자에게 개인정보가 새지 않았는지 검사를 맡깁니다. 비추천 응답 중 환불·분쟁 소지가 있는 건은 에스컬레이션 목록으로만 보여드리고 CS에 자동으로 연락하지 않습니다.

[승인 요청]
⏸ 이대로 갈까요? 진행하려면 `진행 승인`이라고 답해주세요.
````

## 이 재구성이 근거로 삼은 것

- **G2 계획 화면 규칙** — `plugins/marketing-team/skills/ai-마케터/SKILL.md` 558~596행. `[실행 계획]`~`[승인 요청]` 구조와 "gate:true든 pii:true든 같은 「멈추는 곳」줄에 적는다 — 개인정보만 남으면 「완성한 뒤 AI 규제검토자에게…」" 문구(589~591행)를 그대로 따랐다.
- **006 실측 예** — 같은 문서 598~629행. 「멈추는 곳」한 줄에 완료 후 규제검토뿐 아니라 실행 중 동작("환불·분쟁 소지 건은 목록으로만 보여드리고 CS에 자동으로 넘기지 않습니다")까지 함께 적은 전례를 그대로 따라, 070의 에스컬레이션 동작을 같은 자리에 녹였다.
- **061 실측 예의 줄 구성** — `100-skills/07-analytics/skills/061-sales-data-analysis/example/plan-screen.md`. 결과물 표 → 분석할 자료 → 근거로 쓰는 것 → 나오는 곳 → 나오는 형식 → 걸리는 시간 → 멈추는 곳 → 승인 요청 순서를 그대로 썼다.
- **070 SKILL.md Contract(37행)·게이트 표(52~59행)** — 점수 열이 0~10이 아니면 지어내지 않고 선형 환산 후 `[추정]`을 붙인다는 규칙, 점수 열·주관식 열 매핑을 모르면 추측하지 않고 멈춘다는 규칙, "개인 식별 정보는 전 구간 마스킹한다"(41행)는 원칙을 반영했다.
- **저장 경로·형식** — `writes_to`: `outputs/{날짜}/070-nps-analysis/070-nps-analysis.md`.
- **sample_fallback의 척도 불일치 경고** — 070 frontmatter의 `sample_fallback` 주석("이 파일의 점수 열은 별점(1~5)이라 0~10 NPS 척도가 아니다")을 그대로 「분석할 자료」 줄에 옮겼다.
