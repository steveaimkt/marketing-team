# 실습 계획 화면 · 066 KPI 체계 설계 (스펙 기반 재구성)

> ⚠️ **실제 대화 로그 캡처가 아니다.** 이 스킬은 원고소스에 실측 기록이 없어, SKILL.md 계약과
> ai-마케터 SKILL.md의 G1/G2 규칙에서 그대로 구성했다. 실제로 불러본 결과가 이것과 다르면
> 실제 결과가 맞는지, 이 재구성이 계약을 잘못 읽은 것인지 확인하고 필요하면 SKILL.md를 고친다.

> ⚡ **이 스킬은 「빠른 진입」 대상이다 — 계획 화면(`[실행 계획]`/`[승인 요청]`)이 뜨지 않는다.**
> 066은 `gate: false`·`mutating: false`이고 `pii` 필드가 없다. 산출물(지표 체계·임계값 표)
> 자체는 예산 확정·배분이나 계약·법률 표현이 아니라서 ⚡ 네 조건에 걸리지 않는다.
> 다만 066은 **목표·북극성 후보**처럼 스킬 자체가 실제로 필요한 재료를 Phase 1·2에서 묻는다 —
> SKILL.md 자신의 게이트 표가 "목표를 못 받으면 … 후보 3개를 제안한다 · **멈추지 않는다**"라고
> 명시하므로, ⚡ 경로에서도 이 부분은 화면을 세우지 않고 후보를 제안한 채로 계속 진행한다.

## 나

```
KPI 트리 만들어줘
```

## AI 마케터

````text
`inputs` 도 brand/profile.md 의 분기·연간 목표도 비어 있어, 고객 데이터
(`sample-data/A브랜드-고객마스터.csv`)의 추세로 목표 후보 3개를 만들고 그중 하나로 진행했습니다
(결과에 [샘플]) — 다른 목표가 있으면 "목표는 {문장}"이라고 다시 요청하시면 됩니다.

✅ KPI 체계 설계 완료

무엇이 나왔나
  📁 outputs/2026-09-15/066-kpi-tree/066-kpi-tree.md

무엇을 알게 됐나
 북극성 후보 3개를 "고객 가치 반영·매출 선행성·측정 가능성" 기준으로 비교하고, 커머스 기본값인 재구매 포함 월 구매자수로 확정했습니다
 북극성을 2~3단 곱셈/덧셈 구조로 분해해 말단 입력 지표(발행 수·광고비·발송 수 등) 최대 12개까지 내렸습니다
 지표마다 계산식·소스·주기·현재값과 경고/위험 2단 임계값(목표 경로 90%/80% 하회 기준)을 표로 냈습니다

무엇이 부족한가
 연습용 데이터의 추세로 목표 후보를 만들었습니다 — 실제 분기·연간 목표를 알려주시면 그 값으로 다시 만듭니다
 brand/profile.md §4가 비어 있어 임계값은 추세(최근 12주 평균 ±σ) 기준으로만 설계했습니다

다음 액션: → 062 마케팅 대시보드 작성 (이 지표 정의표 그대로 KPI 카드 스펙으로)
````

## 이 재구성이 근거로 삼은 것

- **⚡ 빠른 진입 판정** — `plugins/marketing-team/skills/ai-마케터/SKILL.md` 325~341행. mutating·pii·예산·법률 네 조건 모두 해당 없다.
- **066 SKILL.md 게이트 표 (46~50행)** — "Phase 1 ⏸ 목표가 무엇인가요? · 목표를 못 받으면 매출(고객) 데이터에서 추세를 읽어 목표 후보 3개를 제안한다 · 고르면 트리를 만든다 · **멈추지 않는다**"를 그대로 따랐다. Phase 2의 기본값("커머스면 재구매 포함 월 구매자수")도 그대로 썼다.
- **G4 보고 형식** — 같은 문서 921~965행 틀.
- **저장 경로·형식** — `writes_to`: `outputs/{날짜}/066-kpi-tree/066-kpi-tree.md`.
- **다음 액션** — `chains_to: ["062"]`.
