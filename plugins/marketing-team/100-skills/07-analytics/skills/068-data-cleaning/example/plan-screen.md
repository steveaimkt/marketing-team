# 실습 계획 화면 · 068 데이터 정리 (스펙 기반 재구성)

> ⚠️ **실제 대화 로그 캡처가 아니다.** 이 스킬은 원고소스에 실측 기록이 없어, SKILL.md 계약과
> ai-마케터 SKILL.md의 G1/G2 규칙에서 그대로 구성했다. 실제로 불러본 결과가 이것과 다르면
> 실제 결과가 맞는지, 이 재구성이 계약을 잘못 읽은 것인지 확인하고 필요하면 SKILL.md를 고친다.

> ⚡ **이 스킬은 「빠른 진입」 대상이다 — 계획 화면(`[실행 계획]`/`[승인 요청]`)이 뜨지 않는다.**
> 068은 `gate: false`·`mutating: false`이고 `pii` 필드가 없다. 원본은 절대 덮어쓰지 않고 항상
> 사본에서만 작업하므로 되돌릴 수 없는 실행(mutating)에 해당하지 않고, 예산·법률 표현도 없다.
> SKILL.md 자신의 Phase 3 ⏸("결측·이상치 처리 방식은 사람이 고른다")는 답이 없으면 가장 덜
> 파괴적인 "유지 + 표기"로 자동 완주하도록 기본값이 이미 정해져 있어, ⚡ 경로에서도 화면을
> 세우지 않고 그 기본값으로 진행한다.

## 나

```
데이터 정리해줘
```

## AI 마케터

````text
`inputs` 가 비어 있어 연습용 샘플(`sample-data/A브랜드-메타광고-30일.csv`)로 정리했습니다
(결과에 [샘플]). 결측·이상치는 가장 덜 파괴적인 "유지 + 표기"로 처리했습니다 — 제거·대체·보간으로
바꾸시려면 "결측은 {방식}으로"라고 다시 요청하시면 됩니다.

✅ 데이터 정리 완료

무엇이 나왔나
  📁 outputs/2026-09-15/068-data-cleaning/068-data-cleaning.csv (+ 068-data-cleaning-log.csv, 068-data-cleaning-해설.md)

무엇을 알게 됐나
 결측·중복·형식·이상치 4개 항목을 진단했고, 항목별 검출 건수를 요약에 표기했습니다
 원본은 건드리지 않고 사본(`{원본명}_cleaned`)에서만 작업했고, 변경은 전부 로그에 1행씩(원값→새값·규칙·사유) 남겼습니다
 처리 전후 행 수·매출 합계 대조표로 무엇이 왜 바뀌었는지 확인할 수 있습니다

무엇이 부족한가
 연습용 데이터라 실제 파일의 결측·이상치 패턴과는 다를 수 있습니다
 자동으로 판단하기 애매한 항목은 "잔여 이슈"로 따로 남겨 사람 확인을 기다립니다

다음 액션: → 061 판매 데이터 분석 (정리본으로 즉시 분석 시작)
````

## 이 재구성이 근거로 삼은 것

- **⚡ 빠른 진입 판정** — `plugins/marketing-team/skills/ai-마케터/SKILL.md` 325~341행. mutating·pii·예산·법률 네 조건 모두 해당 없다. "원본 파일은 절대 덮어쓰지 않는다"(Contract 35행)는 되돌릴 수 없는 실행이 아니라 오히려 그 반대(항상 사본)라서 mutating 조건에 걸리지 않는다.
- **068 SKILL.md 게이트 표 (50~54행)** — Phase 3 ⏸ 결측·이상치 처리 방식의 기본값 "유지 + 표기, 가장 덜 파괴적인 선택"을 그대로 썼다. Phase 5 뒤 "원본을 덮어쓸까요?" 질문의 기본값도 "사본으로만 낸다"이므로 화면에서 다시 묻지 않는다.
- **G4 보고 형식** — 같은 문서 921~965행 틀.
- **저장 경로·형식** — `writes_to`: `outputs/{날짜}/068-data-cleaning/068-data-cleaning.csv` · `-log.csv` · `-해설.md` 3파일.
- **다음 액션** — `chains_to: ["061"]`.
