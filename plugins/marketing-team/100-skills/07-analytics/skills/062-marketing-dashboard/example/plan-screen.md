# 실습 계획 화면 · 062 마케팅 대시보드 작성 (스펙 기반 재구성)

> ⚠️ **실제 대화 로그 캡처가 아니다.** 이 스킬은 원고소스에 실측 기록이 없어, SKILL.md 계약과
> ai-마케터 SKILL.md의 G1/G2 규칙에서 그대로 구성했다. 실제로 불러본 결과가 이것과 다르면
> 실제 결과가 맞는지, 이 재구성이 계약을 잘못 읽은 것인지 확인하고 필요하면 SKILL.md를 고친다.

> ⚡ **이 스킬은 「빠른 진입」 대상이다 — 계획 화면(`[실행 계획]`/`[승인 요청]`)이 뜨지 않는다.**
> 062는 `gate: false`·`mutating: false`이고 `pii` 필드가 없으며, 산출물에 예산 확정·배분이나
> 계약·법률 표현이 들어가지 않는다. ai-마케터 SKILL.md의 ⚡ 빠른 진입 네 조건(발송·게시 등
> 되돌릴 수 없는 실행 / 개인정보 / 예산 확정·배분 / 계약·법률 표현) 중 하나도 해당하지 않으므로,
> 요청 한 줄 뒤 곧바로 G4 보고로 간다. 아래는 그 보고 화면이다.

## 나

```
마케팅 대시보드 만들어줘
```

## AI 마케터

````text
`inputs/` 도 브랜드 프로필의 채널 데이터도 비어 있어, 연습용 샘플(가상의 A브랜드)로 돌렸습니다
(결과에 [샘플]). 핵심 지표는 기본 6종(매출·광고비·ROAS·전환·CPA·트래픽)으로 잡았습니다 —
다른 지표로 보시려면 "지표는 {목록}으로"라고 다시 요청하시면 됩니다.

✅ 마케팅 대시보드 작성 완료

무엇이 나왔나
  📁 outputs/2026-09-15/062-marketing-dashboard/062-marketing-dashboard.html

무엇을 알게 됐나
 KPI 카드 6개(매출·광고비·ROAS·전환·CPA·트래픽) — 값과 전기 대비 증감을 함께 표기
 추이·채널 막대·채널 구성비 변화(100% 스택)·광고비↔매출 산점도·요일×주차 히트맵까지 인라인 SVG 5종
 미연결 소스가 있으면 그 카드는 지우지 않고 회색으로 남겨 "데이터 없음"이라 적었습니다

무엇이 부족한가
 연습용 데이터라 실제 채널 구성·지표 값과 다를 수 있습니다 — 실데이터는 CSV로 `inputs/`에 넣고 다시 요청하세요
 자동 갱신은 걸지 않았습니다 — 갱신 가이드만 붙였고, 다음 갱신 시 같은 형식 CSV로 교체 후 재실행하면 됩니다

다음 액션: → 067 월간 마케팅 성과 분석 (이 지표 세트로 월간 보고 정례화)
````

## 이 재구성이 근거로 삼은 것

- **⚡ 빠른 진입 판정** — `plugins/marketing-team/skills/ai-마케터/SKILL.md` 325~341행. 네 조건(mutating·pii·예산 확정·법률 표현) 중 하나도 062에 해당하지 않아 계획 화면을 생략하고 바로 실행한다. "결과 첫 줄에 무엇으로 도는지 밝힌다"(338~340행) 규칙에 따라 [샘플] 여부를 첫 줄에 적었다.
- **G4 보고 형식** — 같은 문서 921~965행의 `✅ 완료 → 무엇이 나왔나(📁 경로) → 무엇을 알게 됐나(표+결론 3줄) → 무엇이 부족한가` 틀을 그대로 썼다.
- **062 SKILL.md Contract·게이트 표** — KPI 카드 4~8개(기본 6종), 차트 5종(추이·막대·100%스택·산점도·히트맵), 미연결 소스는 회색 카드로 남긴다는 규칙, 자동 갱신을 기본으로 걸지 않는다는 규칙을 그대로 인용했다.
- **저장 경로·형식** — `writes_to`의 단일 파일 `outputs/{날짜}/062-marketing-dashboard/062-marketing-dashboard.html` (오늘 날짜 2026-09-15로 치환). 062는 2026-09-15에 `.xlsx` 언급이 빠지고 단일 `.html` 스킬로 정정된 상태다 — 이 재구성은 그 정정 이후 상태를 반영한다.
- **다음 액션** — `chains_to: ["067"]`.
