# 실습 계획 화면 · 069 성과 예측 분석 (스펙 기반 재구성)

> ⚠️ **실제 대화 로그 캡처가 아니다.** 이 스킬은 원고소스에 실측 기록이 없어, SKILL.md 계약과
> ai-marketer SKILL.md의 G1/G2 규칙에서 그대로 구성했다. 실제로 불러본 결과가 이것과 다르면
> 실제 결과가 맞는지, 이 재구성이 계약을 잘못 읽은 것인지 확인하고 필요하면 SKILL.md를 고친다.

> ⚡ **이 스킬은 「빠른 진입」 대상이다 — 계획 화면(`[실행 계획]`/`[승인 요청]`)이 뜨지 않는다.**
> 069는 `gate: false`·`mutating: false`이고 `pii` 필드가 없다. 매출 시나리오는 어디까지나
> **내부 분석 초안**(전제·오차 범위·`[추정]` 태그가 붙는 계산기 역할)이지, 산출물이 확정된
> 예산 배분이 아니다 — Contract가 "매출 오릅니다류 보장 표현 금지"를 명시하는 것도 이 산출물이
> 확정이 아니라 범위 제시라는 뜻이다. ⚡ 네 조건 중 하나도 해당하지 않아 요청 한 줄 뒤 곧바로
> G4 보고로 간다.

## 나

```
다음 달 매출 시뮬레이션해줘
```

## AI 마케터

````text
`inputs` 가 비어 있어 연습용 샘플(`sample-data/A브랜드-채널성과-90일.csv`)로 돌렸습니다
(결과에 [추정]). 시나리오 변수 폭은 기본값(보수 광고비 -20%·전환율 -10% / 중립 현행 유지 /
공격 광고비 +50%·전환율 +10%·객단가 +5%, CPC +15% 가정)으로 잡았습니다 — 실제 계획(행사·
신제품 등)이 있으면 그 값으로 다시 요청하시면 됩니다.

✅ 성과 예측 분석 완료

무엇이 나왔나
  📁 outputs/2026-09-15/069-forecast-simulation/069-forecast-simulation.md

무엇을 알게 됐나
 기준선(최근 7일 가중 50% + 전체 평균 50%)에 과거 주간 변동폭 기반 오차 범위를 붙여 산출했습니다
 보수/중립/공격 3안 각각의 예상 매출·ROAS·마진 기여와 손익분기 여부를 표로 비교했습니다
 변수 1개씩 ±10%만 흔들어본 민감도 분석으로 매출에 가장 크게 영향받는 변수 1개를 지목했습니다

무엇이 부족한가
 연습용 데이터의 추세 기준이라 실제 시나리오 폭·오차 범위와 다를 수 있습니다
 "매출이 오릅니다" 같은 보장 표현은 쓰지 않았습니다 — 전제(효율 체감·시즌성 미반영 등)가 깨지면 범위도 깨진다는 점을 함께 적었습니다

다음 액션: → 095 마케팅 예산 설계 (선택 시나리오의 광고비를 월별 캐시플로로 전개)
````

## 이 재구성이 근거로 삼은 것

- **⚡ 빠른 진입 판정** — `plugins/marketing-team/skills/ai-marketer/SKILL.md` 325~341행. mutating·pii·예산 확정·법률 표현 네 조건 모두 해당 없다 (예산 확정이 아니라 범위·전제 제시).
- **069 SKILL.md 게이트 표 (50~54행)** — Phase 4 ⏸ 변수 범위 합의의 기본값을 그대로 썼다("보수 −20% / 중립 유지 / 공격 +50%"). Phase 7 뒤 질문의 기본값("중립안 제시 후 종료")도 화면에는 다시 묻지 않고 결과에 반영했다.
- **G4 보고 형식** — 같은 문서 921~965행 틀. "신뢰도를 함께 밝힌다"(974~991행) 규칙에 따라 [추정] 태그와 전제 한계를 결과에 남겼다.
- **저장 경로·형식** — `writes_to`: `outputs/{날짜}/069-forecast-simulation/069-forecast-simulation.md`.
- **다음 액션** — `chains_to: ["095"]`.
