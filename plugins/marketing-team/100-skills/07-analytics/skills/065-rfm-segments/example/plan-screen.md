# 실습 계획 화면 · 065 고객 등급 분석 (스펙 기반 재구성)

> ⚠️ **실제 대화 로그 캡처가 아니다.** 이 스킬은 원고소스에 실측 기록이 없어, SKILL.md 계약과
> ai-마케터 SKILL.md의 G1/G2 규칙에서 그대로 구성했다. 실제로 불러본 결과가 이것과 다르면
> 실제 결과가 맞는지, 이 재구성이 계약을 잘못 읽은 것인지 확인하고 필요하면 SKILL.md를 고친다.

> ⛔ **이 스킬은 ⚡ 빠른 진입 대상이 아니다.** `pii: true` — 고객ID·주문일·주문금액으로 RFM
> 등급을 매기는 개인 단위 분석이라, ai-마케터 SKILL.md 325~341행의 ⚡ 네 조건 중 「개인정보」에
> 걸려 계획 화면을 생략할 수 없다. 아래는 **065를 단독으로 부른 경우**(SKILL.md 자체 게이트
> 표의 "Phase 6 ⏸ (단독 호출)" 행)의 재구성이다 — 「리텐션캠페인」 체인(064→065→073→075→077)
> 으로 들어오면 이 질문을 내지 않고 곧바로 073(고객 여정 정리)으로 이어간다.

## 나

```
RFM 으로 고객 나눠줘
```

## AI 마케터

````text
[실행 계획]
**계획, 고객 등급 분석(RFM)**

| 순서 | 스킬 | 무엇이 나오나 |
|---|---|---|
| 1 | 스킬 065 · 고객 등급 분석 | RFM 점수표, 세그먼트 정의표(6~8종), 세그먼트별 액션 맵(목표→제안 액션→오퍼 방향→실행 스킬) |

**분석할 자료** — `inputs/` 폴더가 비어 있어 연습용 샘플(가상의 A브랜드)로 진행합니다. 결과에는 `[샘플]`이 붙습니다.
- 구매 이력: `sample-data/A브랜드-구매이력-12개월.csv` (고객ID·주문일·주문금액 3열 · 064와 같은 자료를 이어 읽습니다)

실데이터로 다시 하시려면 위 3열을 갖춘 구매 이력을 `inputs/`에 넣고 다시 요청하시면 됩니다.

**근거로 쓰는 것** — `brand/profile.md` §4 AOV·마진율·구매주기 (RFM 분위 기준선의 근거. 임의의 절대값을 업종 불문 적용하지 않습니다)
**나오는 곳** — `outputs/2026-09-15/065-rfm-segments/065-rfm-segments.csv` (+ `065-rfm-segments-해설.md`)
**나오는 형식** — 표 `.csv` + 해설 `.md` (이 스킬의 고정 세트)
**걸리는 시간** — 2~3분
**멈추는 곳** — 고객ID가 들어간 개인 단위 데이터라, 완성한 뒤 AI 규제검토자에게 개인정보가 새지 않았는지 검사를 맡깁니다. 고객ID는 실행마다 새로 만든 대체키로 바꾸고 원문↔대체키 대응표는 저장하지 않습니다. 분석이 끝나면 "먼저 공략할 세그먼트 2개를 제안합니다. 맞나요?" 를 한 번 더 확인받습니다(단독 호출 기준).

[승인 요청]
⏸ 이대로 갈까요? 진행하려면 `진행 승인`이라고 답해주세요.
````

## 이 재구성이 근거로 삼은 것

- **G2 계획 화면 규칙** — `plugins/marketing-team/skills/ai-마케터/SKILL.md` 558~596행. `[실행 계획]`~`[승인 요청]` 구조와 "gate:true든 pii:true든 같은 「멈추는 곳」줄에 적는다 — 개인정보만 남으면 「완성한 뒤 AI 규제검토자에게…」" 문구(589~591행)를 그대로 따랐다.
- **061 실측 예의 줄 구성** — `100-skills/07-analytics/skills/061-sales-data-analysis/example/plan-screen.md`. 결과물 표 → 분석할 자료 → 근거로 쓰는 것 → 나오는 곳 → 나오는 형식 → 걸리는 시간 → 멈추는 곳 → 승인 요청 순서를 그대로 썼다.
- **065 SKILL.md 자신의 Contract 4항(38~41행)·게이트 표(53~65행)** — 개인 식별 정보를 산출물에 노출하지 않고 실행별 임의 대체키를 쓴다는 규칙, "Phase 6 ⏸ (단독 호출)" 질문과 그 기본값("규모 × 매출 기여 상위 2개 제시하고 종료")을 「멈추는 곳」한 줄에 함께 녹였다(006 실측 예의 전례를 따름, ai-마케터 SKILL.md 616행).
- **저장 경로·형식** — `writes_to`: `outputs/{날짜}/065-rfm-segments/065-rfm-segments.csv` · `-해설.md`.
- **입력 3열·sample_fallback** — 065 frontmatter의 `inputs`·`sample_fallback` 그대로 (064와 동일 자료 공유).
