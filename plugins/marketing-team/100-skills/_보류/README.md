# 보류 · 쓰지 않는 스킬

여기 있는 것은 **라우팅에 안 잡힌다.** 지우지 않고 옮겨 둔 것이다.

| 폴더 | 왜 옮겼나 | 언제 |
|---|---|---|
| `059-crowdfunding-page` | `id: "059"` 가 `059-presale-inquiry` 와 겹쳤다. `SKILL.md` 가 101개인데 고유 id 는 100이었다. 원고 부록 A 의 059 는 「상품 문의 응대 가이드」이므로 그쪽을 남긴다 | 2026-09-04 |
| `052-detail-page-figma` | `id: "052"` 가 `052-detail-page-image-prompt` 와 겹쳤다. ROUTING.md 와 원고 부록 A 의 052 는 「상세페이지 이미지 프롬프트 작성」이므로 그쪽을 남긴다. 옮기고 나서 `SKILL.md` 100개 · 고유 id 100개 · 001~100 빠짐없음을 확인했다 | 2026-09-09 |

되돌리려면 폴더를 `100-skills/06-commerce/skills/` 로 옮기고 `id` 를 빈 번호로 바꾼다.

🔴 **`06-commerce/skills/` 에 남아 있던 `059-crowdfunding-page` 중복본은 2026-09-09 에 지웠다.**
여기 사본이 더 최신이다 — 2026-09-05 에 「판정 → 결과」 치환이 들어갔고, 산 자리 것은 2026-08-29 원본이었다.

🔴 **`052-detail-page-figma`는 2026-09-13에 통째로 지웠다(옮긴 게 아니라 삭제).** `_보류/`로 옮겨 놓아도 `plan-compiler.mjs`·`run-receipt.mjs`의 스킬 스캔이 이 폴더를 배제하지 않아, `id: "052"` 중복이 실행 중 계약검증을 조용히 깨뜨리는 사고가 실측으로 재현됐다(2026-09-13 · 8장 실측). 스캔 로직에 `_보류/` 배제를 추가하는 것과 별개로, 이 스킬(피그마 연동) 자체를 제품에서 완전히 뺀다는 사용자 결정에 따라 삭제했다. 백업은 `plugins/` 밖 저장소 루트 `_삭제백업_20260913/052-detail-page-figma/`에 있다.

🔴 **`059-crowdfunding-page`도 2026-09-14에 같은 이유로 지웠다.** `verify.mjs`의 스킬 개수 검사가 `_보류/`를 빼지 않고 세다 보니 "101개(기준 100)" 경고가 계속 떴다. 검사 로직에 예외를 추가하는 대신(규칙을 늘리지 않는 방향), 2026-09-04에 이미 "059-presale-inquiry를 남긴다"고 정해 둔 결정을 그대로 실행해 삭제했다. 백업은 `plugins/` 밖 저장소 루트 `_구버전백업_20260914/059-crowdfunding-page/`에 있다.
