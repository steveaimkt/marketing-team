# 092 나만의 스킬 만들기 계획 화면

> 이 화면은 모델이 짓지 않는다. `scripts/plan-compiler.mjs compile` 이 아래 「틀」과 계획(plan.json)과 스킬 선언으로
> 찍고, AI 마케터는 **한 글자도 바꾸지 않고** 그대로 낸다 (2026-09-15, 원고와 코워크 화면 100% 일치).
> 틀의 문구는 실측 2026-09-13(`원고소스/05_실측정리_원본자료/13장/092-101_20260913_5턴/대화로그/13장_run_turn2_기준답변.jsonl`,
> 세션 `52ab50b5-4d22-4db1-a2b8-d57b229f000f` line 409)의 내용을 옮겼다. 걸리는 시간은 같은 실측 3턴의 실제 7분 29초에 맞췄다.
> 범위 확정과 기준 확정은 이 화면보다 먼저 `SKILL.md` 「확인 질문 (그대로 낸다)」으로 묻는다. 확정한 판단 룰은 이 화면에 적지 않는다.
> 아래 「찍힌 화면」은 샘플 없음·빈 프로필·형식 선택 없음 조건의 출력이다. `test-plan-compiler.mjs` 가 글자 단위로 대조한다.

## 틀

```틀
무엇이 나오나: 새 스킬의 SKILL.md 초안과 부를 말 평가 파일(routing-eval.jsonl), 한 번 돌려 본 시범 실행 결과, 겹침 대조와 등록 심사 결과를 담은 제작 리포트
걸리는 시간: 5~8분
멈추는 곳 더: 시범 실행과 등록 심사 결과를 보여드리고, 「등록해줘」라고 답하셔야 brand/my-skills/에 넣습니다. 스킬을 만드는 동안 광고 계정 같은 외부 계정은 건드리지 않습니다.
함께 알려주면 좋은 것:
- 시범 실행 자료: 이 일에 매주 쓰는 자료 파일을 inputs/에 넣어 주시면 그 파일로 한 번 돌립니다. 없으면 연습용 A브랜드 자료로 돌리고 결과에 [샘플]을 붙입니다.
- 잘된 결과물: 지난번에 잘 끝난 결과물 2~3개가 있으면 inputs/에 함께 넣어 주세요. 새 스킬이 그 구조를 따릅니다.
```

## 나

```
이 업무로 스킬 만들어줘.
매주 광고 소재 보고 끌지 말지 정하는 일이야.
```

## 찍힌 화면

````text
[실행 계획]
계획: 나만의 스킬 만들기

무엇이 나오나
  스킬 092 · 나만의 스킬 만들기: 새 스킬의 SKILL.md 초안과 부를 말 평가 파일(routing-eval.jsonl), 한 번 돌려 본 시범 실행 결과, 겹침 대조와 등록 심사 결과를 담은 제작 리포트

근거로 쓰는 것: 브랜드 프로필이 비어 있어 연습용 A브랜드 프로필을 씁니다.
나오는 곳: outputs/2026-09-15/092-skill-builder-meta/
나오는 형식: 문서 .md · 평가 파일 .jsonl
멈추는 곳: 시범 실행과 등록 심사 결과를 보여드리고, 「등록해줘」라고 답하셔야 brand/my-skills/에 넣습니다. 스킬을 만드는 동안 광고 계정 같은 외부 계정은 건드리지 않습니다.
걸리는 시간: 5~8분

함께 알려주면 좋은 것
  시범 실행 자료: 이 일에 매주 쓰는 자료 파일을 inputs/에 넣어 주시면 그 파일로 한 번 돌립니다. 없으면 연습용 A브랜드 자료로 돌리고 결과에 [샘플]을 붙입니다.
  잘된 결과물: 지난번에 잘 끝난 결과물 2~3개가 있으면 inputs/에 함께 넣어 주세요. 새 스킬이 그 구조를 따릅니다.

[승인 요청]
⏸ 이대로 갈까요? 진행하려면 「진행 승인」이라고 답해주세요.
````
