# 실습 계획 화면 · 092 나만의 스킬 만들기

> 실측 2026-09-13 · `원고소스/05_실측정리_원본자료/13장/092-101_20260913_5턴/대화로그/13장_run_turn2_기준답변.jsonl`
> 원문(세션 `52ab50b5-4d22-4db1-a2b8-d57b229f000f`, line 409)에서 그대로 옮겼다. **이 스킬을 부르면
> 아래와 같은 구성·순서로 낸다** — 문장을 토씨까지 맞출 필요는 없지만, 어떤 줄이 있고 어떤 순서로
> 오는지는 이 예시를 따른다.

## 나

```
이 업무로 스킬(반복 업무 체인) 만들어줘. 업무 내용: 매주 광고 소재 성과를 보고 계속
운영할지(끌지) 중단할지 판단하는 일.
```

## AI 마케터

````text
[실행 계획]
계획, 광고 소재 주간 On/Off 판단 스킬 만들기

| 순서 | 무엇이 나오나 |
|---|---|
| 1 | 스킬 092 · 나만의 스킬 만들기 (Phase 2~7 진행) | SKILL.md 초안 + routing-eval.jsonl + 샘플 데이터로 1회 시범 실행 + 등록 심사 결과 |

- **입력**: `A브랜드-메타광고-30일.csv` (플러그인 샘플, `[샘플]`)
- **판단 룰(확정)**: 소재 단위 집계, 누적 노출 3,000 미만 제외, 신규 소재 첫 3일 제외
  - ROAS < 1.6 이 3일 연속 → **끈다(OFF)**
  - 1.6 ≤ ROAS < 2.0 → **소재만 교체**
  - ROAS ≥ 2.0 (손익분기, 공헌이익률 50%) → **유지**
- **사람 게이트**: 소재를 실제로 끄기(Off) 직전, 대상 소재 목록을 보여주고 ⏸ 확인 — 이 스킬은 판단만 내고 실제 매체 조작은 하지 않음
- **근거로 쓰는 것**: 공헌이익률 50% → 손익분기 ROAS 2.0 (사용자 제공)
- **나오는 곳**: `outputs/2026-09-13/092-skill-builder-meta/101-meta-ad-creative-weekly-decision/`
  - `SKILL.md` (등록 통과 시 `brand/my-skills/101-meta-ad-creative-weekly-decision/`로도 복사)
  - `routing-eval.jsonl`
  - 제작 리포트 `092-skill-builder-meta.md`
- **나오는 형식**: 문서 `.md` (스킬 본문 특성상 고정)
- **걸리는 시간**: 3~5분
- **멈추는 곳**: 없음 (사내 도구 제작물, 대외 발행물 아님 → 규제 검토 대상 아님). 등록 전 최종 심사 결과만 화면에 보여드립니다.

[승인 요청]
⏸ 이대로 갈까요? 진행하려면 `진행 승인`이라고 답해주세요.
````

## 이 예시에서 반드시 지킬 것

- **줄 구성 순서**: 계획 표(순서·무엇이 나오나) → 입력 → 판단 룰(확정) → 사람 게이트 → 근거로 쓰는
  것 → 나오는 곳 → 나오는 형식 → 걸리는 시간 → 멈추는 곳 → `[승인 요청]`.
- **"나오는 곳"은 `outputs/{날짜}/092-skill-builder-meta/{만들 스킬 slug}/`까지 적고, 그 아래
  실제로 나올 파일(SKILL.md·routing-eval.jsonl·제작 리포트)을 각각 줄로 나눠 적는다.**
- **"판단 룰"처럼 사용자가 이미 확정해 준 기준이 있으면 계획 화면에 그대로 옮겨 적어 승인 전에
  다시 확인시킨다.** 숫자·조건을 빠뜨리지 않는다.
- **"사람 게이트"에는 이 스킬이 만드는 산출물이 실제 매체 조작까지는 하지 않는다는 것을 명시한다.**
  무엇을 판단만 하고 무엇은 하지 않는지 경계를 분명히 적는다.
- **"멈추는 곳"에 규제 검토 대상 여부(대외 발행물이 아니므로 게이트 없음)를 적는다.** 대신 등록
  전 최종 심사 결과는 보여준다고 적는다.
- **계획 검증(plan-compiler) 오류 같은 내부 재시도 사정은 이 화면 어디에도 적지 않는다.** 원본
  세션에는 이 화면이 나오기 전 "[실행 계획]·[승인 요청] 표식이 없다"는 내부 게이트 재시도가
  두 차례 있었지만, 사용자에게는 보이지 않았던 부분이라 이 예시에도 옮기지 않는다.
