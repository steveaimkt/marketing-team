---
id: "041"
name: 쿠팡 광고 분석
description: "캠페인 데이터 → 낭비 키워드·입찰 조정안 (검증 자산). 주간/월간 쿠팡 광고 성과를 정리하고 예산 조정이 필요할 때 사용."
slug: coupang-ads-analysis
category: 05-ads
tier: EXPERT
triggers:
  - "쿠팡 광고 분석"
  - "쿠팡 캠페인 성과 봐줘"
  - "쿠팡 낭비 키워드 찾아줘"
  - "쿠팡 광고비 어디서 새는지 봐줘"
  - "쿠팡 입찰가 조정안 만들어줘"
inputs: [쿠팡 광고센터 키워드 리포트(xlsx·CSV), 스토어 일별 매출 요약(SELLER_INSIGHTS_DAILY_SUMMARY_METRICS · 선택), 분석 기간]
sample_fallback: sample-data/A브랜드-쿠팡광고-30일.csv   # `inputs` 를 먼저 보고, 없으면 **묻지 않고 바로** 이 파일로 완주한다 (산출물에 [샘플])
outputs: [KPI 성과 요약, 키워드 분류표(효자·낭비·잠재), 지면별·캠페인별·상품별 상세, 전환 0 키워드 목록, 그래프 3개, 입찰 조정안, 실행 계획(긴급·주간·월간), 저장 파일(.xlsx + .html + .md)]
requires: [brand/profile.md]
chains_to: ["046", "043"]
gate: false
mutating: false
writes_to: [outputs/{날짜}/041-coupang-ads-analysis/041-coupang-ads-analysis.xlsx, outputs/{날짜}/041-coupang-ads-analysis/041-coupang-ads-analysis.html, outputs/{날짜}/041-coupang-ads-analysis/041-coupang-ads-analysis.md]
builder: 사용자 (이 회사)
version: 1.0
persona: "16년차 이커머스 광고 운영자, 데이터로만 말하고, 낭비를 못 참는다"
when_to_use: "주간/월간 쿠팡 광고 성과를 정리하고 예산 조정이 필요할 때"
success_metrics: [낭비 키워드 절감액, 리포트 작성 시간, ROAS 개선폭]
---

# 041 쿠팡 광고 분석

> 쿠팡 광고센터 키워드 리포트 1개 → KPI 분해 → 효자/낭비/잠재 분류 → 입찰 조정안 + 긴급·주간·월간 실행 계획.
> 계승: 파트너사 「쿠팡 광고 분석 에이전트」의 KPI 공식 5종, 키워드 분류 체계, ROAS 원인 트리, 지면 분석 로직.

> **내 일이 아닌 것**: **쿠팡 한 매체**를 깊게 본다. 여러 매체 취합은 045, 예산 이동은 046.
> 나누는 말은 `docs/헷갈리는-쌍.md` 에 있다.

## Contract
- 결과 기준은 `brand/profile.md` §4 **손익분기 ROAS(=1/마진율)** 다. 절대값 하드코딩 금지. profile 미작성 시에만 원본 기본값(BEP 2.0·목표 3.0, **배수** 표기)을 쓰고 `[추정]` 태그를 붙인다. ROAS는 항상 배수로 쓴다 — `2.0`이지 `200%`가 아니다 (`sample-data/profile-sample.md` 규칙).
- 전환 지표는 **14일 전환 기준**으로 통일하고 산출물에 기준을 명시한다 (1일 전환과 혼용 금지).
- 낭비/효자 결론마다 근거 수치(광고비·주문수·ROAS)를 병기한다. 근거 없는 키워드 지목 금지.
- 입찰 조정안은 제안까지만. 실제 키워드 OFF·입찰 변경은 사용자가 광고센터에서 수동 실행한다.
- 같은 리포트를 다시 넣으면 같은 분류가 나온다 (분류 임계값을 산출물 ①에 명시, 재현성).

## 게이트 (대화형에서만 멈춘다)

> ⚠️ **이 스킬은 재료가 없으면 멈춘다.** 없는 것을 지어내면 산출물 전체가 거짓말이 된다.
> 대시보드·cron 에서는 아래 기본값으로 완주하고 산출물에 그 사실을 밝힌다.

> **재료는 세 가지다.** ① `sample-data/` 샘플로 구경 ② `inputs` 에 둔 **내 파일** ③ `brand` 정보.
> 답이 없으면 ①로 간다. **`inputs` 를 먼저 훑어 쓸 만한 파일이 있으면 짚어 보여 주고 고르게 한다.** 내 파일이 있는데도 샘플로 실행하는 일을 막는다.
> ⚠️ **고객사 자료를 넣기 전에 사내 정책을 확인한다.** 미공개 문건·개인정보 명단은 넣지 않는다. 판단이 서지 않으면 ①로도 전 과정이 완주된다.

| 멈추는 곳 | 묻는 것 | 기본값 (답 없으면) |
|---|---|---|
| Phase 2 (자료 없음) | `inputs` 에 리포트가 **전혀 없으면** 묻지 않고 `sample_fallback` 으로 완주한다 (frontmatter 참고) | 멈추지 않는다 · `sample_fallback` 으로 바로 완주 |
| Phase 2 (자료 형식 오류) | `inputs` 에 **캠페인·광고그룹 리포트만** 있으면(컬럼 구조가 달라 대체 불가) 아래 「질문 리포트 종류」 그대로 | 캠페인 리포트로는 대체 불가 · **D3 계약**을 낸다 (쿠팡 광고센터 → 보고서 → **키워드** 리포트로 다시 받기) |
| Phase 2 | **스토어 일별 매출 요약**(SELLER_INSIGHTS_DAILY_SUMMARY_METRICS)이 있으면 함께 읽는다 · 선택 자료다 | 없으면 **광고 의존도 카드와 핵심 ①을 내지 않는다** · 그 사실을 ⑫에 적는다 |
| Phase 1 | profile.md §4 에 손익분기 ROAS 가 없으면 아래 「질문 손익분기」 그대로 | **2.0** · 폴백값임을 산출물에 밝힌다 |
| Phase 7 · Phase 5 뒤 | 아래 「질문 조정 순서」 그대로 | 제안만 하고 종료 · **입찰을 건드리지 않는다** |

### 확인 질문 (그대로 낸다)

`{중괄호}` 만 실행 때 채운다. 나머지 글자는 바꾸지 않는다.

```질문 리포트 종류
⏸ inputs/의 {파일 이름}은 캠페인 리포트나 광고그룹 리포트라 열 구성이 달라 읽지 못합니다. 쿠팡 광고센터 → 리포트 → 키워드 리포트 → 기간 설정 → 다운로드로 다시 받아 inputs/에 넣어주세요.
```

```질문 손익분기
⏸ 브랜드 프로필에 손익분기 ROAS가 없습니다. 손익분기를 얼마로 볼까요? 답이 없으면 2.0으로 계산하고, 기본값을 썼다고 결과에 적습니다.
```

```질문 조정 순서
⏸ 입찰 조정안 {건수}건({대상과 조치})을 파일의 「다음 액션」에 담아 뒀습니다. 이 순서로 손볼까요, 다른 우선순위로 다시 볼까요?
```

## Phases
1. **기준 로드**: profile.md §4에서 손익분기 ROAS·목표 ROAS·AOV 자동 참조 (재질문 금지).
2. **데이터 확보**: `docs/데이터-가져오기.md` 기준 쿠팡은 공식 MCP 없음 → **광고센터 키워드 리포트 파일 업로드가 기본 경로** (광고센터 → 리포트 → 키워드 리포트 → 기간 설정 → 다운로드). ⚠️ 캠페인/광고그룹 리포트는 컬럼 구조가 달라 호환 불가. 형식 검증 후 진행. 실데이터 `[실데이터]` / 샘플 `[샘플]` 태그.
3. **KPI 계산**: CTR=클릭/노출 · CPC=광고비/클릭 · CVR=주문/클릭 · ROAS=매출/광고비 · CPA=광고비/주문. 캠페인×지표 매트릭스 + 일별 추이.
4. **키워드 분류**: BEP 기준으로 상대화한 분류 체계 (원본 임계값은 profile 미작성 시 폴백):

| 분류 | 조건 | 액션 |
|---|---|---|
| 효자 (Star) | ROAS ≥ 목표 ROAS + 주문 있음 | 수동 캠페인 등록·입찰 상향 |
| 양호 | BEP ≤ ROAS < 목표 + 주문 있음 | 현행 유지 |
| 잠재 | 주문 있으나 ROAS < BEP | 입찰 하향·상세페이지 개선 |
| 낭비 (Waste) | 광고비 ≥ 5만원 + 주문 0건 | 즉시 OFF 또는 제외 키워드 |
| 관찰 | 광고비 있음 + 주문 0건 (임계 미달) | 관찰 후 판단 |

5. **지면 분석**: 검색/비검색/리타겟팅 지면별 ROAS·CPC 비교 → 지면 입찰 가중치 제안.
6. **원인 트리 진단**: ROAS = CTR × CVR × AOV ÷ CPC 로 분해, 각 요소 상/중/하 결론 → 병목 우선순위 CTR→CVR→CPC→AOV.
7. ⏸ **실행 계획 확정**: 긴급(즉시 OFF 낭비 키워드) / 주간(입찰 조정) / 월간(캠페인 재편) 3단 계획을 제시하고 사용자 승인 후 마무리 → 체인: 046 예산 재배분(채널 차원) · 043 소재 카피 교체(CTR 병목 시).

8. **파일로 남긴다**: 위 산출물을 아래 「⛔ 착지」의 세 파일로 저장하고 경로를 알린다. 화면에만 띄우고 끝내지 않는다. 마케터가 다음 날 다시 열 수 있어야 한다.
   > 쓰기 권한이 없으면 **실패로 처리하지 않는다.** 산출물은 그대로 화면에 내고 맨 아래에 "`outputs/{날짜}/041-coupang-ads-analysis/041-coupang-ads-analysis.md` 로 저장하려 했으나 권한이 없어 남기지 못했습니다" 를 적는다. 못 한 일을 못 했다고 말하는 것도 산출물의 일부다.

> ⛔ **착지 · 세 파일로 쓴다**
> · 표 → `outputs/{날짜}/041-coupang-ads-analysis/041-coupang-ads-analysis.xlsx`
> · 화면 → `outputs/{날짜}/041-coupang-ads-analysis/041-coupang-ads-analysis.html`
> · 해설 → `outputs/{날짜}/041-coupang-ads-analysis/041-coupang-ads-analysis.md`
> 경로를 새로 만들지 않는다. 위 줄을 그대로 쓰고 `{날짜}` 만 오늘로 바꾼다.
> 아티팩트·스크래치패드·화면 출력은 착지가 아니다. **파일이 없으면 안 한 것이다.**
> **형식** · `.xlsx` 는 openpyxl 로 만든다 · 첫 시트는 「요약」, 표마다 시트 하나 · 머리 행 굵게 + 배경 `EBEBEB` · 틀 고정 A2 · 자동 필터 · **수는 수로 넣는다**("1,240" 은 합계가 안 돈다) (`docs/공통규약.md §H`)
> **`.html` 에는 그래프를 넣는다.** 표만 있는 화면은 어디가 새는지 한눈에 안 보인다.
> · **① 지면별 광고비와 전환매출** 막대: 검색·비검색·리타겟팅을 나눈다. 리포트에 `광고 노출 지면` 열이 있을 때만
> · **② 일별 광고비와 ROAS**: 막대(광고비) 위에 선(ROAS) 을 겹친다. 손익분기를 가로 점선으로 얹는 것은
> 　 **`profile.md` §4 에 BEP 가 있을 때만**이다. 없으면 기간 평균 ROAS 선을 긋고
> 　 「본전선이 없다」를 차트 아래 한 줄에 적는다
> 　 ⚠️ **리포트가 기간 합계만 주면 이 그래프를 넣지 않는다.** 없는 날짜를 지어내지 말고
> 　 「일별 추이는 리포트에 날짜 열이 없어 내지 못했다」를 한 줄로 적는다 (2026-09-10)
> · **③ 키워드 지출 상위 15** 가로 쌍막대: **광고비 내림차순**, 위 막대 = 광고비, 아래 막대 = 전환매출
>   **전환 0 키워드는 붉은 막대**로 칠한다. 막대 오른쪽에 광고비와 ROAS 를 적는다
>   그래프 아래 한 줄에 **전환 0 키워드가 몇 개이고 그 광고비 합계가 얼마인지** 적는다

> 🔴 **맨 위 한 판은 인쇄 요약판이다** (`docs/차트-규격.md` §3-4, 저자 2026-09-10).
> 화면 전체가 아니라 **첫 판 하나**가 책에 실린다. 그 판 안에서 결론이 나야 한다.
>
> **본보기**: `원고소스/02_실측/광고원자료/041-쿠팡광고-리포트-2026-02.html`
> 저자가 준 참고본이다. 아래 차례는 거기서 뽑았다.
>
> | | 첫 판에 넣는 것 |
> |---|---|
> | ① 머리 | 제목 · `[실데이터|샘플]` 배지 · **입력 = 무엇을 읽었나** · 기간(며칠) |
> | ② KPI 카드 **3개 + 결론 카드 1개** | 값 아래 **작은 글씨로 조건**을 붙인다 (「VAT 제외」·「14일 기준 · 주문 140건」) |
> | ③ 소제목 한 줄 | 이 판이 답하는 물음 (「광고비가 어디로 갔나」·「두 비중이 어긋나는 줄」) |
> | ④ 시각화 **1개** | 아래 스킬별 지정을 따른다 · **차트 아래 해석 한 줄**을 반드시 붙인다 |
> | ⑤ 핵심 표 **6행** | 넘으면 자르고 「나머지 N개는 파일에 있다」 |
> | ⑥ 닫는 한 줄 | **어디에 돈이 몰렸고 무엇이 그것을 벌었나** |
>
> · 화폭 **960 × 1200 (4:5)** · 글자 **20px 이상** · 형광 `#EBFF2C` 은 **판당 한 곳**
> ⛔ **화면을 그대로 축소하지 않는다.** 본문 15px 짜리를 줄이면 지면에서 4.25pt 가 된다. 첫 판은 다시 짠다.
> ⛔ **첫 판에 「핵심 N가지」 산문을 통째로 넣지 않는다.** 그 셋~넷은 아래 본문에 있고,
>    첫 판에는 그 가운데 **하나만** ⑥의 닫는 줄로 옮긴다.
> · KPI 카드 셋은 **광고비 · 광고 전환매출 · ROAS** 이고, 결론 카드는 **광고 의존도**다
> · 시각화는 **지면 3구분 가로 쌍막대**(위 = 광고비, 아래 = 전환매출)이고, 3구분은 검색·비검색·리타겟팅이다.
>   지면 열이 없으면 **키워드 지출 상위 6개**로 대신한다
> 🔴 **인라인 `<svg>` 로 직접 그린다.** 외부 차트 라이브러리를 부르지 않는다.
> 산출물은 인터넷 없이 더블클릭으로 열려야 한다. 값이 하나뿐이면 그래프를 만들지 않고 그 사실을 적는다.

> 🔴 **`.xlsx` 는 우리가 직접 만들지 않는다.** 표 내용은 여기서 만들고 **파일로 바꾸는 일만 앤트로픽 공식 `document-skills` 의 xlsx 스킬**에 넘긴다.
> 안 깔려 있으면 **`.csv` 로 내고 그렇게 말한다.** 설치 명령은 `/plugin marketplace add anthropics/skills`, `/plugin install document-skills@anthropic-agent-skills` 이다. ⛔ 설치를 강요하지 않는다.

## Output Format · **파일에 들어갈 내용**

⛔ **아래는 `outputs/{날짜}/041-coupang-ads-analysis/041-coupang-ads-analysis.md` 안에 들어갈 것이다. 화면에 그대로 쓰는 것이 아니다.**
파일에 쓰고 나서, 화면에는 **주요 표 하나 · 경로 · 결론 3줄 · 부족한 것**을 낸다 (표 말고는 15줄 이내).
```
## 쿠팡 광고 성과 리포트 — {기간} · [실데이터|샘플]
결론 요약: 총 광고비 {n}원 → 매출 {n}원 (ROAS {n}, BEP {n} 대비 {↑↓}) · 낭비 키워드 {n}개 · 즉시 회수 가능 {n}원/주

### ① 결과 기준 (재현용)
BEP ROAS {n} (profile.md §4) · 목표 ROAS {n} · 전환 기준 14일 · 낭비 임계 광고비 5만원+주문 0

### ② 핵심 {3~4}가지 — 참고본이 쓰는 꼴이다
  제목 한 줄(결론)과 그 아래 근거 문단 하나. **제목만 읽어도 무엇이 문제인지 알아야 한다.**
  예 · 「광고를 끄면 매출의 88%가 사라진다」 · 「검색 광고비의 절반이 전환 0 키워드로 갔다」

### ③ 지면별 광고비와 전환매출 (차트)   ### ④ 일별 광고비와 ROAS (차트)
### ⑤ 키워드 상위 15개 (차트)
  🔴 차트마다 **아래에 해석 한 줄**을 붙인다. 「위 막대 = 광고비 · 아래 막대 = 전환매출. 리타겟팅 지면은 광고비 0원인데 클릭 791회가 들어왔다」

### ⑥ 지면별 상세   ### ⑦ 캠페인별   ### ⑧ 상품별
### ⑨ 키워드 지출 상위 30   ### ⑩ 전환 0 키워드 · 지출 상위 20
  한 건도 못 판 곳에 돈이 얼마나 갔는지 따로 낸다

### ⑪ 다음 액션 ⏸
- 긴급: {낭비 키워드 n개 OFF → 주 {n}원 회수} / 주간: {입찰 조정 n건} / 월간: {캠페인 재편안}

### ⑫ 이 리포트를 읽을 때 주의할 것 — 해당하는 것만 적는다
  · 전환 건수가 30 미만이면 「결론할 표본이 아니다」를 먼저 적는다
  · 손익분기 ROAS 가 profile 에 없으면 폴백값을 쓴 사실과 그 값을 적는다
  · **광고유형이 섞여 있으면 그렇게 적는다** — 매출 최적화형은 키워드 입찰을 직접 바꿀 수 없다
  · 전환 기준(1일/14일)을 밝힌다 — 두 값이 다르게 나온다
  · **광고비는 부가세 별도다** — 실제 청구액은 10% 높다

다음 액션: → 046 ROAS 진단 (채널 간 이동) / 043 메타 광고 카피 작성 (CTR 병목 소재 교체)

저장 파일: outputs/{날짜}/041-coupang-ads-analysis/041-coupang-ads-analysis.md
```

## Anti-Patterns
- **1일 전환으로 결론**: 쿠팡 리포트의 1일/14일 전환을 섞으면 ROAS가 절반으로 보인다. 14일 기준 고정.
- **합산 ROAS만 보고 종료**: 캠페인 합산이 흑자여도 키워드 단위 낭비는 숨는다. 반드시 키워드 레벨까지 분해.
- **광고유형을 섞어 놓고 「입찰을 내려라」**: 매출 최적화형은 키워드 입찰이 없다. 조치가 유형마다 다르다.
- **업종 불문 300% 기준**: 마진 20% 브랜드의 BEP는 500%다. 기준은 항상 profile.md 손익분기 ROAS에서.
- **관찰 키워드 조기 처형**: 광고비 소액+주문 0을 바로 OFF하면 신규 키워드 학습 기회를 죽인다. 낭비 임계 미달은 관찰로.
- **분석만 하고 실행 경로 생략**: 조치별 광고센터 실행 위치(키워드 관리·수동 캠페인·지면 설정)까지 안내해야 리포트가 실행된다.

## 실습해 보기

```
쿠팡 광고 키워드 리포트 분석해줘
```

**키워드 리포트여야 한다.** 캠페인·광고그룹 리포트는 컬럼이 달라서 못 읽는다.
광고센터 → 리포트 → **키워드 리포트** → 기간 설정 → 다운로드.

## 활용
- **주간 루틴**: 매주 월요일 지난주 키워드 리포트 다운로드 → 본 스킬 실행 → 낭비 OFF·효자 수동 등록 → 046으로 주간 예산 재배분까지 한 흐름.
- **신제품 초기 진단**: 집행 2주차 리포트로 원인 트리만 먼저 본다. CTR 병목이면 043 소재, CVR 병목이면 051 상세페이지로 분기.
- **월간 캠페인 재편**: 월말 4주치 리포트로 캠페인 처방(확장/유지/축소/개편/상품개선)을 받아 다음 달 구조 확정.
