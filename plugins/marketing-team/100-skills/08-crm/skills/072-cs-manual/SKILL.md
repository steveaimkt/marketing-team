---
id: "072"
name: 고객 응대 매뉴얼 작성
description: "FAQ·상황별 스크립트 + 에스컬레이션 기준. 반복 문의·리뷰 응대를 표준 스크립트로 통일해야 할 때 사용."
slug: cs-manual
category: 08-crm
tier: PRO
triggers:
  - "CS 매뉴얼 만들어줘"
  - "상담 스크립트 정리해줘"
  - "응대 가이드 문서 만들어줘"
  - "리뷰 답변 템플릿 만들어줘"
inputs: [자주 오는 문의·리뷰 목록(로그·캡처), 정책 문서(배송·반품·환불), 고객센터 연락처·영업시간]
sample_fallback: sample-data/A브랜드-리뷰-200건.csv   # `inputs/` 를 먼저 보고, 없으면 **묻지 않고 바로** 이 파일로 완주한다 (산출물에 [샘플])
outputs: [3분류 응대 스크립트 세트(악성·문의·칭찬), FAQ, 에스컬레이션 기준표(수치화), 저장 파일(.docx)]
requires: [brand/profile.md]
chains_to: ["071"]
gate: false
mutating: false
writes_to: [outputs/{날짜}/072-cs-manual/072-cs-manual.docx]
builder: 사용자 (이 회사)
version: 1.0
persona: "고객센터 슈퍼바이저 10년차 · 상담사가 3초 안에 못 찾는 매뉴얼은 없는 매뉴얼로 친다"
when_to_use: "반복 문의·리뷰 응대를 표준 스크립트로 통일해야 할 때"
success_metrics: [건당 응대 시간 단축폭, 상담사 간 응대 품질 편차, 에스컬레이션 오판 건수]
---

# 고객 응대 매뉴얼 작성

> 문의·리뷰 3분류(악성🔴/문의🟡/칭찬🟢) → 유형별 글자수 제한 스크립트 → 수치화된 에스컬레이션 기준표.
> 계승: 디스코드 CS 응대 에이전트의 3분류 체계·감정점수(1~5)·복합 판정 규칙·유형별 답변 구조(200/300/150자)·금지사항.

> **내 일이 아닌 것** · **사람이 읽는 문서**다. 봇을 만드는 것은 071.
> 가르는 말은 `docs/헷갈리는-쌍.md` 에 있다.

## Contract
- 모든 문의·리뷰 유형은 3분류(악성/문의/칭찬) 중 하나에 배정되고, 복합 판정 규칙(악성+문의→악성 우선, 문의+칭찬→문의 우선, 불명확→문의)이 명시된다.
- 유형별 스크립트는 글자수 상한을 지킨다: 악성 200자 / 문의 300자 / 칭찬 150자 이내.
- 악성 스크립트는 사실 기반 응대만 담는다: 보상 약속·고객 주장 부정·변명·감정 대응 문구 금지.
- 에스컬레이션 기준은 수치로 정의된다 (감정점수·금액·반복 횟수) — "심하면 상담원 연결" 같은 모호한 기준 금지.
- 매뉴얼은 **넘길 건인지 먼저 가리는 순서**로 배열된다: 에스컬레이션 기준표 → 응대 분기도 → 분류 기준 → 유형별 스크립트 → FAQ.
- 응대 분기도를 **워드 3열 표**(`조건 → 판단 → 그러면`)로 싣는다. 한 행이 한 갈래다 (`docs/차트-규격.md` §3-3).
- 스크립트는 brand/profile.md의 톤(기본 해요체)을 따르고, 경쟁사 언급·내부 프로세스 노출 문구가 없다.

## 게이트 (대화형에서만 멈춘다)

> 사람이 부르면 아래에서 멈춰 답을 받는다. 대시보드·cron 에서는 기본값으로 완주하고
> 산출물에 "물어보지 못해 기본값으로 갔다"를 밝힌다.

> **재료는 세 갈래다.** ① `sample-data/` 샘플로 구경 ② `inputs/` 에 둔 **내 파일** ③ `brand/` 정보.
> 답이 없으면 ①로 간다. **`inputs/` 를 먼저 훑어 쓸 만한 파일이 있으면 짚어 보여 주고 고르게 한다** — 있는 줄 모르고 샘플로 도는 일이 없게.
> ⚠️ **고객사 자료를 넣기 전에 사내 정책을 확인한다.** 미공개 문건·개인정보 명단은 넣지 않는다. 판단이 서지 않으면 ①로도 전 과정이 완주된다.

| 멈추는 곳 | 묻는 것 | 기본값 (답 없으면) |
|---|---|---|
| Phase 1 | 고객센터 연락처·영업시간이 없으면 묻는다 | `brand/profile.md` §5 → 없으면 **A브랜드 값**으로 · 그것도 없으면 해당 문장을 **빼고** 「연락처 미정 — 확정 후 넣을 자리」를 맨 아래 적는다 · **플레이스홀더는 남기지 않는다** |
| Phase 2 | 문의 로그가 없으면 업종 표준 유형 + `[추정]` | 커머스 5유형 `[추정]` |
| Phase 6 ⏸ | "환불 자동 처리 상한을 얼마로 할까요?" | **묻고 멈춘다** · 금액 기준은 임의로 정하지 않는다 |

## Phases
1. **브랜드 컨텍스트 로드** — `brand/profile.md` 자동 참조 (톤·금기어·고객센터 정보). 연락처·영업시간 없으면 질문 (플레이스홀더 방치 금지).
2. **문의 유형 인벤토리** — 실제 문의·리뷰 로그에서 유형·빈도 집계. 로그가 없으면 업종 표준 유형으로 구성하고 [추정] 태그 (예: 커머스 = 배송/반품·교환/환불/제품/기타).
3. **3분류 체계 정의** — 유형별 판별 기준·키워드·감정점수(1: 분노 ~ 5: 열렬 추천)·긴급도 표 작성: 악성🔴(욕설·법적 언급·근거 없는 비방, 긴급도 높음) / 문의🟡(질문형·절차 확인, 보통) / 칭찬🟢(만족·재구매 의사, 낮음) + 복합 판정 규칙.
4. **유형별 스크립트 작성** — 구조 고정: 악성 = 공감·사과→해결 방안→고객센터 안내 (200자) / 문의 = 감사→직접 답변→번호 목록 절차→추가 문의 환영 (300자) / 칭찬 = 감사→고객이 칭찬한 포인트 인용→지속 이용 기대 (150자). 상황별 변형(배송·반품·제품·환불) 각 1개 이상.
5. **FAQ 작성** — 빈도 상위 문의 10개를 Q&A로 정리, 각 답변에 근거 정책 문서 참조 표기.
6. **에스컬레이션 기준표 + 응대 분기도 작성** — 즉시 연결(감정점수 1 + 법적 언급·금전 보상 요구·개인정보·동일 고객 3회 이상 반복) / 부분 응대(기준 금액 초과 환불·복합 문의·정책 확인 필요) / 자동 처리(표준 절차) — 금액 기준은 사용자에게 확정받는다 ⏸. 같은 판단을 **3열 분기도**(`조건 → 판단 → 그러면`)로 한 번 더 그린다 (`docs/차트-규격.md` §3-3).
7. **일하는 순서로 재배열 + 산출 + 체인 제안** — 만든 순서(3~6)가 아니라 **읽는 순서**로 문서를 조립한다: 에스컬레이션 → 분기도 → 분류 → 스크립트 → FAQ. 체인은 071 고객 응대 챗봇 설계 구축 (이 매뉴얼을 KB 소스로 투입해 자동응대로 확장).

### 운영 규칙 (매뉴얼에 함께 수록)
- 모든 응대는 로그로 남긴다: 일시 · 유형 · 감정점수 · 사용 스크립트 · 에스컬레이션 여부 (월 1회 빈도 재집계 → 인벤토리 갱신).
- 스크립트 수정은 매뉴얼 버전을 올리고 변경 이력 1줄을 남긴다 (구버전 응대 혼용 방지).
- 답글에는 출처·내부 문서명을 노출하지 않는다 (고객에게 보이는 채널이므로).

8. **파일로 남긴다** — 위 산출물을 `outputs/{날짜}/072-cs-manual/072-cs-manual.docx` 로 저장하고 경로를 알린다. 화면에만 띄우고 끝내지 않는다 — 마케터가 다음 날 다시 열 수 있어야 한다.
   > 쓰기 권한이 없으면 **실패로 처리하지 않는다.** 산출물은 그대로 화면에 내고 맨 아래에 "`outputs/{날짜}/072-cs-manual/072-cs-manual.docx` 로 저장하려 했으나 권한이 없어 남기지 못했습니다" 를 적는다. 못 한 일을 못 했다고 말하는 것도 산출물의 일부다.

> ⛔ **착지 · 여기로 쓴다** — `outputs/{날짜}/072-cs-manual/072-cs-manual.docx`
> 경로를 새로 만들지 않는다. 위 줄을 그대로 쓰고 `{날짜}` 만 오늘로 바꾼다.
> 아티팩트·스크래치패드·화면 출력은 착지가 아니다. **파일이 없으면 안 한 것이다.**

## Output Format · **파일에 들어갈 내용**

⛔ **아래는 `outputs/{날짜}/072-cs-manual/072-cs-manual.docx` 안에 들어갈 것이다. 화면에 그대로 쓰는 것이 아니다.**
파일에 쓰고 나서, 화면에는 **경로 · 결론 3줄 · 부족한 것**만 낸다 (15줄 이내).
**순서의 이유 · 상담원은 「이걸 내가 답해도 되나」를 먼저 가린다.** 넘길 건을 손에 쥔 채 스크립트부터 읽으면 늦는다 — 그래서 에스컬레이션이 1번, 분기도가 2번, 말할 내용은 그 뒤다.

```markdown
# 📞 CS 응대 매뉴얼: {브랜드} (v1.0, {작성일})

> 읽는 순서 = 일하는 순서. ① 넘길 건인가 → ② 어디로 가나 → ③ 무슨 유형인가 → ④ 뭐라고 말하나 → ⑤ 자주 묻는 것

## 1. 에스컬레이션 기준표
| 레벨 | 조건 (수치) | 처리 |
|---|---|---|
| 즉시 연결 | 감정점수 1 / 보상 요구 / 반복 3회+ | 스크립트 중단, 상담원 |
| 부분 응대 | 환불 {금액}원 초과 / 복합 문의 | 답변 + 고객센터 권유 |
| 자동 처리 | 표준 절차 · 위 어디에도 안 걸림 | 스크립트로 종결 |

## 2. 응대 분기도
| 들어온 것이 | 판단 | 그러면 |
|---|---|---|
| 욕설·법적 언급 | 감정점수 1~2 | ⛔ 즉시 상담원 연결 |
| 환불·보상 요구 | {금액}원 넘나 | 부분 응대 후 이관 |
| 개인정보·계정 문제 | 본인 확인 되나 | 확인 전엔 답하지 않는다 · 이관 |
| 같은 고객 3회째 | 직전 응대가 남아 있나 | ⛔ 즉시 상담원 연결 |
| 단순 문의 | FAQ 에 있나 | 4번 문의 스크립트 (300자) |
| 칭찬·재구매 언급 | 감정점수 4~5 | 4번 칭찬 스크립트 (150자) |
| 위 어디에도 안 걸림 | — | 문의로 분류 · ①로 |

## 3. 분류 기준
| 유형 | 판별 기준 | 키워드 | 감정점수 | 긴급도 |
|---|---|---|---|---|
| 악성 🔴 | 욕설·법적 언급·비방 | 사기, 최악, 신고 | 1~2 | 높음 |
복합 판정: 악성+문의→악성 / 문의+칭찬→문의 / 불명확→문의

## 4. 유형별 스크립트 (상황별)
### 악성 — 제품 불만 (200자 이내)
"안녕하세요, 고객님. 제품이 기대에 미치지 못해 진심으로 죄송합니다. …"
## 5. FAQ TOP 10
| Q | A (근거) |
## 다음 액션
→ 071 고객 응대 챗봇 설계 구축 (이 매뉴얼을 KB로 투입해 자동응대 확장)

저장 파일: outputs/{날짜}/072-cs-manual/072-cs-manual.md
```

### 도식화 · 2번 응대 분기도는 **워드 3열 표**로 그린다

`.docx` 는 `python-docx` 로 만든다. **인라인 SVG 가 들어가지 않는다.** 그래서 워드가 원래 잘 그리는 것으로 그린다 — 표다 (`docs/차트-규격.md` §3-3).

| | |
|---|---|
| 3열 고정 | `조건(들어온 것이) → 판단 → 그러면` · 한 행이 한 갈래다 |
| 되돌아오는 화살표 없음 | 「①로」라고 **글자로** 적는다. 선을 그려 얽으면 아무도 안 읽는다 |
| 갈래 수 | 일곱 개까지. 넘으면 표를 둘로 가른다 (악성 계열 / 일반 계열) |
| 순서 | **위험한 갈래가 위로.** 상담원 눈이 먼저 닿는 곳에 「즉시 연결」이 있어야 한다 |
| 색 | 셀 배경으로만 준다 · 머리 행 `#EBEBEB` · 강조 행 `#E0CCBE` · 사람이 확정할 자리(금액 칸) `#EBFF2C` · 글자 `#2A2A28` |
| 기호 | 색만으로 의미를 싣지 않는다 — 즉시 연결 행에는 ⛔ 를 함께 박는다 |

⛔ **분기도는 선택이 아니다. 문장으로 대체하지 않는다.**
"악성으로 판단되면 상담원에게 연결합니다" 같은 서술로 표를 지우는 순간 3초 안에 못 찾는 매뉴얼이 된다.
표가 빠졌으면 그 매뉴얼은 완성이 아니다.
⛔ **새 색을 만들지 않는다.** 위 네 값 밖의 색은 쓰지 않는다.

## Anti-Patterns
- **만능 스크립트 1개** — "죄송합니다. 고객센터로 연락주세요"를 전 유형에 돌려쓰면 매뉴얼이 아니라 회피다. 유형×상황별로 쪼갠다.
- **악성 리뷰에 감정 대응 문구** — 방어·반박 문장을 템플릿에 넣는 순간 사고가 복제된다. 사실+연락처만.
- **보상 약속 내장** — "환불해드리겠습니다"를 스크립트에 박으면 상담원 재량이 사라진다. 고객센터 안내로 대체.
- **글자수 무제한** — 긴 사과문은 플랫폼 답글에서 잘리고 변명처럼 읽힌다. 200/300/150 상한을 지킨다.
- **모호한 에스컬레이션** — "심각하면 보고"는 기준이 아니다. 점수·금액·횟수로 못 박는다.
- **스크립트를 앞에 두기** — 스크립트를 먼저 읽게 만들면 넘겨야 할 건을 상담원이 붙잡는다. 답을 찾은 뒤에 「아, 이건 이관이었네」는 이미 늦다. 에스컬레이션 기준표와 분기도가 문서 맨 앞이다.
- **분기도를 문장으로 풀기** — "상황에 따라 판단합니다"는 분기가 아니다. `조건 → 판단 → 그러면` 3열 표로 한 행씩 못 박는다.

## 실습해 보기

```
CS 응대 매뉴얼 만들어줘
```

**환불 자동 처리 상한 같은 금액 기준은 임의로 안 정한다.**
고객센터 연락처가 없으면 스크립트에 플레이스홀더를 남기지 않고 묻는다.

## 활용
- **플랫폼 리뷰 답글 운영**: 쿠팡·스마트스토어 리뷰에 유형 분류→스크립트 적용→붙여넣기 루틴으로 응대 시간을 건당 1~2분으로 줄인다.
- **외주·신입 인수인계**: 매뉴얼 1부로 CS 담당 교체 시 톤 붕괴를 막는다 (에스컬레이션 기준표가 권한 위임 문서 역할).
- **챗봇 이중화**: 071 챗봇의 KB 소스로 넣어 "사람 매뉴얼 = 봇 지식"을 단일 원본으로 유지한다.
