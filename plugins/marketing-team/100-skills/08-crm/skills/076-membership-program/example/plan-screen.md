# 076 멤버십 등급 설계 계획 화면

> 이 화면은 모델이 짓지 않는다. `scripts/plan-compiler.mjs compile` 이 아래 「틀」과 계획(plan.json), 스킬 선언으로
> 찍고, AI 마케터는 **한 글자도 바꾸지 않고** 그대로 낸다 (2026-09-15, 원고와 코워크 화면 100% 일치).
> 틀의 문구는 실측 2026-09-13(2차 클린룸, `원고소스/05_실측정리_원본자료/7장/076_20260913_2차_클린룸/대화로그/076재확인2_run.jsonl`)과
> 원고 7장 076 절(고객 880명 12개월 주문 이력, 기본 3등급, 이름과 연락처는 가리고 넣는다)의 내용을 옮겼다.
> ⛔ 이 화면보다 **먼저** 제 턴으로 「질문 065 먼저」를 낸다 (SKILL.md 「확인 질문」). 답을 받은 뒤에 이 화면을 낸다.
> 아래 「찍힌 화면」은 샘플·빈 프로필·형식 선택 없음 조건의 출력이다. `test-plan-compiler.mjs` 가 글자 단위로 대조한다.

## 틀

```틀
무엇이 나오나: 구매액 분위로 나눈 등급 구조표, 등급별 혜택 차등표, 혜택 원가를 뺀 등급별 실마진 시뮬레이션, 승급·강등·유지 규칙
샘플 자료: 연습용 A브랜드 주문 이력 12개월치(고객 880명, 주문 2,447건, 3,506행)
걸리는 시간: 2~3분
멈추는 곳 더: 혜택 원가가 마진을 넘는 등급은 제안하지 않고, 혜택을 줄이거나 경계를 올린 안을 함께 냅니다.
우리 자료 안내: 고객 ID, 주문일, 주문금액 열만 있으면 됩니다. 이름, 전화번호, 주소, 이메일 열은 지우거나 가린 뒤 넣어주세요.
우리 자료로 하려면:
- 주문 이력: 각 몰 관리자 → 주문 내역 → 기간 전체 엑셀 → inputs/orders-{기간}.csv
함께 알려주면 좋은 것:
- 등급 수: 기본은 3등급(VIP, GOLD, 일반)이고, 4등급을 원하면 「진행 승인. 4등급으로」처럼 함께 적어주세요.
- 마진율: 알려주면 실마진을 그 값으로 계산합니다.
```

## 나

```
고객 등급제 설계해줘.
```

## 확인 질문 (SKILL.md 「질문 065 먼저」 그대로)

```text
고객 등급 분석 결과가 없습니다. 등급 분석(065)을 먼저 하시겠어요?
```

## 나

```
아니, 그냥 진행해줘.
```

## 찍힌 화면

````text
[실행 계획]
계획: 멤버십 등급 설계

무엇이 나오나
  스킬 076 · 멤버십 등급 설계: 구매액 분위로 나눈 등급 구조표, 등급별 혜택 차등표, 혜택 원가를 뺀 등급별 실마진 시뮬레이션, 승급·강등·유지 규칙

분석할 자료: inputs/ 폴더가 비어 있어 연습용 A브랜드 주문 이력 12개월치(고객 880명, 주문 2,447건, 3,506행)로 돌립니다. 결과에는 [샘플]이 붙습니다.
근거로 쓰는 것: 브랜드 프로필이 비어 있어 연습용 A브랜드 프로필을 씁니다.
나오는 곳: outputs/2026-09-15/076-membership-program/
나오는 형식: 표 .xlsx · 화면 .html · 문서 .md
다른 형식: 표를 csv로 받으려면 「진행 승인. 표는 csv로」라고 답해주세요.
멈추는 곳: 혜택 원가가 마진을 넘는 등급은 제안하지 않고, 혜택을 줄이거나 경계를 올린 안을 함께 냅니다.
걸리는 시간: 2~3분

우리 자료로 하려면
  고객 ID, 주문일, 주문금액 열만 있으면 됩니다. 이름, 전화번호, 주소, 이메일 열은 지우거나 가린 뒤 넣어주세요.
  주문 이력: 각 몰 관리자 → 주문 내역 → 기간 전체 엑셀 → inputs/orders-{기간}.csv
  넣은 뒤 승인할 때 「진행 승인. inputs/파일이름」처럼 파일 이름을 함께 적어주세요.

함께 알려주면 좋은 것
  등급 수: 기본은 3등급(VIP, GOLD, 일반)이고, 4등급을 원하면 「진행 승인. 4등급으로」처럼 함께 적어주세요.
  마진율: 알려주면 실마진을 그 값으로 계산합니다.

[승인 요청]
⏸ 이대로 갈까요? 진행하려면 「진행 승인」이라고 답해주세요.
````
