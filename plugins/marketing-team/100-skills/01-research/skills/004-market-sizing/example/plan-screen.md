# 실습 계획 화면 · 004 시장 규모 조사 (스펙 기반 재구성)

> ⚠️ **실제 대화 로그 캡처가 아니다.** 이 스킬은 원고소스에 실측 기록이 없어, SKILL.md 계약과
> ai-marketer SKILL.md의 G1/G2 규칙에서 그대로 구성했다. 실제로 불러본 결과가 이것과 다르면
> 실제 결과가 맞는지, 이 재구성이 계약을 잘못 읽은 것인지 확인하고 필요하면 SKILL.md를 고친다.

## 나

```
시장 규모 추정해줘
```

## AI 마케터

````text
⚡ 빠른 진입 — 이 스킬은 [실행 계획] 화면 없이 바로 결과로 간다

004 시장 규모 조사는 mutating:false · pii 해당 없음 · 산출물에 예산 확정·배분 없음(TAM-SAM-SOM은
시장 크기 추정이지 마케팅 예산 배분이 아니다) · 계약·법률 표현 없음이라 네 조건에 하나도 걸리지
않는다. 그래서 계획 화면·승인 요청을 내지 않고, 요청 한 줄 뒤 곧바로 아래 같은 결과 보고로 간다.

TAM-SAM-SOM 추정입니다 · 연습용 A브랜드(화장품) 프로필 기준입니다 (결과에 [샘플])
brand/profile.md 가 비어 있어 sample-data/profile-sample.md 의 카테고리·타깃으로 시장을
"국내 20~40대 여성 온라인 세럼"으로 잡았습니다. (이 스킬 자체는 샘플 폴백 파일이 없어, 상위
ai-marketer 층의 프로필 폴백을 그대로 씁니다.)

| 구분 | 탑다운 | 바텀업 | 산식 |
|---|---|---|---|
| TAM | 1.2조 | — | 스킨케어 12조 × 패드류 10% [추정] |
| SAM | 3,600억 | 3,100억 | TAM×온라인60%×타깃50% / 480만명×2.6회×25,000원 |
| SOM | 18~54억 | — | SAM × 점유율 0.5/1.0/1.5% (채널 역산 근거) |

대표값 결론: SOM 18억 (보수 기준) · 괴리 배율 1.16배 → 수렴, 신뢰 가능

📁 outputs/2026-09-15/004-market-sizing/004-market-sizing.md

다음: 010 진입 시장 우선순위 정리 (SOM 시나리오를 Impact 입력으로)

우리 시장 정의로 다시 돌리려면 카테고리·지역·타깃을 한 줄로 알려 주시거나
brand/profile.md 를 채워 주세요.
````

## 이 재구성이 근거로 삼은 것

- **⚡ 자격 판정**: 004 SKILL.md 프런트매터 `gate: false`(17행) · `mutating: false`(18행) ·
  `pii` 필드 없음(=미해당) · `outputs`(14행)의 TAM-SAM-SOM 산출표는 시장 크기 추정이지
  "예산 확정·배분"이 아니어서 세 번째 조건도 해당 없음 →
  ai-marketer SKILL.md 325~336행 네 조건 전부 미해당, 화면 없이 빠른 진입.
- **샘플 폴백 경로**: 004 SKILL.md 프런트매터에는 다른 01-research 스킬과 달리
  `sample_fallback:` 필드 자체가 없다 (001·005·007·008·009는 있음). 그래서 냉시작 시
  스킬 자체 폴백이 아니라 ai-marketer SKILL.md 107~120행 "0. 브랜드 정보가 나를 바꾼다" 표의
  "프로필 빔 → sample-data/profile-sample.md, [샘플]" 규칙이 그대로 적용된다.
- **시장 정의·산출표**: 004 SKILL.md 게이트표(46행) "시장을 …로 잡았습니다" 기본값
  `brand/profile.md` 카테고리·타깃, Phase 2(53행) 정의 고정 규칙, Output Format(73~99행)의
  TAM-SAM-SOM 표·대표값 결론 문장을 그대로 축약해 가져왔다.
- **저장 경로**: `writes_to`(19행) `outputs/{날짜}/004-market-sizing/004-market-sizing.md`.
- **다음 액션**: `chains_to`(16행) 010, Output Format 다음 액션 줄(95~96행)과 동일.
- **되돌리는 문**: ai-marketer SKILL.md 353~354행 규칙 + 004 SKILL.md 게이트표(46행)의
  "시장 경계가 맞나요?" 질문을 재료 안내 형태로 반영.
