# 스킬 규격 (SPEC)

> **스킬 하나가 어떻게 생겨야 하는지의 정본.**
> `092 나만의 스킬 만들기` 와 `gates/quality-checklist.md` 가 이 문서를 따른다.
> 실측 기준 — 100개 전부가 아래를 지키고 있다 (`node scripts/validate-skills.mjs`).

## 0. 공식 스킬과 사용자 스킬 · 두 가지만 다르다

| | 공식 100개 | 사용자 스킬 (092 로 만든 것) |
|---|---|---|
| `id` | `001` ~ `100` | **`u01` ~ `u99`** |
| `category` | `01-research` 등 폴더명 | **`custom`** |
| 어디 사나 | `100-skills/{카테고리}/skills/` (패키지 · 읽기 전용) | **`brand/my-skills/`** (작업 폴더) |
| 검사 | `node scripts/validate-skills.mjs` | `node scripts/validate-my-skills.mjs {폴더}` |

**나머지는 전부 같다** — 필수 19 + 선택 1 · 따옴표 · 착지 규칙 · 게이트 · 본문 섹션.

## 1. frontmatter · 필수 19 + 선택 1

| # | 필드 | 무엇 | 예 |
|---|---|---|---|
| 1 | `id` | **공식** 3자리 `001~100` · **사용자** `u01~u99` · 따옴표 필수 | `"043"` · `"u01"` |
| 2 | `name` | 사람이 읽는 이름 | `메타 광고 카피 팩` |
| 3 | `description` | 한 줄 + 「~할 때 사용」 | `"…. 카피를 여러 안으로 뽑아야 할 때 사용."` |
| 4 | `slug` | 영문 케밥 | `meta-ad-copy` |
| 5 | `category` | **공식** 폴더명과 같아야 한다 · **사용자** 는 `custom` | `05-ads` · `custom` |
| 6 | `tier` | `EXPERT` · `SPECIALIST` · `CORE` | `EXPERT` |
| 7 | `triggers` | **3~6개** · 부르는 말 · 리스트 | `- "광고 카피 만들어줘"` |
| 8 | `inputs` | 무엇이 있어야 하나 · 없으면 어떻게 되나 | `[리뷰 CSV(내보내서 inputs/ 에), 기간]` |
| 9 | `outputs` | 무엇이 나오나 · **끝에 `저장 파일(.확장자)`** | `[…, 저장 파일(.md)]` |
| 10 | `requires` | 읽어야 하는 정본 | `[brand/profile.md]` |

⛔ **`requires` 가 없다고 멈추지 않는다.** 대체본이 정해져 있다.
그리고 **대체본이 없을 때도 멈추기 전에 셋을 낸다** — ①간 데까지 ②막힌 곳 ③뚫는 법
(`docs/공통규약.md §0-c D2·D3`). **「없어서 못 합니다」로 끝나는 답은 규격 위반이다.**

| 없는 것 | 대신 읽는 것 | 결과 |
|---|---|---|
| `brand/profile.md` | **`sample-data/profile-sample.md`** (가상의 A브랜드) | 완주 · `[샘플]` |
| `brand/tone.md` | 프로필의 §6 금기 · 없으면 공통 규정 | 완주 · 그 사실을 밝힌다 |
| 스킬별 데이터 | 그 스킬의 `sample_fallback` | 완주 · `[샘플]` |

**`[샘플]` 은 「데이터가 가짜」라는 표시지 「맥락이 없다」는 뜻이 아니다** —
샘플 모드에서도 업종·마진율(손익분기 ROAS)·타깃·경쟁사·금기어를 그대로 쓴다.
그것 없이 돌면 「ROAS 2.8」이 좋은지 나쁜지도 못 말한다.
정본은 `docs/공통규약.md §0-b`.
| 11 | `chains_to` | 다음 스킬 · **따옴표 필수** | `["047", "049"]` |
| 12 | `gate` | 대외 발행물인가 (AI 규제검토자 필수) | `true` / `false` |
| 13 | `mutating` | 상태를 바꾸나 (⏸ 필수) | `true` / `false` |
| 14 | `writes_to` | **착지 경로의 정본** (§2) | `[outputs/{날짜}/043-meta-ad-copy/043-meta-ad-copy.md]` |
| 15 | `builder` | 누가 만들었나 | `사용자 (이 회사)` |
| 16 | `version` | | `1.0` |
| 17 | `persona` | **몇 년차 + 무엇을 다르게 하는가** | `"12년차 퍼포먼스 카피라이터 — 예쁜 문장보다 클릭을 믿는다"` |
| 18 | `when_to_use` | 언제 부르나 | `"카피를 여러 안으로 뽑아야 할 때"` |
| 19 | `success_metrics` | 무엇으로 잘됐다 하나 | `[안 수, CTR 예측, 게이트 통과율]` |
| — | `sample_fallback` | **선택** · 외부 데이터를 요구하면 **필수** | `sample-data/A브랜드-리뷰-200건.csv` |

⛔ **`sample_fallback` 은 「선택」이지만, `inputs` 가 CSV·엑셀 같은 파일을 요구하면 없으면 안 된다.**
없으면 데이터 없는 사용자에게 스킬이 그냥 멈춘다. `validate-skills.mjs` 가 검사한다.

## 2. `writes_to` · 착지 경로의 정본

```
outputs/{날짜}/{id}-{slug}/{id}-{slug}.{확장자}
```

- `{날짜}` 만 실행 시각으로 바뀐다. **나머지는 그대로 쓴다**
- 확장자는 산출물의 성격이 정한다 — 대시보드 `.html` · 판매분석 `.csv` · 제안서 `.pptx`
- 외부 대상(`notion` · `figma` · `email`)은 경로가 아니라 목적지다. 함께 적어도 된다
- ⛔ `outputs/{카테고리}/` · `output/`(단수) · 날짜 없는 경로는 전부 위반

## 3. 본문 필수 섹션

| 섹션 | 무엇 |
|---|---|
| `## Contract` | **지켜야 할 약속 4~6개.** 검증 가능한 문장으로 |
| `## 게이트` | 어디서 멈추고 무엇을 묻나 · 답 없을 때의 기본값 |
| `## Phases` | **3단 이상.** 마지막에 **착지 블록**(§4) |
| `## Output Format` | 산출물 뼈대 · 사용자가 볼 모양 |
| `## Anti-Patterns` | **하지 말아야 할 것 3~5개.** 이유까지 |
| `## 실습해 보기` | 한 문장 예시 |
| `## 활용` | 실제 쓰임 2~3가지 |

권장 — `> **내 일이 아닌 것** · …` 경계 한 줄. **헷갈리는 쌍**(`docs/헷갈리는-쌍.md`)에 있으면 **필수**.

## 4. 착지 블록 · `## Phases` 끝에 반드시

```markdown
> ⛔ **착지 · 여기로 쓴다** — `outputs/{날짜}/{id}-{slug}/{id}-{slug}.{확장자}`
> 경로를 새로 만들지 않는다. 위 줄을 그대로 쓰고 `{날짜}` 만 오늘로 바꾼다.
> 아티팩트·스크래치패드·화면 출력은 착지가 아니다. **파일이 없으면 안 한 것이다.**
```

**왜 여기 또 적나** — 규칙이 문서 앞에 있으면 모델이 경로를 **재구성**한다.
행동하는 자리에 두면 복사하면 된다. 실측(2026-08-22)에서 이 한 블록이 21건 중 6건을 살렸다.

## 5. 실습 폴더 · `example/`

```
example/input.md    무엇을 넣었나
example/output.md   무엇이 나왔나
```

⛔ **`gate: true` 면 `output.md` 에 AI 규제검토자 판정 블록이 있어야 한다.**
없으면 독자가 게이트를 안 거쳐도 되는 줄 안다. `verify.mjs` 가 검사한다.

## 6. 사용자가 만든 스킬 · `brand/my-skills/`

`092` 로 만든 스킬은 **패키지 안에 넣지 않는다.** 업데이트하면 날아간다.

```
outputs/{날짜}/092-skill-builder-meta/     초안·검증 결과
brand/my-skills/u{NN}-{slug}/SKILL.md      채택본
brand/my-skills/INDEX.md                   명부 — AI 마케터가 공식 100개보다 먼저 본다
```

- **ID 는 `u01` 부터** (`u01`~`u99`) · `category: custom`
- `writes_to` 는 `outputs/{날짜}/u{NN}-{slug}/…`
- ⛔ **`gate:` 가 없으면 `true` 로 본다.** 내부 문서임이 `gate: false` 로 명시됐을 때만 건너뛴다.
  명부 밖 스킬일수록 검사가 없어지면 안 된다

`INDEX.md`는 아래 네 열을 유지한다. AI 마케터는 이 한 장으로 사용자 스킬을 먼저 찾고, 매칭된 파일만 연다.

```markdown
| ID | 이름 | 부르는 말 | 경로 |
|---|---|---|---|
| u01 | 경쟁 인스타 주간 변화 | 경쟁사 인스타 확인해줘 · 경쟁 계정 주간 기록 | brand/my-skills/u01-competitor-ig-weekly/SKILL.md |
```

- 사용자 문장이 **사용자 스킬의 등록 트리거와 정확히 일치**하면 사용자 스킬을 선택한다
- 의미가 비슷해 공식 스킬과 경합하면 **공식 스킬을 우선**하고, 구분이 안 될 때만 한 번 묻는다
- 등록할 때 `SKILL.md`와 `routing-eval.jsonl`을 같은 폴더에 둔다

## 7. 검사

```bash
node scripts/validate-skills.mjs   # 이 규격을 지키는가 (100개)
node scripts/build-routing.mjs --check   # 명부가 정본과 같은가
node scripts/verify.mjs            # 플러그인으로 설치해도 뜨는가
```
