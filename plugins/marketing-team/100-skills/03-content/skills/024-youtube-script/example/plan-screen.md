# 실습 계획 화면 · 024 유튜브 대본 작성 (스펙 기반 재구성)

> ⚠️ **실제 대화 로그 캡처가 아니다.** 이 스킬은 원고소스에 실측 기록이 없어, SKILL.md 계약과
> ai-마케터 SKILL.md의 G1/G2 규칙에서 그대로 구성했다. 실제로 불러본 결과가 이것과 다르면
> 실제 결과가 맞는지, 이 재구성이 계약을 잘못 읽은 것인지 확인하고 필요하면 SKILL.md를 고친다.
>
> 이 스킬은 **증거 유무가 형식(성과 증명형/과정 공유형/문제 해설형) 자체를 가른다.** ai-마케터
> SKILL.md의 2026-09-14 실측 수정(076 사례)에 따라, 이런 "실행 경로가 갈리는 질문"은 `[실행 계획]`
> 보다 **먼저 별도 턴**으로 자유 문장으로 묻고, 답을 받은 뒤에야 확정된 형식으로 `[실행 계획]`을 낸다.

## 나 · 1번째

```
유튜브 대본 써줘. 주제는 〈리뷰 200건을 하루 만에 분석한 방법〉
```

## AI 마케터 · 1번째

````text
증명 포인트부터 확인하겠습니다.

① 여기까지 했습니다   앵글 후보와 6섹션 틀은 잡아 뒀습니다 (주제: 리뷰 200건을 하루 만에 분석한 방법)
② 여기서 막혔습니다   Before/After 증거(예: "3시간 → 12분" 같은 수치)가 없어 "이렇게 해서 이렇게 됐다"를 못 씁니다
③ 이렇게 뚫습니다     시작·종료 화면 2장(날짜 보이게) + 그 기간 + 같이 바꾼 것 한 줄, 이 셋을 주시면
                      가장 강한 형식인 성과 증명형으로 만듭니다

없으면 지금은 과정 공유형(8분·결과를 약속하지 않는 형식)으로 먼저 드릴까요? 준비되는 대로 다시 부르셔도 됩니다.
````

## 나 · 2번째

```
지금은 없어. 과정 공유형으로 해줘.
```

## AI 마케터 · 2번째

````text
브랜드 프로필이 아직 온보딩 전(빈 템플릿)이라 **샘플(A브랜드, 화장품)** 기준으로 계획을 세웠습니다. 실행 전에 한 번 확인 부탁드립니다.

[실행 계획]
계획, 유튜브 대본 작성 (과정 공유형)

| 순서 | 무엇이 나오나 |
|---|---|
| 1 스킬 024 · 유튜브 대본 작성 | 8분 과정 공유형 대본(HOOK·WHY·PLAN·NOW·OPEN·CTA 6구간, 타임코드·화면 지시 포함), 글자 수 검산표, 규제 검사 결과 블록 |

- **형식**: 과정 공유형(②) — Before/After 증거가 없어 결과를 약속하지 않는 형식으로 갑니다. **증거가 오면 성과 증명형으로 다시 만듭니다** — 필요한 것: 시작·종료 화면 2장(날짜 보이게)·기간·같이 바꾼 것. 이 요청은 산출물 맨 아래에도 그대로 남깁니다.
- **근거로 쓰는 것**: `sample-data/profile-sample.md`(A브랜드, 화장품) — [샘플]. 경쟁 채널 데이터가 없어 앵글은 타깃 페인포인트 기반 [추정]입니다.
- **나오는 곳**: `outputs/2026-09-15/024-youtube-script/`
- **나오는 형식**: 대본 `.md` 한 벌. 다른 그릇은 없습니다.
- **멈추는 곳**: 완성한 뒤 AI 규제검토자에게 과장 성과·보장성 표현 위주로 규제 검토를 맡깁니다. ⛔ 차단이면 수정·재통과 전에는 전달하지 않습니다.
- **걸리는 시간**: 5~8분

말린다: 프로필이 비어 있어 **가상 브랜드 톤**으로 쓰는 초안이고, 증거가 없어 과정 공유형으로 진행합니다. 성과를 약속하는 문장은 쓰지 않으며("이렇게 하고 있습니다"까지만), 증거가 오면 처음부터 다시 만들어야 합니다.

[승인 요청]
⏸ 이대로 갈까요? 진행하려면 `진행 승인`이라고 답해주세요.
````

## 이 재구성이 근거로 삼은 것

- **frontmatter**: `triggers` · `inputs`(주제·시연 소재, 목표 시청자, 근거 데이터 — 없으면 형식이 바뀐다 — 목표 길이) · `sample_fallback: sample-data/A브랜드-유튜브검색-61편.csv`(단, 이 재구성은 "완전 콜드스타트"가 아니라 "주제는 있으나 증거가 없는" 시나리오라 이 파일은 앵글 근거로만 쓰이고 본문엔 명시하지 않았다) · `outputs` · `writes_to`(md 1개) · `gate: true` · `mutating: false` · `chains_to: ["025","026"]`
- **Contract·"형식은 증거가 정한다" 절**: 3형식 표, "증거를 먼저 요청한다"(HANDOFF ①②③ 그대로 인용), "⏸ 요청을 낸 뒤에 형식을 묻는다", "②·③으로 갔어도 요청을 지우지 않는다"
- **게이트 표**: Phase 2(증거 없으면 먼저 HANDOFF, 답 없으면 과정 공유형으로 진행하며 요청은 유지)
- **Phases 2**: "증거가 비면 먼저 HANDOFF로 요청한다 ⏸. 요청을 낸 뒤에야 형식을 묻는다"
- **⛔착지**: `outputs/{날짜}/024-youtube-script/024-youtube-script.md`
- **ai-마케터 SKILL.md**: G1 "⛔ 단, 스킬 자신의 게이트가 '무엇을 다시 실행할지' 자체를 가르는 질문이면 계획 각주로 접지 않는다"(2026-09-14 076 실측 수정) — 형식 분기 질문을 `[실행 계획]`보다 먼저, 자유 문장 답으로 받는 별도 턴으로 처리한 근거 · G2 계획 템플릿
- **⚡ 빠른 진입 미적용 판단**: `gate: true`라 023과 같은 기준으로 전 화면 계획을 냈다. 형식 분기 질문 자체도 4조건(mutating·pii·예산·계약)과 무관하게 "실행 경로 결정" 규칙(G1)에 따라 화면 앞에 따로 뗐다.
