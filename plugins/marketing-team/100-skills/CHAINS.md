# 교차 체인 · 카테고리를 넘나드는 체인 (정본)

> 팀 하나에 속하지 않아 `PLUGIN.md` 에 둘 수 없는 체인만 여기 적는다.
> **카테고리 체인 10종의 정본은 각 팀의 `100-skills/{팀}/PLUGIN.md`** (`chain` · `chain_steps` · `chain_desc`).
> `ROUTING.md` 의 체인 표는 이 파일과 PLUGIN.md 에서 **생성**된다 · `node scripts/build-routing.mjs`

| 체인 | 순서 | 무엇을 하나 |
|---|---|---|
| **VoC애널리틱스** | `006 → 078 → 020 → 054` | 소비자 리뷰 분석 → 고객 의견 정리 → 제품 개선 우선순위 정리 → 리뷰 관리 |
| **브랜드셋업** | `081 → 082 → 008 → 012` | 브랜드 스토리 작성 → 브랜드 가이드 작성 → 고객 유형 정리 → 가치 제안 정리 |
| **유튜브프로덕션** | `005 → 024 → 025 → 026` | 트렌드 조사 → 유튜브 대본 작성 → 유튜브 제목·썸네일 기획 → 숏폼 대본 작성 |
| **인플루언서캠페인** | `033 → 034 → 037 → 027` | 인플루언서 조사 → 인플루언서 협업 설계 → 고객 콘텐츠 수집 설계 → 콘텐츠 채널별 재작성 |
| **위기커뮤니케이션** | `038 → 089 → 032 → 083` | 소셜 언급 분석 → 위기 대응 설계 → 댓글·메시지 응대 관리 → 보도자료 작성 |

> 부를 말은 「{체인 이름} 돌려줘」 · 슬래시 명령이 아니다.

## 체인의 산출물은 프로젝트 폴더로 묶는다

**스킬 하나는 날짜에, 체인은 프로젝트에 떨어진다.**

체인은 여러 날에 걸치기 쉽다. 「브랜드셋업 돌려줘」 네 단계가 8/20·8/22·8/25 로 나뉘면
날짜 폴더 세 개에 흩어지고, 다음 달에 되짚으려면 날짜를 전부 뒤져야 한다.

```
outputs/2026-08-신제품런칭/
├── 00-브리프.md          체인을 시작할 때 쓴다 · 무엇을 왜
├── 01-081-브랜드스토리.md
├── 02-082-브랜드가이드.md
├── 03-008-고객유형.md
└── 04-012-가치제안.md
```

- 앞 번호는 **돌아간 순서**다. 파일 이름만 훑어도 무엇이 무엇에서 나왔는지 보인다
- 폴더 이름은 `{YYYY-MM}-{이름}` 을 권한다 — 시간순 정렬 + 언제 일인지 바로 안다
- 체인을 시작할 때 **폴더 이름을 한 번 묻는다.** 답이 없으면 `{YYYY-MM}-{체인이름}` 으로 간다

⚠️ **단일 스킬은 그대로 날짜 축이다** (`outputs/{날짜}/{번호}-{슬러그}/`). 두 축은 섞지 않는다.
매주 반복하는 것(주간 광고 리포트 등)도 날짜에 쌓아야 추이가 보인다.

끝난 프로젝트는 `archive/` 로 옮긴다. 지우지 않는다.
