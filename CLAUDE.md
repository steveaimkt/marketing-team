# 이 저장소에서 일하는 규칙

> **여기는 제품이다.** 코워크·클로드 코드로 배포되는 플러그인 `marketing-team` 이 산다.
> 고친 것이 곧 사용자에게 간다. 아래는 **여기서 실제로 사고가 났던 자리**만 적었다.

---

## 0. 무엇이 어디에 있나

```
plugins/marketing-team/        ← 배포되는 본체. 여기 것만 사용자에게 간다
  skills/          진입 스킬 3 (등록됨)   ai-마케터 · 마케팅팀-구축하기 · 마케팅팀-업무리스트
  agents/          판정 담당 3 (등록됨)   staff-*
  100-skills/      업무 스킬 100 (일부러 등록 안 함 · 명부만 상시)
  docs/            공통규약 · 헷갈리는-쌍 · 스킬명-대조표 …
  scripts/         검사·생성 도구
.githooks/         커밋 훅 (bootstrap.mjs 가 켜 준다)
```

**정본이 어디인지 헷갈리면 이 순서다** — `SKILL.md` > `docs/` > 생성물(`ROUTING.md`·`카탈로그.html`).
생성물은 손으로 고치지 않는다. `build-routing.mjs`·`build-catalog.mjs`·`build-stats.mjs` 가 만든다.

---

## 1. ⛔ 부를 말(`triggers:`)은 문체 규칙의 대상이 아니다

`triggers:` 는 **사용자가 하는 말**이지 우리가 쓰는 문구가 아니다.
문체를 고친다고 여기를 건드리면 **라우팅이 조용히 달라진다.**

```
2026-08  「AB 테스트 결과 판정」을 문체 규칙으로 바꿨다가 라우팅이 흔들렸다
```

바꿔야 할 이유가 생기면 **바꾼 뒤 반드시** `eval-routing.mjs --live-cc` 로 재본다.

---

## 2. 문체 규칙은 원고의 기계 표를 그대로 쓴다

이 제품의 문구는 책 원고와 같은 규칙을 따른다. **정본은 원고 쪽에 있다.**

```
글 정본   …/100skills/최종원고/20260829_v5/_작업/문장규칙_저자지시_20260905.md
기계 정본 …/100skills/최종원고/20260829_v5/_검증/규칙표.py      ← 이걸 돌려라
```

금지 낱말 예 — 판정 · 갈리다 · 가르다 · 되짚 · 걸음 · 슬롯 · 위임 · em dash(—)

⛔ **규칙표의 예외를 넘지 마라.** `[가-힣]{2,6}·[가-힣]{2,6}` **붙여 쓴 짧은 나열**과
표 행(`^|`), 백틱 안은 그대로 둔다.

```
2026-09  「원가·마진율」·「상품명·검색 최적화」(스킬 이름!) 까지 쉼표로 바꿔 되돌렸다
```

---

## 3. 스킬 설명은 1,024자를 넘지 않는다

넘으면 **코워크가 그 스킬을 통째로 등록하지 않는다.** 오류도 로그도 안 남는다.
클로드 코드는 통과시켜서 로컬에서는 멀쩡해 보인다.

```
2026-09-07  ai-마케터 설명 1,178자 → 코워크 목록에서 사라짐 → 688자로 줄이니 떴다
```

`verify.mjs` 가 잰다. 🔴 1,024 초과 · 🟡 870 초과.

---

## 4. 커밋 전에 검사 넷을 돌린다

```bash
node plugins/marketing-team/scripts/verify.mjs           # 설치해도 그대로 뜨는가
node plugins/marketing-team/scripts/validate-skills.mjs  # 스킬 100개가 계약대로인가
node plugins/marketing-team/scripts/eval-routing.mjs     # 부를 말이 제 스킬로 가는가
node plugins/marketing-team/scripts/build-stats.mjs --check
```

커밋 훅이 자동으로 돌린다. 라우팅 파일(`SKILL.md`·`routing-eval.jsonl`·`docs/`)을
건드렸으면 **B층 60건**까지 돈다 (맥스 구독 · 추가 결제 없음 · 약 2분).
급하면 `SKIP_LIVE=1 git commit …` 이지만, **나가기 전에 한 번은 밟아라.**

생성물이 어긋나면 다시 만든다 — `build-routing.mjs` → `build-catalog.mjs` → `build-stats.mjs`.

---

## 5. 나갈 때는 버전을 올린다 · 두 곳 함께

```
plugins/marketing-team/.claude-plugin/plugin.json
.claude-plugin/marketplace.json
```

**코워크는 `marketplace.json` 의 버전만 본다.** 안 올리면 고쳐도 기존 사용자에게 안 간다.

```
2026-09-07  배포 파일 404개를 닷새간 커밋 안 한 채로 두었다. 원격은 09-02 그대로였고
            코워크의 「새것 없음」은 맞는 판정이었다
```

`verify.mjs` 가 검사한다 — 로컬은 🟡(커밋을 막지 않는다) · CI 는 🔴.

---

## 6. 배포 폴더에 git 이 모르는 파일을 두지 않는다

`plugins/` 안의 untracked 파일은 **로컬에만 있고 사용자에게는 안 간다.**
고쳤는데 왜 안 바뀌냐가 여기서 나온다.

백업은 `plugins/` **밖**에 둔다. `.전_20260905` · `.bak` · `_보류/` 를 안에 두지 마라.

---

## 7. 이 저장소는 여러 세션이 동시에 만진다

커밋 안 된 파일이 수백 개일 수 있다. **내 것이 아닌 변경 위에 올라타지 마라.**

- 중복 id · 개명 잔재가 보이면 **먼저 무엇이 정본인지 확인한다**
  정본 판정 순서 — **원고 > `PLUGIN.md` > `ROUTING.md` > `스킬명-대조표.md`**
  (대조표는 낡을 수 있다. 2026-09-07 에 052·059 가 그랬다)
- 남의 작업 중 폴더를 지우기 전에 **백업하고, 왜 지우는지 커밋 메시지에 적는다**
- 다른 세션에 알릴 일은 `SendMessage`. 원고 쪽 파일(`100skills/`)은 그쪽이 정본이다

---

## 8. 이름을 바꿀 때는 짝을 함께 옮긴다

`SKILL.md` 의 `name:` · 본문 H1 · 폴더명 · `PLUGIN.md` · 참조 문서가 한 벌이다.
공유 용어(`나누는 말` 같은 것)는 **한쪽만 바꾸면 문서끼리 어긋난다.**

맥은 대소문자를 구분하지 않는다. `AI-x` → `ai-x` 는 `git mv` 를 **두 단계**로 한다.

---

## 9. 하지 않는 것

- **평가셋에 맞춰 트리거를 늘리지 않는다.** 점수는 오르고 실사용은 나빠진다
  (2026-08 · 고아 스킬 24개 처방이 전수 검증에서 근거가 무너져 적용하지 않았다)
- 판단 기준을 새로 만들면 **그것이 무엇이든 구분하는지 먼저 확인한다**
  (「eval 커버리지 0.15 미만」은 고아 75% · 비고아 75% 로 아무것도 못 갈랐다)
- `실제확인-기록/` 은 지난 실측이다. **과거를 고쳐 쓰지 않는다**
